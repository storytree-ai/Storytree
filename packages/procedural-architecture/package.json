{
  "name": "@storytree/procedural-architecture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The procedural-architecture organism: buildings grown from declared structural relations rather than typed coordinates. Pure math + string building — a shape/builder core, a physics invariant checker, a deterministic draw-order pass, one bake with two printers (SVG document / scene drawables), and three parametric buildings. Zero runtime dependencies, browser-safe, depends on no other workspace package.",
  "exports": {
    ".": "./src/index.ts",
    "./kit.json": "./baked/kit.json",
    "./stone.json": "./baked/stone.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 300000 src/",
    "bake": "node --import ../../scripts/tsx-cache-off.mjs --import tsx scripts/bake-kit.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~7.0.2"
  }
}

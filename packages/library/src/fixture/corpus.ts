// The BOOTSTRAP FIXTURE CORPUS — a small, frozen, hand-picked set of library artifacts.
//
// ADR-0302 D1 makes the live Cloud SQL store the only source of truth and decommits
// `apps/studio/data/knowledge.json`. But ADR-0302 D3 deliberately keeps `STORYTREE_DB_USER` OUT of
// `pnpm -r test` (it is set per-step in ci.yml, never at job level) so the unit suites stay
// HERMETIC. Those two rules together mean the suites cannot follow the generators onto the live
// store — they need a corpus that needs no credential.
//
// This is that corpus, and the three properties that make it safe are all deliberate:
//
//   SMALL     — 22 artifacts, one per shape a suite actually names, rather than a mirror of the
//               219-unit seed it replaces. Nothing here is load-bearing for production.
//   FROZEN    — it is NOT regenerated from live and NOT reconciled against it. Drift is the intended
//               state, not a defect: a fixture that tracked live would re-create exactly the
//               seed↔live coupling ADR-0302 D4 deleted the machinery for. Nothing here is a claim
//               about what the Library currently says.
//   CLOSED    — every `asset:` ref resolves WITHIN this set (dangling ones were dropped on
//               extraction), so nothing here needs a transitive closure maintained behind it.
//
// It is TYPED and inline rather than a JSON file on disk on purpose: no path resolution, no
// `STORYTREE_REPO_ROOT`, no repo-root derivation — so it works from a worktree, a foreign checkout,
// or a bundle, which is precisely where the departing seed's `dataPath()` was fragile.
//
// WHAT DEPENDS ON WHICH ENTRY, because a "tidy the fixture" pass would otherwise break suites in
// another package: `edit-first-curation` and `pull-based-context-architecture` are named by id in
// the CLI's dashboard/doctrine assertions; `glossary-wins` carries the `doc:` refs the tree-focus
// OUTBOUND test needs; `trunk` → `approval-gated-trunk` is the INBOUND back-edge pair;
// `live-store-is-the-edit-surface` is the doctrine pointer a write-without---pg refusal prints;
// `merge-ceremony` is kept for its genuinely multi-KB `steps` field (the `--raw` byte-exactness
// test) and its real `surfaces` prose; `deep-modules` is what the studio UAT searches for. The two
// `adr` rows are the studio UAT's DECISION tier (ADR-0425 dec 4): its walkthrough opens a decision
// through the Library lens, hops `adr-0013` → `adr-0002` as an in-corpus cross-link, and follows
// `deep-modules`'s own `asset:adr-0002` source into the decision behind it. They are here because
// ADR-0403 dec 1 made decisions ordinary artifacts and deleted `docs/decisions/`, so the offline
// sandbox had NO decision to open at all and that leg of the journey had no subject. Their bodies
// are deliberately abridged: this is a sandbox seed, not an archive. The four
// AGENTS are here because `drive` RENDERS them into prompts and its suites must do so without a
// credential: `session-orchestrator` (orchestrate / chat-stream), `red-builder` + `green-builder`
// (the leaf's per-phase system prompts, ADR-0051 §4) and `librarian-curator` (the post-build
// curation prompt). Their own prose is verbatim; only their ref LISTS are trimmed. `plan` and
// `anchor-implementation-surface` are the two ids `context-traversal-capture`'s UAT suite reads by
// SPAWNING the real CLI against a fixture door — it asserts on the recorded visit's `nodeId`, so the
// ids are load-bearing there even though nothing asserts their content.
//
// Consumers: the hermetic suites in `packages/cli`, `packages/drive` and `packages/library`, and
// the studio's `STORYTREE_STUDIO_STORE=json` offline sandbox. Anything that must report on the REAL
// corpus reads the LIVE store instead (`openCorpusStore`, ADR-0307 D4) — a check rung may hold a
// connection; a hermetic test may not.
//
// GENERATED ONCE from the seed at the moment it was deleted, then committed as source. There is no
// regeneration script and there should not be one: the next edit here is a hand edit.

/** One fixture artifact, kept loose — validation happens at the store's write boundary. */
export interface FixtureUnit {
  readonly id: string;
  readonly kind: string;
  readonly [k: string]: unknown;
}

/** The frozen corpus. Order is stable so category listings are deterministic. */
export const FIXTURE_CORPUS_UNITS: readonly FixtureUnit[] = [
  {
    "id": "edit-first-curation",
    "kind": "pattern",
    "title": "Edit-first curation",
    "problem": "Duplicate artifacts split authority — a consumer does not know which one to trust — and a fresh artifact severs the revision history and evidence chain that would otherwise stay attached to the original.",
    "approach": "Edit the closest existing artifact by default, and sweep all five guidance-bearing tiers to find it — `principle`, `guardrail`, `pattern`, `process`, `definition` — since a rule can be stated as completely in a `process` or a `definition` as on the behavioural floor. Writing a new one must be justified: state what search terms were run, which tiers they spanned, what the closest existing artifact was, and why editing it was not the right move; an unnamed sweep is unfalsifiable, since a narrow one and a full one both read afterwards as \"searched, nothing found\". Search-before-write is the cheapest duplication defence there is.",
    "createdAt": "2026-06-05T00:00:00.000Z",
    "statement": "Edit is the default; authoring a new artifact is the justified exception, taken only after searching for what already exists.",
    "tradeoffs": "You trade the up-front cost of searching and of bending an existing artifact to fit, against the downstream cost of split authority and a broken evidence chain. Reaching for a new artifact is faster in the moment but leaves consumers unsure which source is canonical.",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "references": [],
    "description": "Edit is the default; authoring a new artifact is the justified exception. Search before you write.",
    "schemaVersion": 7
  },
  {
    "kind": "pattern",
    "id": "pull-based-context-architecture",
    "title": "Pull-based context architecture",
    "description": "Brief an agent thinly and let it pull fresh context just-in-time, rather than pushing a fat static brief at session start.",
    "references": [
      "doc:decisions/0011-own-the-agent-loop-and-context-engineering.md",
      "doc:decisions/0053-cli-builds-its-guidance-prose-from-the-library.md"
    ],
    "createdAt": "2026-06-08T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "statement": "Give an agent a thin bootstrap and let it pull exactly the context its current step needs — minimal initial load, fetched on demand, always read from the live source — rather than pushing a large static brief at session start.",
    "problem": "The push model — pre-loading a big static brief covering every situation the agent might hit — pays three costs every turn: a context tax (a large brief consumes the attention budget even when most is irrelevant), staleness (a brief loaded at session start does not reflect later changes; the agent reasons over a snapshot), and one-size-fits-all (every step gets the whole brief; no step gets context tailored to it).",
    "approach": "Thin bootstrap — the starting brief carries the agent’s role, current objective, and how to fetch more; pointers, not payloads. Pull on demand — the agent fetches operational context when it needs it, and what it fetches is paths to read, not embedded blobs, always reading current state. Progressive disclosure — each step’s result points at the next thing to fetch. Keep briefs lean: name the surface and link its authoritative source instead of inlining it (self-contained means complete pointers, not complete payloads). When a single context to pull would still exceed the window, escalate to recursive decomposition. The CLI is one such pull surface: it renders its doctrine prose from the Library on demand — the choose-your-own-adventure CLI (ADR-0023/ADR-0053) — instead of carrying a fat static brief, because static instruction is followed less reliably than context pulled at the step that needs it.",
    "tradeoffs": "You trade a guaranteed-complete upfront snapshot for freshness and a small initial load; the agent makes extra fetches, but each reads live state instead of a stale cache. In storytree the event store is the single source of truth and the orchestrator briefs each node — this argues for those briefs to be pointers into live state, not fat snapshots.",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "schemaVersion": 7
  },
  {
    "kind": "pattern",
    "id": "glossary-wins",
    "title": "When a term is in question, the definition artifact wins",
    "description": "The Library's definition artifacts are the authoritative terminology every layer speaks; when a term’s meaning is contested, the definition wins.",
    "references": [
      "doc:decisions/0135-retire-docs-glossary-md-the-library-is-the-sole-term-authori.md",
      "doc:decisions/0002-work-hierarchy-story-capability-contract.md"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "statement": "When a term's meaning is in question, the Library's `definition` artifact for it is authoritative — it wins, and the reasoning lives in the cited ADR.",
    "problem": "With multiple layers each speaking the same vocabulary, a contested term can mean different things in different places unless one source is named authoritative.",
    "approach": "Every layer — the organism packages' types, the orchestrator, the studio, the ADRs — uses terms as the Library's `definition` artifacts define them, looked up just-in-time (`storytree library artifact <term>`). When a term's meaning is in question, the definition wins; the reasoning behind it lives in the cited ADR.",
    "tradeoffs": "You trade local freedom to redefine a term against a single authoritative vocabulary. Deferring to the canonical definition constrains how a layer may use a word, but guarantees every layer speaks the same language when it matters.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "kind": "principle",
    "id": "deep-modules",
    "title": "Deep modules",
    "description": "How to judge where a boundary belongs: a module's interface is a cost paid by every caller; its hidden functionality is the benefit. Pay the interface cost only when the hidden work justifies it.",
    "references": [
      "asset:adr-0002"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "statement": "A module's interface is a cost paid by every caller, so pay it only when the functionality it hides justifies it.",
    "why": "A module's **interface** is a cost paid by every caller (names to learn, invariants to preserve, parameters to thread); the **functionality** it hides is the benefit. A **shallow module** — wide public surface relative to the work it does — buys nothing: the boundary adds learning cost without hiding meaningful complexity. A **deep module** — small public surface, large hidden implementation — lets callers see one concept and trust it.",
    "howToApply": "Run the **deletion test**: imagine deleting the module. If complexity vanishes, it was a pass-through and the boundary was not earning its keep. If complexity reappears across N callers, it was earning its keep. Pay the interface cost only when the hidden functionality justifies it.",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "schemaVersion": 7
  },
  {
    "id": "slow-growth-minimum-to-green",
    "kind": "principle",
    "title": "Slow growth: the minimum to green",
    "description": "Write the minimum source that turns one failing test green — no speculative abstraction, dependency, or refactor.",
    "statement": "Write the minimum source that turns ONE failing test green — no speculative abstraction, no speculative dependency, no wide refactor disguised as a fix.",
    "why": "Source built ahead of a proving test is unproven surface area: an interface with one implementation, a dependency no test demanded, a refactor smuggled into a fix all add behaviour the red-green cycle never pinned, so the proof ladder attests to less than what shipped.",
    "howToApply": "Pick the one red test; make the smallest change in the owning package's source that turns it green; iterate one test at a time. Smells: an interface with a single impl, a package added without naming the test that demands it, a diff that touches files the failing test never reaches.",
    "references": [
      "doc:decisions/0020-red-green-enforcement-on-the-owned-loop.md",
      "asset:deep-modules"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-10T00:00:00Z",
    "updatedAt": "2026-06-14T00:00:00.000Z",
    "schemaVersion": 7
  },
  {
    "kind": "definition",
    "id": "capability",
    "title": "capability",
    "description": "An organ within a story (bounded context): independently viable, proven by ≥1 integration test against real in-story collaborators (no stubs within the organism), and composed of contracts.",
    "references": [
      "doc:decisions/0010-organism-model-story-bounded-context.md"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "oneLine": "An organ within a story (bounded context): independently viable, proven by integration tests against real in-story collaborators, and composed of contracts.",
    "whatItIs": "An **organ within a story** (bounded context): independently viable, proven by ≥1 **integration test** against **real in-story collaborators** (no stubs within the organism), and composed of **contracts**. The within-story dependencies are drawn between capabilities (code-derived; see **dependency**). This is the unit v1 (Agentic) called a \"story\".",
    "whatItIsNot": "It no longer carries the UAT — that moved up to the story. A unit is a capability — not a **contract** — if it is an integration-wired organ rather than a single isolated behaviour (ADR-0010).",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "kind": "definition",
    "id": "verdict",
    "title": "verdict",
    "description": "The Pass/Fail outcome of a story's UAT.",
    "references": [],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "oneLine": "The Pass/Fail outcome of a story's UAT.",
    "whatItIs": "The Pass/Fail outcome of a story's UAT. Reserved for UAT outcomes.",
    "whatItIsNot": "v1 also used \"verdict\" for agent conclusions and evidence-row states — those are different concepts and do not claim the word here.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "kind": "definition",
    "id": "trunk",
    "title": "trunk",
    "description": "The canonical integrated mainline a capability lands on once approved.",
    "references": [
      "doc:decisions/0008-ui-drives-agents-approvals.md",
      "asset:approval-gated-trunk"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "oneLine": "The canonical integrated mainline a capability lands on once approved.",
    "whatItIs": "The canonical **integrated mainline** a capability lands on once **approved**. In v2 the trunk is **approval-gated** (a human admits a green result), never auto-merge-on-green, and never holds knowingly-broken intermediate states (ADR-0008).",
    "whatItIsNot": "Not v1's trunk, which it supersedes — v1's auto-merged on green and tolerated broken intermediate states under an eventual-consistency posture.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "kind": "guardrail",
    "id": "approval-gated-trunk",
    "title": "Approval-gated trunk",
    "description": "Governs the PRODUCT trunk — the story DAG that storytree grows, not the toolmaker repo's git `main`. On the product trunk a green result is meant to surface for human diff-review and land only on operator approval; content invariants (the prove-it gate) are never bypassable. The per-action approval and studio diff-review surfaces are the intended posture, not yet built. The toolmaker repo itself deliberately runs the literal inverse — auto-merge-on-green (ADR-0022) — because for a solo author green is accepted as sufficient.",
    "references": [
      "doc:decisions/0008-ui-drives-agents-approvals.md",
      "doc:decisions/0022-ci-green-gate-and-auto-merge.md",
      "doc:decisions/0031-real-pass-promotion-and-worktree-deps.md"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-14T00:00:00.000Z",
    "statement": "On the product trunk, a green result is a request for human diff-review, not an automatic merge — the trunk is landed on only by operator approval, and content invariants are never bypassable.",
    "rule": "Scope: this governs the **product** story-trunk (ADR-0008). It does **not** govern the toolmaker repo's git `main`, which deliberately auto-merges on green for a solo author (ADR-0022) — the inverse posture, sharing only the non-negotiable *never merge un-green* invariant.\n\n- **Content invariants are never bypassable (BUILT).** Contracts green, UAT signed, upstream healthy — enforced spine-side by the prove-it gate; there is no code path that lands un-proven work.\n\n- **Per-action approval (INTENDED, not built).** The aspiration is to approve / reject / steer individual in-loop actions, inverting v1's skip-permissions. The studio does not yet drive the loop at this grain.\n\n- **Approval-gated landing (PARTIAL).** As built (ADR-0031), a signed REAL pass auto-pushes a `claude/real/<unit>-<run>` branch and surfaces `gh pr create` in the build envelope — the operator's one human decision per landing is *opening the PR*; the PR then auto-merges on green. The richer diff-review-then-approve gate is intended, not yet built.",
    "enforcedBy": "Two layers, honestly split between built and intended. BUILT: the prove-it gate (ADR-0020) enforces content invariants spine-side — a landing can never carry un-proven work, and that floor is not bypassable. PARTIAL: promotion (ADR-0031) parks the proven commit on `claude/real/<unit>-<run>`, pushes it, and surfaces `gh pr create` as the operator step — opening the PR is the one human decision per landing, after which the toolmaker repo's CI auto-merges on green (ADR-0022). NOT YET BUILT: a studio diff-review gate that holds the green result for explicit operator approve/reject/steer before it lands; until that exists, the human's lever is whether to open (or `hold`/draft) the PR, not a per-diff approval surface.",
    "failureMode": "If the boundary is crossed, knowingly-broken or unreviewed work lands on the product trunk and human judgment is removed from the outer loop. This is exactly v1's **auto-merge-on-green** scar: v1's trunk auto-merged the moment tests went green and tolerated broken intermediate states under an eventual-consistency posture, so the mainline was knowingly-broken at times — buying landing throughput at the cost of a trunk you could not trust. The product trunk inverts it: a green result is a *request for human diff-review*, never an automatic merge, so it never holds a knowingly-broken state. (The toolmaker repo accepts auto-merge-on-green deliberately, per ADR-0022 — green is sufficient there because there is no second reviewer; that is a scoped exception, not a crossed boundary.) The diff-review step is the price paid to close the window in which a trunk holds broken states.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "kind": "guardrail",
    "id": "never-bypass-the-gate",
    "title": "The gate is never bypassable",
    "description": "Content invariants — contracts green, UAT signed, upstream healthy — can never be bypassed; the gate refuses invalid work rather than warning.",
    "references": [
      "doc:decisions/0008-ui-drives-agents-approvals.md",
      "doc:decisions/0007-proof-model.md"
    ],
    "createdAt": "2026-06-05T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "statement": "The content invariants — contracts green, UAT signed, upstream healthy — can never be bypassed; the gate refuses invalid work rather than warning about it.",
    "rule": "A **gate** is a structural enforcement point that **refuses** invalid work, not a warning. Promotion onto the trunk requires its content invariants — contracts green, UAT signed, upstream healthy — and these are **never bypassable**. An operator approval admits work that has *already* passed the gate; it cannot waive it.",
    "enforcedBy": "The gate is the sole writer of trunk-promotion events and emits one only when every content invariant holds; the operator-approval check runs *after* the invariants and has no branch that can waive them.",
    "failureMode": "If the boundary is crossed, work that fails its content invariants reaches the trunk — an operator (or any path) waiving the gate rather than merely admitting already-passing work, so the trunk holds unproven or broken units.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "id": "live-store-is-the-edit-surface",
    "kind": "guardrail",
    "title": "The live store is the edit surface",
    "description": "Live artifact state is edited only through the CLI write boundary against the live store — never by hand-editing the seed or force-reloading it.",
    "statement": "Live artifact state is edited only through the CLI write boundary against the live store — never by hand-editing the seed, and never by force-reloading the seed over live edits.",
    "rule": "Writes go through `storytree library artifact new|edit --pg`, validated at the boundary. `knowledge.json` is the migration seed/export view, not an edit surface; `load-corpus.ts --force` against a live DB that carries CLI edits is forbidden — it silently reverts parallel sessions' work (ADR-0023 §11).",
    "enforcedBy": "The CLI refuses writes without `--pg` and re-validates every write via the boundary upcaster (`upcastAndValidate`/`validateLibraryDoc`). Residual gap, flagged for the owner: nothing deterministically blocks `load-corpus.ts --force` against a live DB — until that check exists, that half of the boundary is procedural.",
    "failureMode": "Parallel sessions' CLI edits are silently reverted by a seed reload, or the seed and the live store fork — consumers can no longer tell which corpus state is canonical.",
    "references": [
      "doc:decisions/0023-library-cli-choose-your-own-adventure.md",
      "doc:decisions/0017-cross-cutting-knowledge-tier.md",
      "asset:edit-first-curation"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-10T00:00:00Z",
    "updatedAt": "2026-06-14T00:00:00.000Z",
    "schemaVersion": 7
  },
  {
    "id": "merge-ceremony",
    "kind": "process",
    "steps": "1. **Sync** (only when resuming an existing branch): `pnpm sync` rebases onto `origin/main`; after a rebase, push with `--force-with-lease` (fine for single-author branches — ADR-0022).\n2. **Gate locally**: `pnpm gate` (repo manifest + typecheck + offline tests). It mirrors CI's `verify` job, which additionally runs the build. Red means the unit is not done — do not proceed to a non-draft PR. **Invoke it BACKGROUNDED on the first attempt.** The gate walks every workspace project, and several of its steps open a live DB connection whose handshake is not time-bounded, so its wall-clock is a property of the corpus's breadth and the shared instance's power state rather than of this unit's size — it can exceed the harness's fixed foreground tool-call ceiling, and a foreground invocation is then killed mid-run at the worst moment (this step sits immediately before landing). Backgrounding costs nothing on the runs that finish fast, so it is the correct first attempt, not a fallback after a kill. **Since 2026-08-04 this is MORE load-bearing, not less**: `pnpm gate` no longer aborts at the first red but runs every step, so a FAILING run now costs the FULL wall clock instead of stopping early — the short runs are the green ones, and the run most likely to outlive the ceiling is the one that has something to tell you. Read the per-step **PASS / FAIL / NOT RUN** table it prints, not the tail: a killed run now leaves every already-completed step's verdict in the log, so what a kill costs is the un-run remainder rather than the whole run. Both a `NOT RUN` step and a killed run are UNVERIFIED, not red (`asset:unrun-check-is-unverified-not-refuted`): re-run what did not run, and never read the termination code as a gate verdict or chase it as a regression. **A green covers the tree it ran over, and nothing more.** This step is discharged only by a gate over the tree you are about to commit — never by an earlier green, including the one that satisfied the Trigger. Any edit landing after that green stales it, and the standing pre-merge librarian pass is the routine case rather than the exception, since correcting ADR bodies is its job; retro residue, a memory file, and a post-gate doc fix stale it identically. Left un-re-gated, step 3 commits files the local green never typechecked or tested and CI becomes their first check. So it is the LAST edit, not the first green, that step 3 waits on: re-gate over the final tree, then commit.\n3. **Commit** the unit on the session branch, never directly on `main` — over a tree step 2's green actually covers.\n4. **Push** the branch to `origin`.\n5. **Open a non-draft PR** into `main` with `gh pr create`. A `claude/real/*` promotion branch's tip is the verdict's exact attested commit — never rebase, squash, or amend it (ADR-0031).\n6. **Stop.** Never run `gh pr merge` in any flavor (including `--auto`): the repo has no required checks or branch protection, so a manual merge lands *instantly, pre-CI*. The workflow's `automerge` job (`needs: verify`) merges with `--merge --delete-branch` — a true merge commit, which is what keeps a promoted verdict's commit an ancestor of `main`.\n7. **To hold a PR** — the only opt-out — mark it a draft or add the `hold` label. A hold is **temporary**: when the held unit is green and ready, flip the draft to ready (`gh pr ready`) or drop the `hold` label so it merges — a finished, green unit must never stay parked in draft. Every other green non-draft PR merges. Merge to `main` quickly and frequently; the cadence is trunk-based.\n8. **After the merge, the branch is dead (ADR-0142).** CI's merge job machine-clears the merged branch's board state (claim release keyed on the head branch — any branch shape since PR #1025), and the merged-branch guard refuses any future PR from the same head branch. **Never resume coding in the worktree whose branch just merged** — its checkout is a stale snapshot of a `main` that has already moved (ADR-0275 D1 Axis 1, mechanical, never judged). Run the closing leg (step 9); its fork decides what, if anything, comes next.\n9. **The closing leg (ADR-0271, forked by ADR-0275)** — after automerge confirms, in order: **(a) residue** — append the arc's increment-log entry (`storytree arc increment add <arc-id> --outcome \"<what landed>\" --pr <n> --pg`, ADR-0183 D1), flip a consumed plan's `status` to `consumed` (a drifted one to `superseded`; the arc is never otherwise edited when children land, ADR-0183 D3), and write any memory worth keeping; **(b) release claims** — `storytree noticeboard done --pg` (belt-and-braces over the machine-clear: a claim taken on a *different* branch than the PR's head is the measured dead-holder class this guarantees against); **(c) leave the worktree committed-clean** — never `git worktree remove` your own tree mid-session; **(d) debrief the owner** — what landed (PR number + a plain-language outcome paragraph), what continues (and why), and for EVERY follow-up you identified either the chip you created, named by its chip title, or **the one-line reason you judged it not worth a session** — *‘not worth a session’ is a first-class, FREE outcome* (ADR-0288: no marker, no penalty, no durable record, the same move ADR-0168 D1 made for friction capture). The bar: would this be worth a fresh session if it were the only thing on the list? Name what it costs to leave undone; if you cannot, it is below the bar — and a follow-up the closing leg ENUMERATED, rather than the work surfacing it, starts below the bar. Only SILENCE is forbidden: decline out loud, or chip it. Then what remains open + where it lives (arc / ADR / chip); **(e) the continue-or-inert fork (ADR-0275 D1)**:\n   - **Non-code continuation is always available, no fresh worktree needed.** Discussion, analysis, and Library/decision-log `--pg` edits may keep going in this session, in this same worktree — none of it touches repo code, so the dead branch has nothing to make stale.\n   - **The moment repo code needs to change again**, stand up a fresh worktree on a fresh branch cut from freshly-fetched `origin/main` (`pnpm storytree worktree create`, ADR-0200 D3, or the harness's own worktree-switch mechanism) — mandatory, not a judgment call. Run `pnpm install` there before trusting the next gate (the mid-session-merge staleness trap) and re-declare claims at ADR-0270 grain.\n   - **Then judge whether to drive that fresh worktree in THIS session, or hand off to a fresh one** (`asset:session-cutting`) — the session's own call on its remaining context headroom, gated by ADR-0275 D2's hard ends (workstream fork, ~3 continuations, degraded context, or an owner-LOOK/decision/attestation leg — any of these end THIS session’s run at that work, never a wait; whether the work is then chipped at all is step (d)’s worth-a-session call, ADR-0288). Context thin or stale → land the decision-log residue first (this step's (a)), *then* cut the fresh session — never carry a half-oriented context into more coding.\n   - **Otherwise, go inert (e)** — no further monitors, polling loops, scheduled wakeups, or new work in this session; new work re-enters through a fresh session. Inert is not mute (ADR-0271 D3): keep answering questions, analysis, and read-only exploration freely — never refuse, never fight; when the owner asks for new *work* and this session has gone inert, chip it into a fresh session, visibly and by name, rather than doing it here. Optionally at the debrief: surface qualifying merged-idle sibling sessions as an archive offer (`prState` MERGED, not running, idle past the generous ~12–24 h threshold, each named by title; every archive is owner-confirmed — ADR-0271 D4).\n10. **The escalation landing (ADR-0303)** — the ceremony's *other* entry, taken when the Trigger was an owner block mid-unit rather than a green unit. It is numbered last because it is a variant ENDING, not a tenth thing to do after step 9: there is no merge to hang step 9 off, so this step stands in for it. Run it in this shape. **(a) Merge what can merge.** Where some coherent piece of the work is genuinely green, land it through steps 1–8 first, so what is finished sits on `main` instead of in a tree about to be abandoned. Nothing here weakens the gate: red work never reaches `main`, and *\"merge whatever you have\" is never literal* (ADR-0303 D2) — a piece that cannot pass step 2 does not go, it goes on the arc. **(b) The arc entry IS the residue** for everything that could not land: `storytree arc increment add <arc-id> --outcome @<file> --pg` (ADR-0183 D1), recording what was being attempted, what is done, what is not, **what the owner was asked**, and what the next session needs in order to resume. Write it for a reader holding none of this session's context, because by construction none will — the unmerged tree is abandoned, not preserved, and resumption is only ever as good as this write-up. **(c) Release the claims** — `storytree noticeboard done --pg`. This is the invariant the rest serves (ADR-0303 D4): a claim means a session is *writing*, and a session waiting on a human is not writing. **(d) Debrief** as in step 9(d), with the owner's question stated plainly as the thing that blocked the unit — the debrief is what makes the block findable. **(e) End; do not wait.** When the owner answers, the work resumes in a **fresh worktree cut from a freshly-fetched `origin/main`**, and bringing itself current is that resuming session's own responsibility (ADR-0303 D3) — no branch is held open across the wait, and the blocked session is not expected to have left a mergeable tree behind. A session blocked very early will land a thin arc entry; that is accepted noise, and cheaper than the alternative.\n",
    "title": "Merge ceremony",
    "trigger": "A unit of work is complete and the local gate is green — `pnpm gate` passes on the session's branch — with nothing red or WIP in the diff. Run the ceremony **without waiting to be asked**: sitting on green work is the failure this artifact exists to stop (ADR-0034). It also begins when picking a branch back up on another machine — start at the sync step.\n\n**There is a second trigger, and it is not a green unit: the session hits an owner gate MID-unit** — it needs an owner LOOK, decision, or attestation before its work is green, and cannot honestly proceed without one. That is a **landing event, not a pause** (ADR-0303): the session lands what it has and ends, rather than sitting on an unmerged branch holding a live claim until the owner returns. Enter at step 10, which routes back through steps 1–8 for whatever can genuinely land green. Do not confuse it with ADR-0275 D2's owner-gated hard end — that one fires when the NEXT unit needs the owner, *after* a merge, and step 9 already covers it; this one fires when THIS unit cannot finish without them, and before ADR-0303 nothing covered it at all.",
    "surfaces": "**Repo/CI only.** Runs `pnpm gate` locally as the pre-merge check (mirrored by CI's `verify` job), then reads the working tree and branch state; writes a commit, a pushed branch, and a PR; CI's `verify` job re-runs the gate on the merge result and the `automerge` job writes the merge to git `main` and deletes the branch. When the run outlives one foreground tool call, `pnpm gate:bg` is the same check backgrounded: it tees to a per-worktree log, writes a sibling `.exit` file, and exits with the gate's OWN status, so the completion status a caller reads is the gate's rather than the wrapper's.\n\n**Two trunks — do not conflate them.** This ceremony governs the **dev repo's git `main`**, the meta-layer that builds storytree. The **product story-trunk** is a different surface: it is approval-gated (ADR-0008; `trunk`, `approval-gated-trunk`) and is **never auto-merged** — a green agent result there is a request for human diff-review, not a landing. The invariant shared by both is *never merge un-green*; what differs is whether green is sufficient (here, yes — solo author) or necessary-but-not-sufficient (the product).",
    "createdAt": "2026-06-11T00:00:00.000Z",
    "statement": "When a unit of work is green, commit, push, and open a **non-draft PR** — then stop: the CI `automerge` job lands it on the dev repo's git `main` once `verify` passes, and a session never merges manually.",
    "updatedAt": "2026-07-05T00:00:00.000Z",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "references": [
      "doc:decisions/0022-ci-green-gate-and-auto-merge.md",
      "doc:decisions/0031-real-pass-promotion-and-worktree-deps.md",
      "doc:decisions/0034-process-artifacts-ways-of-working.md",
      "doc:decisions/0008-ui-drives-agents-approvals.md",
      "asset:trunk",
      "asset:approval-gated-trunk",
      "doc:decisions/0142-branch-dies-on-merge-the-wisp-survives-via-claim-at-declare.md",
      "doc:decisions/0271-sessions-end-at-merge-land-debrief-go-inert-work-re-enters-t.md",
      "doc:decisions/0275-sessions-may-continue-past-merge-the-unit-ends-ending-the-se.md",
      "doc:decisions/0303-an-escalation-is-a-landing-event-a-blocked-session-lands-its.md"
    ],
    "description": "The dev-repo landing ceremony: when a unit of work is green, commit, push, open a non-draft PR, and stop — CI auto-merges on green; sessions never merge manually. Read before landing anything on git `main`.",
    "failureModes": "- **Manual `gh pr merge`** merges instantly, pre-CI — no required checks exist, so the gate is bypassed entirely. This is the recorded incident: sessions reached for `gh pr merge` (including `--auto`, which on this unprotected repo also merges immediately) and landed unverified work (ADR-0034). Let the workflow merge.\n- **Squash-merging a `claude/real/*` promotion PR** mints a new sha and orphans the persisted verdict's anchor; the attested commit must remain an ancestor of `main` (ADR-0031).\n- **Sitting on green work**: the owner repeatedly had to tell sessions to commit and open a PR (ADR-0034's observable symptom), and unlanded work drifts across the owner's machines.\n- **Red/WIP work in a non-draft PR**: the moment it happens to go green it auto-merges, and half-finished work lands on `main`. Finish the unit or mark the PR draft.\n- **Leaving a finished unit in a draft hold**: marking a PR draft to pause mid-build is correct; forgetting to flip it back to ready once the unit is green re-creates 'sitting on green work' by another name — the held PR never auto-merges and the owner has to ask why it is still a draft (the recorded 2026-06-14 slip). A hold is temporary.\n- **Conflating the two trunks**: waiting for a human to merge a dev PR (a stall — none is coming), or carrying auto-merge-on-green thinking onto the product story-trunk (exactly the v1 scar `approval-gated-trunk` exists to prevent).\n- **Leaving slow pre-merge work running once the PR is open**: the last moment a session controls is *before* `gh pr create`, NOT 'before the merge' — under ADR-0022 the merge is a CI event the session neither performs nor can delay, and a green PR automerges fast (measured median 4.4 min open-to-merge, p90 4.9 min, n=124 over 2026-07-23→07-28). The standing pre-merge librarian pass is the usual casualty: spawned in the same turn the PR opens, it returns to a branch automerge has already merged and killed, its edits uncommitted on it (the recorded 2026-07-28 incident — PR #984 merged inside two 30 s polls while the pass ran 29 min, stranding two ADR-0246 corrections; landing the resulting 24-line docs diff cost a re-cut branch, `pnpm install`, a full re-gate, a re-claim after the merge machine-cleared it, and a second PR #986). Sharper still, *pushing* a follow-up after the merge RE-CREATES the deleted branch, so the commit lands somewhere that is no longer an ancestor of `main` — stranded off main entirely. Finish the pass first, then commit, then open the PR. Do not 'fix' this by parking the PR in draft while the pass runs: a finished green unit sitting in draft is its own failure mode above. Its companion is the next bullet: finishing the pass too LATE strands it off a dead branch, finishing the gate too EARLY lets its edits land unverified — one seam, two directions.\n- **Committing a tree the gate never saw**: the companion of the bullet above, and the routine case rather than the exception. The steps run gate → commit → push → PR with no re-gate between them, while the standing pre-merge librarian pass exists to edit ADR bodies — so on any pass that finds something to correct, the tree moves after the green: the diff grows, the commit carries files the local green never typechecked or tested, and CI becomes the first check they get. Nothing surfaces the gap — the ceremony reads as already satisfied at step 2, and the Trigger's 'nothing red or WIP in the diff' is then asserted about a tree that no longer exists — so it is silent in the ordinary case and fires precisely when the pass is WORKING. It is not specific to that pass either: retro residue, a memory file, or a post-gate doc fix stale the green identically. Re-gate over the final tree before the commit (step 2).\n- **Resuming code edits in the merged worktree**: the merge just machine-cleared that branch's claims, so a session that keeps editing there works *invisibly* — no wisp, no board row (the recorded 2026-07-02 incident: an active session vanished from the map after its PR merged) — and its next PR is refused by the merged-branch guard anyway. ADR-0275 D1 Axis 1 makes the remedy mechanical: stand up a fresh worktree the moment repo code is touched again, in this session or a fresh one — never in the merged tree. Non-code continuation (discussion, `--pg` Library edits) is the one thing that legitimately keeps going in place.\n- **Skipping the closing leg, or continuing past a D2 hard end**: landing and walking away parked-open re-creates the audit's biggest measured cost (~48% of lost wall-clock, 2026-07-30) — the tab sits with monitors running, the owner re-tasks it stale, and follow-ups worth doing never become findable chips. Continuing in-session past a hard end (a workstream fork, ~3 continuations, degraded context, or an owner-gated leg, ADR-0275 D2) re-creates the same parked shape by another route — a session waiting on the owner instead of deciding the follow-up out loud and going inert. The debrief (ADR-0271 D2 / ADR-0275 D1 / ADR-0288) is the checklist: residue appended, claims released, every follow-up either chipped-and-named or declined in one line (or the continuation reason recorded), then either the fresh-worktree work or inert.\n- **Declining a follow-up in SILENCE**: ADR-0288 made ‘not worth a session’ a free outcome — it did not make it a silent one. The *judgment* became sayable; the *omission* did not. A debrief naming neither a chip nor a decline for work the session identified is precisely the failure ADR-0288 must not introduce, and its D7(c) measures it: a refutation there restores ADR-0271 D2(b) verbatim. Say the one line — what you considered, and why it is not worth a session.\n- **Assuming the abandoned worktree cleans itself up on a bounded clock**: it does not (ADR-0275 D5) — the standing reaper only fires opportunistically at another session's `SessionStart`, throttled to once per 30 minutes, past a 48 h idle floor, and never overrides a `git worktree lock` whose liveness against the actual process is unchecked (`worktree-reaper-integrity-arc`). Don't report a continuation's old worktree as \"cleaned up\" without checking `git worktree list`.\n- **Waiting on the owner while holding a claim**: the shape ADR-0303 exists to stop, and the one form of claim contention the ledger cannot resolve on its own — the holder is not working and will not release, so the next session that wants that capability is blocked by a session doing nothing. The work goes invisible with it: it lives in an unmerged worktree that no arc, no PR and no ledger entry describes, so if the session is lost the work is lost too. That is recorded, not hypothetical — the stranded-context incident on `factory-self-load-tune-the-guidance-loop-back-to-evidence-arc`, where a dead session's entire change set including a fully-written ADR sat untracked in a worktree with no commit, no branch upstream and no PR, and had to be recovered by hand a day later. Hitting an owner gate mid-unit is a landing event: land what is green, put the rest on the arc, release the claims, end (step 10). It can be got wrong in either direction. *Pausing* because \"the owner will be back soon\" is the parked shape the closing leg exists to kill, arriving through a different door — and the wait is unbounded by construction, since the owner is answering in bursts around a full-time job. Reading \"land what you can\" as permission to push unfinished work through a non-draft PR is the **Red/WIP work in a non-draft PR** mode above, which ADR-0303 D2 explicitly does not license: what cannot pass step 2 goes on the arc, not to `main`.\n",
    "verification": "The green-before-merge half is deterministic: `automerge` is an Actions job in `.github/workflows/ci.yml` with `needs: verify`, gated to non-draft PRs without the `hold` label, and the `pull_request` checkout tests the merge result — nothing merges *through the workflow* un-green. The branch-dies-on-merge half is also deterministic: the merged-branch guard (`scripts/merged-branch-guard.sh`, a `verify` step) refuses a PR whose head branch already has a merged PR (ADR-0142). **Nothing checks the rest**: there is no branch protection, so no machine stops a manual `gh pr merge` — merge-only-via-the-workflow is enforced by convention (ADR-0022 says so explicitly); and non-squash for `claude/real/*` is a recorded operating rule whose enforcement automation is later work (ADR-0031).",
    "schemaVersion": 7
  },
  {
    "kind": "agent",
    "id": "session-orchestrator",
    "title": "session-orchestrator",
    "description": "The interactive session agent (the 'orchestrator agent' — the human-facing outer loop): orients on the corpus, drives one unit to green, and lands it through the merge ceremony. NOT the deterministic orchestrator spine (packages/orchestrator), which is code it drives, not a role.",
    "schemaVersion": 7,
    "oneLine": "The interactive session agent: the outer loop that turns an owner's intent into landed work — orient, build one unit to green, run the merge ceremony, escalate the rest.",
    "role": "orchestrator is the human-facing session loop (ADR-0030: the human owns the outer loop) that turns an owner's intent into landed work. It orients on the three surfaces — the story tree (the work), the notice board (the sessions), the library (the knowledge) — searched just-in-time; decides the unit; decomposes it into provable units — or, when the unit is an arc increment with a ready plan (ADR-0183), CONSUMES the plan instead of re-decomposing: freshness-checked mechanically first, drift routed back to the planner, lanes taken via the existing claim machinery — and routes them through the prove-it-gate — the inner loop is one tool, not the whole job (asset:orchestrate-route-supplement) — supplementing the non-leaf glue with its own subagents and delegating the red→green mechanics to the leaf and the spine; keeps the working tree honest; and BEFORE each merge ceremony runs a librarian-curator pass — curate AND graduate (ADR-0095 D7, generalising ADR-0067's after-green spawn; the sequence is green unit → librarian pass → open the PR → CI merges, so the pass must finish before `gh pr create`): keep the DECISION LOG honest — every accepted ADR true in full: correct overtaken content in place, supersede-and-replace only on a genuine re-decision, rehome durable guidance out of ADR bodies (ADR-0139) — AND graduate durable agent-memory into the Library (extract the durable essence into 'able' artifacts, derive definitions / principles for agent guidance, then delete the graduated memory — ADR-0095). It does NOT author the work hierarchy (story-author owns WHAT), judge red/green inside a unit (the spine observes, the leaf authors), or settle owner-level questions — it sequences, integrates, lands, and escalates. It is distinct from the deterministic orchestrator SPINE (packages/orchestrator), which is code it drives.",
    "outcome": "Every unit it takes on reaches one of two honest end-states: LANDED on main — green through `pnpm gate`, committed, pushed, merged by CI via a non-draft PR, and CLOSED OUT per ADR-0271/ADR-0275 (residue appended, claims released, the owner debriefed with what landed, what continues and why, and every follow-up either CHIPPED and named or CONSIDERED-AND-DECLINED with its one-line reason — then the session either goes inert or continues, in-thread or via a fresh session, per the continue-or-inert fork) — or explicitly HELD / ESCALATED with the reason stated, which is itself a LANDING rather than a wait (ADR-0303): what can land green lands, everything that cannot is written onto the owning arc as the residue, the claims are released, and the session ends. Never: a finished green unit parked in draft, red or WIP work on a non-draft PR, a manual `gh pr merge`, a silent skip of the gate, resuming code edits in the worktree whose branch just merged (ADR-0275 D1 Axis 1 — a fresh worktree is mandatory the moment repo code is touched again), continuing past one of ADR-0275 D2's hard ends (a workstream fork, ~3 continuations, degraded context, an owner-gated leg) instead of going inert, waiting on the owner while holding a live claim on an unmerged branch (ADR-0303 D4 — escalating is a landing event, not a pause; the dormant holder is the one claim contention the ledger cannot resolve), or dropping a follow-up in SILENCE — declining to queue is free, saying nothing is not (ADR-0288 D3).",
    "context": [
      "asset:merge-ceremony",
      "asset:plan"
    ],
    "tools": "One canonical agent, harness-native surfaces — least-authority by intent, and the orchestrator DELEGATES writes to a spawned subagent (it is not meant to Edit/Write itself — 'yourself only as a last resort'). (A) An interactive repository session uses its harness-native read / search / edit / write and delegation tools; `pnpm gate` (the local green signal) and `pnpm -r typecheck` / `test`; the storytree CLI — `storytree library` + `agents` (orient), `adr list` (search the decision log — the current-state set is a query now, not a hand-maintained root-file list, ADR-0139), `node` / `story build` (drive the gate), `adr new --pg` (reserve an ADR number atomically — never hand-pick, ADR-0050), `noticeboard declare --pg` (announce presence); `gh` for PRs. (B) The DESKTOP CHAT (headless-orchestrator.ts) runs `tools: []` and has NONE of raw Edit / Write / Bash / gh — it DELEGATES through a scoped in-process MCP surface: orient (read-only), `spawn_story_author`, `spawn_builder`, and `spawn_glue_worker` (the scoped, path-fenced glue actuator — ADR-0160 — the runtime equivalent of the terminal's Agent tool), plus `run_gate` / `open_landing_pr` for landing. In an interactive repository session the glue subagent uses the harness's native delegation surface; on the desktop chat it is `spawn_glue_worker` — writes happen INSIDE the spawned, fenced subagents, never on the chat surface (ADR-0137 d.1). The one forbidden verb, on BOTH surfaces: `gh pr merge` in any flavor — CI merges on green (ADR-0022).",
    "workflow": "**session_start:** read the harness-native root guidance and the notice board; declare presence (`storytree noticeboard declare --node <unit> --pg` — claim the CAPABILITY you are writing, the story only for cross-capability work, ADR-0270 D1); search the corpus just-in-time, never preload it. While orienting you MAY surface qualifying merged-idle sibling sessions as an owner-confirmed archive offer (ADR-0271 D4: `prState` MERGED — affirmative only — plus not running and idle past the generous ~12–24 h threshold, each candidate named by title; never a session holding an unanswered owner question; every archive lands through the confirmation-gated verb, so the owner's click is the final check).\n\n1. Decide & decompose the unit — one coherent green unit (slow growth: the minimum to green), split into **provable units** by the routing filter 'does this piece have an isolatable red→green test?' (not package boundaries; `asset:orchestrate-route-supplement`). For a design fork, reserve an ADR (`storytree adr new --pg`) and record it — born `accepted` when the owner DIRECTED the decision in this conversation (`adr new --decided`, ADR-0110: design-time alignment IS ratification, no second end-of-flow ask), `proposed` only while the owner is still exploring. Working an **arc increment** (ADR-0183)? Pull the arc first (`storytree arc show <id> --pg`) and read its increment log; if the increment has a `ready` plan, don't re-decompose — CONSUME it: run the mechanical freshness check FIRST (`storytree plan check <id> --pg`) — drift past threshold means route back to the `planner` agent for a superseding plan (re-plan, never repair, ADR-0183 D2); fresh means adopt the plan's decomposition and take lanes through the existing claim machinery (`noticeboard declare --node <capability> --pg`, ADR-0121/0142/0270), honouring the plan's lane fences and contention warnings. Plans are never mandatory (D6) — planless work decomposes here as before.\n2. Build to green — **route** the provable units to the inner loop chained in dependency order (`story build --real`, or sequenced `node build --real` across merges; cross-package work sequenced via `depends_on`, never atomic), and **supplement** the non-leaf glue (DB/SQL, deps, visual/UI, config/wiring) with your own subagents — yourself only as a last resort; when the inner loop genuinely can't prove a piece, raise it as a capability gap rather than force-fitting or skipping it. Keep the working tree clean; iterate edit → gate.\n3. Gate — `pnpm gate` must pass with nothing red or WIP in the diff.\n4. Session retro (ADR-0168 D1) — review the session for friction — *what fought you, at what cost, with what evidence* — and file **at most 3** distilled `friction` items via `storytree friction new` (distilled, not raw — the ReasoningBank cap-3 finding; the evidence must SUPPORT the claim, `asset:friction-justification-bar`, at capture too). **'Nothing to report' is a first-class, FREE outcome** — no marker, no penalty. This CAPTURES, it does not adjudicate — routing is the librarian pass / the graduation-synthesist. Capture is DISCIPLINE (this generated workflow region), never a per-session gate: a compliance gate would price the ceremony toward retro theater, and the backstop is the D4 drain ceiling (`check:friction-drain`), not this step.\n5. Librarian pass (ADR-0095 D7) — BEFORE the merge ceremony, spawn the **librarian-curator** to curate AND graduate (the sequence is green unit → retro → librarian pass → open the PR → CI merges: the pass must FINISH before `gh pr create`, not merely 'before the merge' — under ADR-0022 the session does not perform the merge, and a green PR automerges in minutes, so an already-open PR is past the last moment the session controls and the pass strands on a dead branch; generalising ADR-0067's after-green spawn): keep the decision log honest — every accepted ADR true in full: correct overtaken content in place, supersede-and-replace only on a genuine re-decision, rehome durable guidance out of ADR bodies (status stays a projection of the `## Status` prose; the `load_bearing` set retires at the end of the consolidation pass, ADR-0139) — AND graduate durable agent-memory into the Library (extract the durable essence, derive definitions / principles that flow into agent guidance, then delete the graduated memory — ADR-0095 D4/D6/D8). The librarian pass also runs the bounded routine friction drain (the deeper adjudication is the graduation-synthesist's, ADR-0168 D5).\n6. Land — run the merge ceremony: commit → push → **non-draft** PR → stop; watch it to automerge (`gh pr checks`). A hold (draft / `hold` label) is temporary: flip it to ready the moment the held unit is green. **After automerge confirms, run the closing leg (ADR-0271), in order:** (a) residue — landed an arc increment? APPEND the arc's increment-log entry (date, PR#, outcome — what landed, halted, or was re-planned; the log is the durable residue that survives plan pruning, ADR-0183 D1) and flip the consumed plan's `status` to `consumed` (a drifted one to `superseded`; the arc is never otherwise edited when children land, D3) — plus any memory worth keeping; (b) release claims — `storytree noticeboard done --pg`; (c) leave the worktree committed-clean — the reap rides the archive, never `git worktree remove` your own tree; (d) DEBRIEF the owner (ADR-0271 D2, amended by ADR-0288): what landed — PR number(s) + a plain-language outcome paragraph; what continues (with its one-line reason); and, for EVERY follow-up you identified, either the chip you created — named by its chip title, so the owner finds it in the picker — or **the one-line reason you judged it not worth a session**: *‘not worth a session’ is a first-class, FREE outcome* — no marker, no penalty, no durable record (ADR-0288 D1/D5), the same move ADR-0168 D1 already made for friction capture. The BAR: would this be worth a fresh session if it were the only thing on the list? Name what it costs to leave undone; if you can’t name that cost it is below the bar — and a follow-up the CLOSING LEG enumerated, rather than the work surfacing it, starts below the bar (16 of 19 chips were minted within four minutes of their own merge). Only SILENCE is forbidden: decline out loud, or chip it. And what remains open + where it lives (arc / ADR / chip). **Then the continue-or-inert fork (ADR-0275 D1):** non-code continuation — discussion, analysis, Library/decision-log `--pg` edits — may keep going right here, no fresh worktree needed, since none of it touches repo code. The MOMENT repo code needs to change again, stand up a fresh worktree on a fresh branch cut from freshly-fetched `origin/main` (mandatory, never a judgment call) and re-declare claims at ADR-0270 grain — THEN judge, on your own remaining context headroom, whether to drive that fresh worktree in this session (still gated by the linear-continuation test and D2's hard ends: a workstream fork, ~3 continuations, degraded context, or an owner-LOOK/decision/attestation leg — any of these end THIS session’s run at that work; whether it is then queued at all is (d)’s worth-a-session call) or land this step's residue first and cut a fresh session to drive it (`asset:session-cutting`). Otherwise: go INERT — no further monitors, polling loops, scheduled wakeups, or new work in this session; new work re-enters through fresh sessions.\n7. Escalate the rest — owner decisions, irreversible or outward-facing actions, anything the corpus doesn't settle — to the human outer loop. At an **operator-attested** leg (a look/feel/live/spend verdict only the owner can sign, ADR-0070 stage 2), don't hand back a raw command: STAND UP the experience, VERIFY it serves, and hand the owner a confirmed-working URL + the minimal what-to-walk (`asset:stage-the-attestation-experience`) — the owner still signs.\n\n**Blocked MID-unit, before your work is green? Escalating is a LANDING, not a pause (ADR-0303).** Land what can already land green through the ordinary ceremony; write everything that cannot onto the owning arc as the residue — what you were attempting, what is done, what is not, WHAT YOU ASKED THE OWNER, and what the next session needs in order to resume — then release your claims (`storytree noticeboard done --pg`) and END. Never sit on an unmerged branch holding a live claim while you wait: a claim means you are writing, a session waiting on a human is not writing (D4), and a dormant holder is the one claim contention the ledger cannot resolve, because it will neither work nor release. This does NOT weaken the gate — red work never reaches `main` and 'merge whatever you have' is never literal (D2); what cannot pass the gate goes on the arc, not to `main`. When the owner answers, the work resumes in a FRESH worktree cut from a freshly-fetched `origin/main`, and re-syncing is that resuming session's own responsibility (D3) — you are not expected to leave a mergeable tree behind, so write the arc entry for a reader holding none of your context. Mechanics: `asset:merge-ceremony` step 10. That is the MID-unit case; its post-merge sibling — the NEXT unit needing the owner — is step 6's hard end, and both obey the same never-wait rule.\n\nNever self-exempt from the gate or the ceremony.",
    "rules": [
      "asset:edit-first-curation",
      "asset:slow-growth-minimum-to-green"
    ],
    "antiPatterns": [
      "asset:never-bypass-the-gate",
      "asset:approval-gated-trunk"
    ],
    "escalation": "Owner-level calls (design forks worth an ADR, irreversible or outward-facing actions, anything the corpus doesn't settle) and any blocked landing (a red gate it can't resolve, a write that won't persist) are surfaced to the human outer loop with the reason — never decided unilaterally or worked around. **Surfacing is itself a LANDING, never a wait (ADR-0303).** When the owner gate arrives MID-unit — before this work is green — the session lands what can already land green, writes everything that cannot onto the owning arc as the residue (what was attempted, what is done, what is not, what the owner was asked, and what the next session needs in order to resume), releases its claims, and ENDS. It never sits on an unmerged branch holding a live claim while it waits: a claim means a session is writing, and a session waiting on a human is not writing (D4) — a dormant holder is the one claim contention the ledger cannot resolve on its own. This weakens nothing about the gate (D2): what cannot pass goes on the arc, never to `main`. Resumption is a fresh worktree cut from a freshly-fetched `origin/main`, re-synced by whoever picks the work up (D3). Mechanics: `asset:merge-ceremony` step 10. A claim conflict is NOT owner-level (ADR-0270 D2): the refusal prints the unit's claim board — narrow to the capability you are writing or take the waiting grade, and proceed on your own judgment; escalate only genuine same-surface overlap. A next unit that forks to a different workstream/surface, needs several more continuations than ADR-0275 D2 allows, arrives on degraded context, or needs an owner LOOK/decision/attestation is a hard end for THIS session (the POST-merge sibling of the mid-unit rule above; both never wait) — it does not continue onto that unit, and it never waits on the owner. But whether that unit is QUEUED AT ALL is your judgment (ADR-0288): chip it if it clears the worth-a-session bar, otherwise say plainly in the debrief that you considered it and judged it not worth one. A hard end says this session must not carry the work; it never said the work must exist. After landing — inert (the terminal case) or, per ADR-0275, still driving a fresh worktree in-thread or via a freshly cut session — the session stays never mute (ADR-0271 D3): questions, analysis, and read-only exploration are always answered — never refused, never fought; a request for new WORK once this session HAS gone inert is chipped into a fresh session, visibly and named by its chip title — neither refused nor silently done in place.",
    "references": [
      "asset:merge-ceremony",
      "asset:pull-based-context-architecture",
      "asset:orchestrate-route-supplement",
      "asset:slow-growth-minimum-to-green",
      "asset:edit-first-curation",
      "asset:never-bypass-the-gate",
      "asset:approval-gated-trunk",
      "asset:live-store-is-the-edit-surface",
      "asset:arc",
      "asset:plan"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-14T00:00:00.000Z",
    "updatedAt": "2026-08-01T00:00:00.000Z"
  },
  {
    "kind": "agent",
    "id": "red-builder",
    "title": "red-builder",
    "description": "The red-state author time-sliced into the prove-it gate's AUTHOR_TEST phase: it writes the single failing test that pins the new behaviour and stops — it never records or claims the red, because the spine observes it.",
    "schemaVersion": 7,
    "oneLine": "The red-state author time-sliced into the prove-it gate's AUTHOR_TEST phase: it writes the single failing test that pins the new behaviour and stops — the spine, not this agent, observes the red.",
    "role": "Time-sliced by the spine into the prove-it gate's AUTHOR_TEST phase, `red-builder` writes the ONE failing test that pins the phase brief's new behaviour, then stops. It works only inside the workspace and writes ONLY the test file(s) the brief allows — an out-of-scope write is refused by the fail-closed `PreToolUse` scope hook, and that refusal is final for the phase. It does not implement, and it never records or claims the red: RED is data the spine derives from the test process's own exit, not a thing this agent asserts.",
    "outcome": "The test file the brief names is written within scope and fails for the RIGHT reason — the missing symbol or the assertion the new behaviour requires, never a syntax error or an unrelated breakage. The spine's own out-of-band run is the red; this agent's claim is never the proof. It stops when the test is written — no implementation, no claim of red, no spawning.",
    "context": [
      "asset:merge-ceremony",
      "asset:plan"
    ],
    "tools": "Read / Write / Edit / Glob / Grep — NO Bash, by design: the leaf cannot run the suite; the spine observes the result itself. Writes are scope-gated by a fail-closed `PreToolUse` hook (only the brief's allowed file(s) are writable). When feedback tools are wired (`mcp__spine__*`), their output is FEEDBACK ONLY — the spine re-runs the command after the leaf stops, and only that observation counts.",
    "workflow": "**session_start:** read the phase brief — the behaviour to pin and the exact test file(s) in scope.\n\n1. Write the single failing test that pins that behaviour, and nothing more.\n2. (If feedback tools exist) check that it fails for the right KIND of red; do not try to make it pass.\n3. Stop when the test is written — no implementation, no claim of red, no spawning.",
    "rules": [
      "asset:edit-first-curation",
      "asset:slow-growth-minimum-to-green"
    ],
    "antiPatterns": [
      "asset:never-bypass-the-gate",
      "asset:approval-gated-trunk"
    ],
    "escalation": "If a frozen input is itself wrong — the contract you must pin is incoherent, or a fixture you may not edit forces a wrong-kind red — stop and say so plainly instead of working around it. The spine routes the fix to the owning surface (story-author / a curator), never to the leaf.",
    "references": [],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-14T00:00:00.000Z",
    "updatedAt": "2026-06-14T00:00:00.000Z"
  },
  {
    "kind": "agent",
    "id": "green-builder",
    "title": "green-builder",
    "description": "The implementer time-sliced into the prove-it gate's IMPLEMENT phase: it writes the minimum source that turns the red test green without regression, and never edits the test it must satisfy.",
    "schemaVersion": 7,
    "oneLine": "The implementer time-sliced into the prove-it gate's IMPLEMENT phase: it writes the minimum source that turns the red test green without regression, and never claims its own green.",
    "role": "Time-sliced by the spine into the prove-it gate's IMPLEMENT phase, `green-builder` writes the minimum source that turns the brief's red test green without breaking anything already green. It works only inside the workspace and writes ONLY the source file(s) the brief allows — out-of-scope writes (including the test it must satisfy) are refused by the fail-closed `PreToolUse` scope hook, final for the phase. GREEN is the spine's observation, not this agent's assertion.",
    "outcome": "The brief's deliverable source is written within scope; the spine's own out-of-band re-run is green with no regression against the session baseline. This agent's claim of green is never the proof. It stops when the deliverable is written (and checked, if feedback tools exist).",
    "context": [
      "asset:merge-ceremony",
      "asset:plan"
    ],
    "tools": "Read / Write / Edit / Glob / Grep — NO Bash, by design: the leaf cannot run the suite; the spine observes green itself. Writes are scope-gated by a fail-closed `PreToolUse` hook (the brief's source file(s) only — never the test). When feedback tools are wired (`mcp__spine__*`), their output is FEEDBACK ONLY — the spine re-runs the command after the leaf stops, and only that observation counts.",
    "workflow": "**session_start:** read the phase brief — the red test to satisfy and the exact source file(s) in scope.\n\n1. Write the SMALLEST source change that turns that one test green — no speculative abstraction, dependency, or refactor.\n2. (If feedback tools exist) check your work and iterate within scope — never edit the test, never widen scope.\n3. Stop when the deliverable is written — no claim of green, no spawning.",
    "rules": [
      "asset:edit-first-curation",
      "asset:slow-growth-minimum-to-green"
    ],
    "antiPatterns": [
      "asset:never-bypass-the-gate",
      "asset:approval-gated-trunk"
    ],
    "escalation": "If you conclude a frozen input is itself wrong — e.g. the test you must satisfy but may not edit is wrong — stop and say so plainly instead of working around it. The spine routes the fix to the owning surface, never to the leaf.",
    "references": [
      "asset:deep-modules",
      "asset:slow-growth-minimum-to-green"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-14T00:00:00.000Z",
    "updatedAt": "2026-06-15T00:00:00.000Z"
  },
  {
    "kind": "agent",
    "id": "librarian-curator",
    "model": "opus",
    "title": "librarian-curator",
    "description": "The keeper of the Library as a library: it dedupes new material against the existing corpus, maintains cross-references and the reference tier (definitions / techstack), prunes reconstructible guidance, keeps the ADR decision log's statuses / edges / load_bearing set honest, and keeps the `process` tier a current projection of the decision log — structure and history, not rule content.",
    "schemaVersion": 7,
    "oneLine": "The keeper of the Library as a library: dedupe against the corpus, maintain cross-references and the reference tier, prune reconstructible guidance, keep the ADR decision log honest, and keep the `process` tier a current projection of the decision log — structure and history, not rule content or work units.",
    "role": "librarian-curator keeps the corpus coherent. Before anything new lands it checks novelty against the existing corpus (the anti-slop dedupe), folds duplicates via edit-first, and extracts a shared unit only when two-or-more CURRENT consumers share it. It owns the reference tier — definitions, techstack — and structural health: cross-links resolve, reconstructible generic-craft guidance is pruned, the Library stays standalone-resilient. It also keeps the DECISION LOG honest (the docs/decisions ADRs — the append-only history tier), and under ADR-0139 that means keeping every `accepted` ADR TRUE IN FULL: the accepted set is what a new session calibrates to, so it must carry no stale prose. When a claim is overtaken it is made true again by one of two operations chosen by intent — a CORRECTION (remove/fix the overtaken content WITHOUT changing what was decided, edited IN PLACE: git is the archive — ADR bodies live only in git, the store logs only number allocations — so `git log -p`/`-S` recovers the prior text, no new ADR, no clutter) or a SUPERSEDE-AND-REPLACE (a genuine re-decision copied to a NEW ADR, `storytree adr new --pg`, that `supersedes` the old; the old flips to `superseded` and is KEPT as a browsable file, because other ADRs and story `decisions:` edges point at it and 'why we changed our mind' is worth keeping). It also REHOMES durable cross-cutting guidance buried in an ADR body into the Library artifact that owns it (the ADR-0095 graduation mechanism applied to ADR bodies; the ADR stays the source decision record, the derived guidance moves to the surface an orchestrator pulls just-in-time, asset:accepted-adrs-carry-no-stale-prose). Edges are binary on the axis of the TARGET'S SURVIVAL in the current set, not of its text — `supersedes` = the target LEAVES the set, `amends` = it STAYS in it as a live accepted ADR; `supersedes_in_part` is RETIRED and 'live in part' is no longer a state. `amends` is NOT a claim that the target is unchanged: an amender routinely retires or narrows a clause of its target, which is then corrected IN PLACE and its dead clause annotated (asset:accepted-adrs-carry-no-stale-prose). It maintains ADR frontmatter `status` and supersession edges (surfaced by `storytree adr list`), and — extending ADR-0084's agent green-flip — it MAY flip an ADR to `superseded` as curation, PROVIDED the flip projects the ADR's own `## Status` prose (it transcribes evidence, never invents a flip, ADR-0006/0031/0139). Active ⟺ load-bearing: every accepted ADR is current-state by definition, so the separate `load_bearing` tag retires at the END of the consolidation pass (the librarian's editorial call — NO hard cap on the set size), useful until then only as the worklist marker. Per ADR-0095 (amending ADR-0032) it also GRADUATES durable agent-memory into the Library — a third signal source alongside comments and cites: it extracts the durable ESSENCE from the private memory silo, deriving the definitions / principles that flow into agent guidance through the render pipeline (ADR-0051/0053) and routing a way-of-working to a `process` or design rationale to an open-question. It graduates ONLY genuinely durable, reusable material (the no-bloat bar, ADR-0095 D8) — the Library, ADRs excepted, holds only such 'able' artifacts, never event-specific cruft whose record stays in git / the ADR (D5) — then DELETES the graduated memory rather than caching it, dogfooding the Library as the canonical read surface (D6). The mechanical candidate core is `graduationCandidates` (@storytree/library, ADR-0095 D3); the genuine-durability judgment is the librarian's. Per ADR-0202 (amending ADR-0095) a reviewed memory that will NOT graduate is PARKED — a lease, not a tombstone: the verdict (`wont-graduate`), the reason, a content hash at review, the review date, and the lease length (default 60 days) land in the machine-local park ledger, so `check:graduation-worklist` WARNs only on new / changed / lease-expired candidates (normally zero, meaningful when not); any edit breaks the hash and re-enters the memory immediately, and lease expiry INVERTS the re-review question to 'is this still alive?' — re-park / delete / graduate-then-delete — so no dead memory outlives its death by more than one lease. Per ADR-0154 (amending ADR-0034) it also keeps the `process` tier a CURRENT projection of the load-bearing decision log — a standing, proactive charter reading the log FORWARD into processes, complementing (not replacing) the reactive memory-graduation above: any load-bearing ADR that changes a way-of-working must have a current `process` deriving from it (ADR-0034 §2 reference-don't-restate — the cited ADR wins, a process makes no new policy). It owns the process↔entrypoint bijection the advisory `check:surface-coverage` gate enforces (advisory PER GAP, but FAIL-CLOSED ON GROWTH since 2026-07-27 — a two-axis drain ceiling, `unresolved` and `orphan`, each redding on its own and never summed, ADR-0252 D3 in ADR-0168 D4’s shape; BOTH axes sit at ZERO since the 2026-07-28 drain of `pnpm ci:affected`, so a first gap on either axis reds the gate — see the ADR-0154 D2 correction and `packages/cli/src/surface-coverage-drain.ts`, which hold the live numbers), whose orphan list is the worklist — a surface with no process is graduated one or retired, a process with no surface is built or its absence recorded; coverage is gated, never necessity. Per ADR-0168 D4 the same standing pre-merge pass ALSO drains friction: it hygiene-checks new `friction` items (evidence and refs sane) and runs a BOUNDED drain of the K≈3 oldest ROUTABLE items (aged ≥1 session, filed by ANOTHER branch — never the whole worklist, never marking its own homework) through the `friction-adjudication` gate, handing the deeper adjudication — the whole-system view, the `adr` route, the owner-escalation judgment — to the graduation-synthesist (the dedicated adjudicator, ADR-0168 D5); the librarian HELD that chair under the same role-agnostic ceremony until the synthesist was built, and now runs only the routine bounded drain. It does NOT author the work hierarchy (story-author) or the behavioural rule content (guidance-curator), and never flips `accepted → proposed`; it curates where things sit, whether they belong, and whether the decision log still tells the truth.",
    "outcome": "New material is either a genuinely novel unit or an edit to the existing one — never a near-duplicate; every extracted unit names its 2+ consumers; the reference tier is internally consistent; pruning proposals cite the blind-reconstruction test. The decision log stays honest: every `accepted` ADR is TRUE IN FULL (ADR-0139 — overtaken prose is corrected in place or the ADR is superseded, never left stale), every ADR `status` projects its `## Status` prose, every supersession is recorded as an outgoing edge (the `supersede-consistency` gate), no ADR carries the retired `supersedes_in_part` edge, durable guidance buried in an ADR body is rehomed into its Library artifact, and a re-decision leaves the old body intact as a superseded file while an in-place correction leaves no shell behind (its prior text recoverable from git). Graduated agent-memory yields ONLY genuine durable artifacts — the durable essence captured into the right kind BEFORE the source memory is deleted (capture-then-delete, never a speculative drop, ADR-0095 D6/D8), redundant or event-specific candidates rejected (D5), and the derived definitions / principles reaching agent guidance through the render pipeline (D4). Reviewed non-graduates are PARKED with reasons (ADR-0202 — the lease defaults to 60 days, hash-invalidated on any edit) so the graduation worklist counts only live candidates — normally zero — and deletion follows graduation or a lease re-review that finds the memory dead, never without an explicit reviewed verdict. The `process` tier stays a current projection of the load-bearing decision log (ADR-0154) — every load-bearing way-of-working ADR has a `process` deriving from it, and `check:surface-coverage` reports no unexplained orphan (each surface resolves to a real entrypoint; each operator-facing entrypoint has a process behind it or a recorded reason). Writes persist through the CLI boundary or the librarian escalates.",
    "context": [
      "asset:merge-ceremony",
      "asset:plan"
    ],
    "tools": "Read / Grep / Glob (incl. the agent-memory store `~/.claude/projects/<project>/memory/` for the ADR-0095 graduation pass); `storytree library` read + `artifact edit|new --pg` (validated boundary) + the `graduationCandidates` engine (@storytree/library) for candidate generation; `storytree adr list` (read the decision log) + Edit on `docs/decisions/*.md` — frontmatter (`status` / supersession edges) AND body (an in-place CORRECTION that removes overtaken prose to keep an accepted ADR true, ADR-0139) — and `storytree adr new --pg` for a supersede-and-replace re-decision; `storytree friction list` (read the worklist) + `storytree friction route --pg` (adjudicate the K≈3 bounded pre-merge drain — set route/routeReason, ADR-0168 D4); the corpus-build view regeneration. Least-authority: no story authoring, no gate; never `accepted → proposed` (un-deciding stays human-only); an in-place body edit is a CORRECTION (the decision unchanged), never a silent re-decision — a changed decision is copy-on-write; a graduated memory (or rehomed ADR-body guidance) is deleted only AFTER its essence lands in the Library (ADR-0095 D6).",
    "workflow": "**session_start:** read the live corpus index (`--pg`) and the area in question.\n\n1. Novelty check — does an existing unit already cover this? Dedupe against the corpus before any new write.\n2. If covered, edit-first; if shared by 2+ consumers, extract a unit naming them; below two, leave it in place.\n3. Maintain the reference tier + cross-links; flag reconstructible guidance for pruning (blind-reconstruction test).\n4. Graduate durable agent-memory (ADR-0095; park-lease per ADR-0202) — read the agent-memory corpus and run the candidate engine (`graduationCandidates`, @storytree/library); the worklist is LEASE-FILTERED (ADR-0202 D4, `classifyWorklist`): only NEW, CHANGED (a parked memory's content hash broke — an edit re-enters it immediately, D2), or LEASE-EXPIRED candidates return for review; parked candidates stay silenced while their lease holds. For each NEW/CHANGED candidate: if it clears the genuine-durability bar (D8), author its durable ESSENCE into the right kind — a definition / principle (these flow into agent guidance, D4), a `process` for a way-of-working, an open-question for design rationale — then DELETE the source memory (capture-then-delete, D6); if it will NOT graduate but still earns its keep in memory, PARK it with a REASON (`storytree library graduate park <name> --reason \"…\"` — verdict `wont-graduate` + reason + content hash + review date + lease, default 60 days): a lease, not a tombstone, and never a silent skip. For each LEASE-EXPIRED candidate the question INVERTS — not 'should this graduate?' but 'IS THIS STILL ALIVE?' — with exactly three honest outcomes: RE-PARK (still earning its keep), DELETE (its risk has passed — a closed-arc pointer whose follow-up landed, a trap a landed fix retired), or GRADUATE-THEN-DELETE (it proved durable after all). Deletion follows graduation OR a lease re-review that finds the memory dead — never without an explicit reviewed verdict (ADR-0202 amending D6). Reject redundant or event-specific candidates; the Library, ADRs excepted, takes only 'able' artifacts (D5).\n5. Decision-log truth-maintenance (ADR-0139) — EVERY loop, as part of this standing pre-merge pass (ADR-0095 D7), keep the `accepted` set TRUE IN FULL (no stale prose). For each ADR a unit overtook, ask: did the DECISION change? NO → CORRECT IN PLACE: edit the ADR body to remove/fix the overtaken prose (git is the archive — `git log -p`/`-S` recovers it; no new ADR). YES → SUPERSEDE-AND-REPLACE: `storytree adr new --pg` for the new ADR that `supersedes` the old, flip the old to `superseded` (projecting its `## Status` prose, recording the edge), keep the old as a browsable file. REHOME durable cross-cutting guidance buried in an ADR body into its Library artifact, leaving a lean ADR that links to it. Edges are binary on the TARGET'S SURVIVAL (`supersedes` = it leaves the current set / `amends` = it stays in it); `supersedes_in_part` is retired. `amends` does NOT mean the target is unchanged — an amender may retire a clause of its target, so correct that target in place and ANNOTATE the dead clause. Active ⟺ load-bearing — the `load_bearing` tag retires at the END of the consolidation pass (the librarian's editorial call, no hard cap), the ~76 accepted-but-untagged ADRs its worklist. Never invent a flip the prose does not support; never let an in-place CORRECTION silently become a re-decision.\n6. Process-tier projection (ADR-0154) — SAME standing pre-merge pass: keep the `process` tier a current projection of the load-bearing decision log, reading it FORWARD (complementing step 4's reactive graduation). For each load-bearing ADR that changes a way-of-working and lacks a current `process`, derive one — ADR-0034 §2 reference-don't-restate, the cited ADR wins and the process makes no new policy — or record why none is needed. Keep the process↔entrypoint bijection honest: `check:surface-coverage` (advisory per gap, but fail-closed on GROWTH past its two-axis drain ceiling — both axes at ZERO since 2026-07-28, so a first orphan or dangling `surfaces` ref reds the gate) is the mechanical check and its orphan list the worklist — a surface with no process → graduate a process or retire the surface; a process with no surface → build it or record why none exists. Gate coverage, never necessity.\n7. Friction drain (ADR-0168 D4) — SAME standing pre-merge pass: hygiene-check new `friction` items (evidence present + concrete, refs resolve), then run a BOUNDED drain of the K≈3 oldest ROUTABLE items (aged ≥1 session, filed by ANOTHER branch — never the whole worklist, never marking your own homework) through the `friction-adjudication` gate (`asset:friction-adjudication`; the RULE and evidence floor are `asset:friction-justification-bar`). This is the ROUTINE drain; hand the deeper adjudication — the whole-system view, the `adr` route, the owner-escalation judgment — to the graduation-synthesist (the dedicated adjudicator, ADR-0168 D5). The fail-closed ceiling is delegated to `check:friction-drain`; queue hygiene only, no count or age decides what graduates.\n8. Verify writes persisted. Stop — rule content is guidance-curator's, work units are story-author's, and the deeper friction adjudication is the graduation-synthesist's.",
    "rules": [
      "asset:edit-first-curation",
      "asset:slow-growth-minimum-to-green"
    ],
    "antiPatterns": [
      "asset:never-bypass-the-gate",
      "asset:approval-gated-trunk"
    ],
    "escalation": "The judgement line is 'did the DECISION change?'. A genuine re-decision is NOT an in-place body edit — it is supersede-and-replace (a new ADR, the old kept as a `superseded` file); only stale prose under an UNCHANGED decision is corrected in place (ADR-0139). A flip whose `## Status` prose does not yet support it is NOT made — never invent a flip; `accepted → proposed` un-deciding stays human-only. The risk a correction is misjudged as truth-maintenance when it is really a re-decision is caught on the reviewed PR — when in doubt, treat it as a re-decision (copy-on-write) or surface it, never silently rewrite a decided body. A prune that removes a unit other units cite, a structural change to the reference tier, or a dedupe judgement the owner should ratify is surfaced, not enacted; structural integrity vetoes deletion.",
    "references": [
      "asset:pull-based-context-architecture",
      "asset:edit-first-curation",
      "asset:glossary-wins",
      "asset:live-store-is-the-edit-surface",
      "asset:never-bypass-the-gate"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "createdAt": "2026-06-14T00:00:00.000Z",
    "updatedAt": "2026-07-16"
  },
  {
    "id": "plan",
    "kind": "definition",
    "title": "Plan",
    "oneLine": "The disposable, git-anchored choreography for one increment of an arc: the units in dependency order with their proof routes, contention, budgets and traps, written as one free `body` — freshness-checked at consumption, executed once, then closed.",
    "whatItIs": "A `plan` (ADR-0183 D2) hands one arc increment from planning intelligence to executing sessions as an inspectable artifact: the unit decomposition (story/capability ids, each with its proof route — `--real` red→green, glue per ADR-0158, or operator-attested), dependency order, parallel lanes (which units are independent, expected file surface per lane as fence hints, contention warnings), budgets in turn-cap vocabulary (ADR-0130), known traps, and escalation points.\n\n**Those seven things are now PROSE, not schema (ADR-0305 D4).** The body is two fields — a required one-sentence `objective` and a required free `body` — where it used to be five. `decomposition` / `lanes` / `budgets` / `traps` were editorial, never structural: `PLAN_BODY_FIELDS` in `packages/cli/src/plan.ts` concatenated all five and mined them identically, so no reader ever distinguished a lane from a budget. Their real function was to prompt the expensive planner model, which is prompt engineering — it moved into the `planner` agent's authoring guidance, where a checklist can be enforced by the reader. Nothing about what a plan must SAY was cut; only the four headings that pretended the machine could tell them apart.\n\nIt is an EPHEMERAL kind (`EPHEMERAL_KINDS` in `packages/library`): live-store-only, and disposable by construction. Born citing its arc (`arcRef`, D3) and pinned to the git commit it was planned against (`anchor.sha`). Consumption begins with a mechanical freshness check — `storytree plan check <id> --pg` git-logs the paths the plan names since its anchor; drift past threshold means re-plan, not repair. Once execution starts it is never edited — re-planning is cheap by construction, so supersede.\n\n**The lifecycle is FOUR states (ADR-0305 D2): `proposal` → `ready` → `active` → `closed`.** `proposal` is decided-but-not-started, absorbing what ADR-0298 D1 called a parked entry. `ready` is authored and consumable. `active` is execution started and carries D2's write-lock unchanged — only the name moved, from `consumed`, so the state says what it IS rather than what was done to it. `closed` is terminal for ANY reason: `superseded` and `retired` differed only in WHY the work stopped, so the reason moved into the closing `outcome.note`, which is what lets a wrong entry be closed honestly rather than marked landed.\n\n**Naming the paths.** The freshness check has no notion of granularity: the extractor behind `plan check` (`extractPlanPaths`, `@storytree/cli`) harvests EVERY backtick path-token from the body, so a path mentioned in passing in a trap note is watched exactly like a lane's fence hint, and the verdict SUMS commits across every named path against a default threshold of 0. The backtick convention is the ONE thing the D4 collapse left load-bearing, and it now rests on `body` alone: a plan naming no paths is reported VACUOUS, explicitly not green. The path set is therefore authored deliberately, and it can be wrong in two OPPOSITE directions — which matters because each is the tempting fix for the other. Too BROAD: a directory matches its whole subtree, so a single orthogonal commit under it reads as drift and forces the mandated re-plan (D2), and naming a leaf *plus* its enclosing directory counts that one commit twice. Name the narrow material file whose change would genuinely invalidate the decomposition, never the directory that contains it. Too NARROW: trimming is not the remedy — a real coupling left unnamed breaks a lane instead of tripping a check. Completeness is judged across every coupling graph, not just the import graph: much of the gate is disk SCANNERS keyed on paths — the coverage checks walk `stories/` and read each capability's registered `proof.real.testFile`, `check:boundaries` gathers `proof.real.sourceFile` and literal source globs the same way, `check:manifest` reads `git ls-files` — and a path coupling leaves no import edge to find. A DELETION plan is the sharp case: inventory those disk-scanning registrations alongside the import graph, or the miss surfaces late, as a red gate on the deletion branch after the lane is already committed. Per path, ask both questions: would a commit here really invalidate the decomposition, and would a commit I did *not* name break a lane?\n\nAuthored by the `planner` agent (D5); consumed by the session-orchestrator, which validates freshness, takes lanes via the existing claim machinery (ADR-0121/0142), executes, and records the outcome on the arc.\n",
    "createdAt": "2026-07-11T00:00:00.000Z",
    "updatedAt": "2026-07-11T00:00:00.000Z",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "references": [
      "doc:decisions/0183-arcs-contain-plans-the-initiative-overlay-upstream-of-storie.md",
      "asset:arc",
      "asset:anchor-implementation-surface",
      "asset:orchestrate-route-supplement"
    ],
    "description": "The disposable, git-anchored execution choreography for one increment of an arc (ADR-0183): an ephemeral, Postgres-only kind — freshness-checked at consumption, executed once, then closed.",
    "whatItIsNot": "Not durable — no git history, no seed, prunable once consumed; anything worth keeping graduates to the arc's increment log or the Library. Not a decision or a work definition — a design fork discovered while planning exits to an ADR, and hierarchy changes route through story-author; a plan only choreographs work that is already defined. Not mandatory — a one-session, one-unit piece of work stays planless (D6); mandatory plans would be plan-theater. Not reachable from remote (web/VM) sessions — plans are live-store data, and such a session cannot run the Cloud SQL connector (client-mTLS cannot cross its TLS-terminating proxy; ADR-0250 D1, narrowed by ADR-0258 D2). The fence is the connector, not a blocked port and not database access in general: an HTTP front door would reach them (ADR-0259), and none is deployed.",
    "schemaVersion": 7
  },
  {
    "id": "anchor-implementation-surface",
    "why": "Surface data is the fastest-rotting prose in the corpus: files move, packages dissolve, and a stale file list misleads silently — the reader cannot tell truth from rot (the exact failure ADR-0139 evicts execution prose from ADR bodies for). In an anchored artifact the same list is honest: `anchor.sha` makes staleness DETECTABLE — git-log the named paths since the anchor, the proof tier's source-drift move applied to intentions — and disposability makes the response cheap: re-plan, don't repair.",
    "kind": "principle",
    "title": "Implementation surface is written only into anchored, disposable artifacts",
    "createdAt": "2026-07-11T00:00:00.000Z",
    "statement": "Implementation surface — file lists, package paths, expected edit footprints, ordering over concrete code — may only be written into artifacts that are both git-ANCHORED (pinned to the commit they were authored against, freshness-checked mechanically at consumption) and DISPOSABLE (superseded on drift, never repaired); a durable artifact — an ADR body, story, principle, definition, or arc — never carries it (ADR-0183 D4).",
    "updatedAt": "2026-07-11T00:00:00.000Z",
    "howToApply": "About to write a file path, package list, or edit footprint into a durable doc? Move it into the anchored disposable tier — today a `plan` (ADR-0183), checked by `storytree plan check <id> --pg` — and leave the durable doc with intent, outcomes, and pointers. The test: would this list still be true in a month, and would the reader notice if it weren't? When a durable doc must name code (a guardrail's enforcement point, a techstack constraint), name the owning SURFACE — a command, a package, a gate — never an enumerated file list. That coarsening rule is the DURABLE side only, and it INVERTS inside the anchored tier: a plan's named paths are the very thing its freshness check git-logs, so naming the owning directory there makes any orthogonal commit in the subtree read as drift — see `asset:plan` for the path-set discipline that applies once surface has been moved. Moving surface out of a durable doc is likewise a rule about the PROSE, not about the artifact: a renderable or binary the owner approved is still committed and pointed at (`asset:an-owner-approved-reference-is-repo-resident`).\n",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "references": [
      "doc:decisions/0183-arcs-contain-plans-the-initiative-overlay-upstream-of-storie.md",
      "doc:decisions/0139-the-accepted-adr-set-carries-no-stale-prose-correct-in-place.md",
      "asset:plan",
      "asset:arc"
    ],
    "description": "File lists, package paths, and edit footprints rot fast: they may only live in artifacts that pin the git SHA they were true at and are superseded (never repaired) on drift — a durable doc carries intent and pointers only.",
    "schemaVersion": 7
  },
  {
    "id": "arc",
    "kind": "definition",
    "title": "Arc",
    "oneLine": "The initiative overlay: a named multi-story owner intent tracked through an increment log to a closed end-state — upstream of stories and ADRs by provenance, containing plans, rendered as \"Epic\" in the studio.",
    "whatItIs": "The fourth grouping tier ADR-0002 parked, returned as an **overlay, not a tier** (ADR-0183 D1): an `arc` names one owner initiative that spans multiple stories, ADRs, and plans, and tracks it to a closed end-state. Nothing proof-related rolls up to it — the story remains the top proof grain (no verdict, no UAT, no rollup on an arc).\n\nAn arc is live-canonical like every non-agent kind (ADR-0023) and ceremony-light by construction. It is BORN through `storytree arc new --title \"…\" --intent <text|@file> --end-state <text|@file> --pg`, which takes those three substantive fields and stamps the mechanical ones (id, description, timestamps, `lifecycle: active`) — an arc is never hand-authored as doc JSON. Thereafter its only authored mutations are slow — intent changes (`storytree arc edit`), and entries appended to its structured `increments` landing log at each merge ceremony (`storytree arc increment add`; date, PR#, outcome — what landed, halted, or was re-planned) — until `storytree arc close` records the end-state as reached (ADR-0239). That log is the durable residue that survives plan pruning.\n\nEvery containment edge lives on the CHILD (D3): a plan is born citing its arc (`arcRef`), and an ADR or story gains an optional, immutable `arc:` provenance stamp at creation (`storytree adr new --arc <id>`; story frontmatter). The arc reveals its plans, stories, and ADRs by derived query (`storytree arc show <id> --pg`), never by editing the arc when a child is born. Upstream means provenance and intent, not authority: the arc never overrides story-author on WHAT or the decision log on WHY.\n\nThe studio displays the kind as **\"Epic\"** (a display alias only — the kind key, CLI, and refs use `arc` exclusively).",
    "createdAt": "2026-07-11T00:00:00.000Z",
    "updatedAt": "2026-07-11T00:00:00.000Z",
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store.",
    "references": [
      "doc:decisions/0183-arcs-contain-plans-the-initiative-overlay-upstream-of-storie.md",
      "asset:plan",
      "asset:anchor-implementation-surface"
    ],
    "description": "The initiative overlay (ADR-0183): a named multi-story owner intent tracked through an increment log to a closed end-state — upstream of stories and ADRs by provenance, rendered as \"Epic\" in the studio.",
    "whatItIsNot": "Not a proof unit — nothing rolls up to it; health is judged at the story (ADR-0010). Not durable guidance — lessons still graduate out through ADR-0095/ADR-0168; an arc holds state and pointers only, and implementation surface is banned on it (ADR-0183 D4 — surface lives in anchored, disposable plans). That ban is on the PROSE, not on the artifact: a design reference the owner has APPROVED is committed to a repo path and the arc carries the pointer to it, never a machine-local one — an approved renderable passed as a visualization path does not survive the repull, and the next session rebuilds a different surface from the prose alone (`asset:an-owner-approved-reference-is-repo-resident`). Not a `plan` — the plan is the disposable choreography for ONE increment of an arc. Not mandatory — one-session, one-unit work stays arc-less (D6): the trigger is work that spans sessions or parallel lanes. Once that trigger IS met the arc is written at DECOMPOSITION TIME — before the second increment is chipped — not reconstructed at close. A multi-increment initiative tracked by `asset:session-cutting` chips alone has nowhere queryable to hold what every increment needs (which increments are done, the running counts, the findings that must not be re-derived), so each chip re-carries the whole method in its own prompt and every session that picks one up pays full re-orientation: chips SPAWN sessions, the arc HOLDS the context those sessions need, and chip-only tracking converts one initiative into N independently-orienting ones. Measured 2026-08-03, when the owner halted the fleet over session sprawl: the ADR-0294 story-UAT surgery ran to two ADRs, one landed increment and three chipped ones with no arc at all, and its arc had to be built retroactively at close.",
    "schemaVersion": 7
  },
  {
    "id": "orchestrate-route-supplement",
    "kind": "pattern",
    "title": "Orchestrate, route, supplement: the inner loop is one tool",
    "problem": "Treating `node/story build --real` as 'the job' force-fits work a machine can't cleanly sign — GLUE with nothing worth asserting (SQL/DB adapters, dependency additions, pure config or wiring) — into a proof that can't pin it, or silently skips it. The routing filter that actually works is 'does this piece have an isolatable red→green test?', NOT package boundaries: one package can hold both an algorithm the leaf proves and glue it can't, and a provable algorithm can straddle packages. (Glue is one of TWO orthogonal reasons a machine can't sign a unit; the other is operator-attested work — look/feel/live/spend (ADR-0070), where the output is only judgeable by a human — routed to the frontend-builder / an operator-attested leg, NOT folded into the glue bucket. ADR-0158 D2.)",
    "approach": "1. DECOMPOSE into provable units — the filter is 'does this piece have an isolatable red→green test?', not package boundaries. The provable units carry the algorithm; the rest is integration/wiring.\n2. ROUTE the provable units to the inner loop, chained in dependency order: either `story build --real` (a story's capabilities, ordered by `depends_on`, stack in one worktree and promote once, ADR-0031) or sequenced individual `node build --real` runs across merges (each fresh worktree is cut from current main, so a landed piece is visible to the next). Cross-package work is SEQUENCED via `depends_on`, never done atomically. A complex driver-composition / multi-surface --real build can exhaust the per-slice turn ceiling (--max-turns, default 16) even after the leaf reaches green (run_proof=green) — it fails to TERMINATE, surfacing as `Reached maximum number of turns`. This is orthogonal to --budget (the USD ceiling): a build well under budget still hits it, so READ the failure — a turn-exhaustion is not a real red. Give such a build headroom — size the turn budget by what the leaf must produce, not by file size (`turn-budget-keys-on-assert-surface`); the default 16 is independently known-tight for orientation too (storytree orchestrate). When the routed unit is an editsExisting re-drive — its behaviour already partly at HEAD — scope its brief to ONE behaviour with a mechanically-pinned red and freeze what is already built (`mechanical-red-redrive-brief`), else the gated leaf silently ships one-of-N behaviours or reds a different, still-uncovered contract.\n3. SUPPLEMENT the non-leaf glue — SQL/DB adapters, dependency additions, pure config/wiring with nothing to assert — with the orchestrator's OWN subagents (the Agent tool); do it yourself only as a last resort. (Visual/UI is NOT glue: it is operator-attested work — look/feel/live/spend (ADR-0070) — a SEPARATE orthogonal axis routed to the frontend-builder / an operator-attested leg, not folded in here. ADR-0158 D2.) When a dependency addition instead feeds a capability the leaf DOES prove but that SHIPS through a different runtime (e.g. `node:test`-proved, esbuild-bundled), it is not pure glue: pin its surviving import form in the spec — a form green in the proof runtime routinely crashes the ship runtime (`pin-the-dual-runtime-import-form`).\nThrough-line: the inner loop carries the ALGORITHM load; the orchestrator carries the INTEGRATION/WIRING load. When the inner loop genuinely can't prove a piece — it needs a DB-backed proof, a new dependency, or a browser/visual proof it lacks — that is a capability gap to RAISE and EXPAND; in the meantime route the piece to a subagent. Never force-fit it into a hollow proof and never silently skip it.",
    "createdAt": "2026-06-16T00:00:00.000Z",
    "statement": "The inner loop — the leaf prove-it-gate → signed verdict (`storytree node|story build --real`) — is ONE tool the session-orchestrator wields, not the whole job. The orchestrator's job is to DECOMPOSE work into provable units, ROUTE those to the inner loop chained in dependency order, and SUPPLEMENT the non-leaf glue with its own subagents — doing the glue itself only as a last resort.",
    "tradeoffs": "Routing costs something: deciding provable-vs-glue per piece and sequencing cross-package work over several merges is slower than one atomic change — but an atomic cross-package edit can't be proven unit-by-unit, and a hollow `--real` proof over un-isolatable glue is worse than no proof. The standing risk is the orchestrator quietly absorbing glue it should have delegated, or force-fitting glue into the inner loop instead of naming the capability gap. Composes with `slow-growth-minimum-to-green` (the leaf's discipline INSIDE a provable unit) and `cross-story-dependency` (the `depends_on` direction rule that orders the routing).",
    "updatedAt": "2026-07-14T00:00:00.000Z",
    "references": [
      "doc:decisions/0030-all-in-on-claude-agent-sdk.md",
      "doc:decisions/0031-real-pass-promotion-and-worktree-deps.md",
      "doc:decisions/0057-dogfood-the-inner-loop-as-the-default-node-borne-proof-confi.md",
      "asset:slow-growth-minimum-to-green"
    ],
    "description": "The leaf prove-it-gate is one tool the orchestrator wields, not the whole job: decompose work into provable units, route those to the inner loop in dependency order, and supplement the non-leaf glue with the orchestrator's own subagents.",
    "schemaVersion": 7,
    "provenance": "Frozen fixture subset (ADR-0302 D1) — NOT a mirror of the live store."
  },
  {
    "id": "adr-0002",
    "kind": "adr",
    "title": "The work hierarchy — story, capability, contract",
    "description": "Three tiers — story, capability, contract — with the PROOF MODE as the boundary between them.",
    "number": 2,
    "status": "accepted",
    "decided": "2026-06-03",
    "body": "# ADR-0002: The work hierarchy — story, capability, contract\n\n## Status\n\naccepted (2026-06-03) — the three tiers and the proof-mode boundary stand. ADR-0010 later moved\nthe integrated UAT up to the story tier; the ladder below already reflects that.\n\n## Context\n\nv1 had a single provable grain, and it was too FINE to double as a system map: the tree fragmented\nto behaviour-sized nodes and the top-level view became a wall of serde-sized cards. The map wants a\ncoarser top unit; prove-it rigor wants a finer one. v1 collapsed both onto one word. storytree\nsplits them into three tiers, and the boundary between tiers is the PROOF MODE.\n\n## Decision\n\nThree tiers, top to bottom:\n\n- **story** — the top-level unit you watch grow, and a node on the DAG the studio renders. A\n  coherent, independently meaningful body of work, composed of capabilities. The map grain: the\n  thing a newcomer points at. Proven as a whole by the integrated UAT walkthrough.\n- **capability** — a component within a story: independently viable, integration-proven against\n  real in-story collaborators, and composed of contracts.\n- **contract** — a single test-proven behaviour within a capability: one automated, isolated test\n  with its collaborators stubbed. The leaf.\n\n## Consequences\n\nBecause the tier boundary IS the proof mode, \"what grain is this?\" and \"how is this proven?\" stop\nbeing two questions. A unit that cannot name its proof mode has not been placed on the hierarchy\nyet.\n",
    "createdAt": "2026-06-03T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "references": [],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — an ABRIDGED stand-in for the live decision, NOT a mirror of it.",
    "schemaVersion": 7
  },
  {
    "id": "adr-0013",
    "kind": "adr",
    "title": "A structured, schema-validated corpus; markdown as a generated view",
    "description": "The corpus is structured and schema-validated at the write boundary; markdown is a generated view of it, never the source.",
    "number": 13,
    "status": "accepted",
    "decided": "2026-06-06",
    "body": "# ADR-0013: A structured, schema-validated corpus; markdown as a generated view\n\n## Status\n\naccepted (2026-06-06) — extends ADR-0002's work hierarchy toward its schema encoding. The FORMAT\ncall here was later flipped from YAML to JSON by ADR-0039; the structured-source principle below is\nunchanged and carried by JSON.\n\n## Context\n\nA corpus kept as prose in document containers cannot be validated, queried, or migrated. Every\nreader re-parses the same paragraphs by hand and every writer is free to invent a shape, so drift\nis invisible until something downstream reads the wrong field.\n\nMarkdown is still what a human wants to read. The mistake is treating the readable form as the\nSOURCE.\n\n## Decision\n\nThe corpus is STRUCTURED and schema-validated at the write boundary. Narrative lives in typed\nprose FIELDS, decomposed only where decomposition pays. Markdown is a GENERATED VIEW rendered from\nthose fields — never the thing edited, and never the thing stored.\n\nThe principle is corpus-wide. Decisions convert like anything else: a decision record is one\nraw-rendered `body` field plus the typed state a reader queries (its number, its status, its\nedges).\n\n## Consequences\n\nValidation has one place to happen and one place to fail loudly. A rendered document can always be\nrebuilt from the store, so no generated view is ever the thing to repair — the row is.\n",
    "createdAt": "2026-06-06T00:00:00.000Z",
    "updatedAt": "2026-06-08T00:00:00.000Z",
    "references": [
      "asset:adr-0002"
    ],
    "provenance": "Frozen fixture subset (ADR-0302 D1) — an ABRIDGED stand-in for the live decision, NOT a mirror of it.",
    "schemaVersion": 7
  }
];

{
  "name": "@storytree/context-traversal-spawn",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The build spawn boundary as linked parent/child context-traversal lanes: per-slice leaf run accounting becomes spawn-handoff, child model-context, and result-return observations (ADR-0235 / ADR-0241).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import ../../scripts/tsx-cache-off.mjs --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@storytree/context-traversal-capture": "workspace:*",
    "@storytree/context-traversal-telemetry": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~5.7.3"
  }
}

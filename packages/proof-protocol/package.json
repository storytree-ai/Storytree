{
  "name": "@storytree/proof-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The published verdict SHAPE (ADR-0068 §3): DATA shapes + zod validators only — the contract readers validate verdict-DATA against, across the built ADR-0010 §4 boundary. No proof machinery, no node: imports, browser-safe, zod-only runtime dep.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 300000 src/"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "~7.0.2"
  }
}

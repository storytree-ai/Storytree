{
  "name": "@storytree/context-traversal-capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable per-session context-traversal capture at the terminal CLI boundary: a local JSONL sink, the argv read-allowlist, and the replay query (ADR-0235 / ADR-0241).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@storytree/context-traversal-telemetry": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~5.7.3"
  }
}

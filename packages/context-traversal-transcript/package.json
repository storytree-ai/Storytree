{
  "name": "@storytree/context-traversal-transcript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The host transcript surface as the orchestrator's own context-window occupancy: per-request resident totals, correlated to a storytree session and ingested into its trace (ADR-0235 / ADR-0241 / ADR-0248 D1).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 300000 src/"
  },
  "dependencies": {
    "@storytree/context-traversal-capture": "workspace:*",
    "@storytree/context-traversal-telemetry": "workspace:*",
    "@storytree/library": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "~7.0.2"
  }
}

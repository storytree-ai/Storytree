{
  "name": "@storytree/arc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The arc organism: the initiative overlay (ADR-0183 / ADR-0267 / ADR-0305) as its own package — the arc / increment / open-question write verbs, their ADR-0023 render surface, and the derived arc → children JOIN every surface shares. Owns the arc domain rather than lodging in @storytree/cli and @storytree/drive; consumed by the CLI, the studio server and the desktop backend.",
  "exports": {
    ".": "./src/index.ts",
    "./arc-rollup": "./src/arc-rollup.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import ../../scripts/tsx-cache-off.mjs --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@storytree/drive": "workspace:*",
    "@storytree/library": "workspace:*",
    "@storytree/storage-protocol": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~5.7.3"
  }
}

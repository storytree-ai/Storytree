/**
 * Codex's hook inputs. Not read yet: contract 3.2 adds them, from inputs recorded from Codex.
 */
import type { HookLines } from "./hooks.js";

export function codexLines(_input: Record<string, unknown>): HookLines | undefined {
  return undefined;
}

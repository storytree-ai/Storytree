/**
 * Capability 7 · Instructions, the habits card (stories/agent-link.md). Not written yet: this stub
 * gives the contracts' tests something to fail against.
 */
export function habitsCard(): string {
  throw new Error("the habits card is not written yet");
}

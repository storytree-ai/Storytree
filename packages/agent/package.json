{
  "name": "@storytree/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "storytree owned agent loop: the per-node leaf runtime on the raw Anthropic Messages API (ADR-0011), behind a thin Model seam. Replaces pi-adapter.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.170",
    "@anthropic-ai/sdk": "^0.39.0",
    "@storytree/notice-board": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~5.7.3"
  }
}

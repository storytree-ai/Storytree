{
  "name": "@storytree/notice-board",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The notice-board organism (ADR-0068 step 6): the session-presence schema + staleness classification (PresenceDeclarationDoc / classifyPresence / mergeDeclaration / staleness thresholds), extracted from the dissolving @storytree/core. Pure zod, browser-safe, no node: imports.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": { "zod": "^3.24.1" },
  "devDependencies": { "@types/node": "^22.10.7", "tsx": "^4.19.2", "typescript": "~5.7.3" }
}

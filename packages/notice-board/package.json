{
  "name": "@storytree/notice-board",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The notice-board organism (ADR-0068 step 6): the per-unit claim-ledger schema + reclaim predicate (the one session machinery since the presence retirement, ADR-0200 D7), extracted from the dissolving @storytree/core. Root barrel is pure zod, browser-safe; the Postgres claim store lives behind ./store.",
  "exports": {
    ".": "./src/index.ts",
    "./store": "./src/store/index.ts",
    "./store/claim-store": "./src/store/claim-store.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 300000 src/"
  },
  "dependencies": {
    "@storytree/library": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/pg": "^8.11.10",
    "typescript": "~7.0.2"
  }
}

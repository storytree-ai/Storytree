{
  "kind": "uat-criterion",
  "id": "studio-build#uat-4",
  "action": "Against a real `node:http` server over the real `handleApiRequest` and a real `BuildRegistry` — with only the RUNNER injected, following `buildApi.integration.test.ts`'s `heldRunner` pattern (a captured `resolve` that holds the run open) — dispatch one intent, then poll `GET /api/build?runId=…` AT LEAST TWICE while the run is still live, capturing each response's `transcript` array in order, releasing more phase lines between polls; then poll once more after the run terminalises. The injected runner stands in for the SDK leaf and emits the coarse phase trail: an offline scripted `PhaseAuthor`-shaped collaborator is explicitly acceptable here (ADR-0010 §5, blessed by the story's own Proof section) because the observable is the accumulation, not the authoring — the real billed leaf is the story's leg 9, not this leg. No subscription spend may be incurred by this spec.",
  "successConditions": "Poll N+1's transcript EXTENDS poll N's rather than replacing it — the earlier lines are still present, in their original order, with the new ones appended — the phase-trail lines appear in order (AUTHOR_TEST → … → GATE), and after the terminal response the client issues no further polls. Two existing proofs are insufficient and do not discharge this leg: `BuildSection.test.tsx:99-132` proves growth and loop teardown against a MOCKED `api` client, so it never exercises the endpoint at all; and `buildApi.integration.test.ts:89-94` issues a SINGLE GET asserting the transcript merely CONTAINS `'phase: AUTHOR_TEST'`, which proves presence, not accumulation. The phase trail must additionally be ASSERTED rather than only seeded — today a `'phase trail: AUTHOR_TEST → GATE'` line is placed in a fixture at `buildApi.integration.test.ts:110` and the assertion at :113 matches only `/verdict: PASS/`.",
  "evidenceExpectations": "The spec's output listing each poll's transcript array in sequence so the prefix-extension is visible rather than asserted in the abstract, plus the poll count freezing after the terminal response. The injected runner must be visible in the evidence so it is unambiguous that no live SDK run was billed. This leg is the MACHINE half of a criterion that previously fused the mechanics onto the billed run and was tagged human as a whole; the split follows `human-witness-is-a-judgment-gap-not-cost` — a transcript that grew is an ordered-list comparison, and only the spend and the genuineness of the run needed the operator.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

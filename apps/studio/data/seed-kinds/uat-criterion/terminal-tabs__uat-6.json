{
  "kind": "uat-criterion",
  "id": "terminal-tabs#uat-6",
  "action": "In the Electron `_electron` Playwright harness (`apps/desktop/e2e/`), in a NEW spec file — never by extending `session-survival.e2e.mjs`, which asserts exactly ONE live session in both directions and would fail the moment a second is opened. Launch with `STORYTREE_DESKTOP_E2E=1` (backend sidecar skipped), Playwright-route `/api/*` to the harness fixtures (`stubApi`), and satisfy the repo gate BEFORE entering the forest by writing `userData/repo-selection.json` pointing at the launch checkout — since `terminal-repo-picker` (#705) `TerminalRepoGate` renders the gate message instead of the dock until a valid repo exists, so a bare launch never shows a session panel at all. Then walk the panel over REAL node-ptys: expand the dock and wait for the first session; click the panel's `+` (`[aria-label=\"new terminal tab\"]`); click row 1 (`[aria-label=\"tab 1\"]`); click row 2's close (`[aria-label=\"close tab 2\"]`). Read the session set at each step via `window.desktopTerminal.list()`.",
  "successConditions": "`desktopTerminal.list()` reports 1 session after expand, 2 after the `+` (a genuinely independent second pty, a NEW sessionId — not a re-used or re-spawned first), and 1 after the `×`, and the SURVIVING id is the id observed for row 1 at step one — so the close disposed exactly the closed row's pty and left the sibling's alive. The panel carries one `.terminal-dock-panel-row` per live session throughout, exactly one row carries `.terminal-dock-panel-row-active`, and that marker follows the click. Exactly ONE `.terminal-dock-toggle` and ONE `.terminal-dock-header-right` exist at every step regardless of session count. FALSE-PASS FENCES: (a) Re-running the jsdom `mst-*` suite is NOT this leg — capability `multi-session-tabs` signs the same lifecycle over a MOCKED `desktopTerminal` whose `spawn` resolves a scripted id; a green there says nothing about whether the real IPC bridge, the real `PtySessionManager`, and a real node-pty behave the same way, which is the only thing this leg adds. (b) Asserting on DOM row counts alone passes trivially and proves nothing about ptys: the load-bearing observable is `list()`, the main-held session set. (c) The dock paints on xterm's WebGL renderer where available, so any assertion reading pane text via DOM `textContent` will read empty and must not be used as evidence of a live session. (d) A spec that opens the app WITHOUT pre-writing `repo-selection.json` and then asserts the panel is absent is testing the repo gate, not this leg, and would report a false negative here.",
  "evidenceExpectations": "The new e2e spec's `node:test` output showing the `list()` cardinality at all four steps (1 → 2 → 1) with the actual sessionIds printed, so the surviving-id identity claim is auditable rather than asserted; plus the per-step active-row marker and the per-dock toggle/headerRight counts. Evidence that shows only DOM row counts, or only a jsdom vitest run, does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

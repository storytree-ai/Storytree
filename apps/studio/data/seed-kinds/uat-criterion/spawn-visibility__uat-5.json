{
  "kind": "uat-criterion",
  "id": "spawn-visibility#uat-5",
  "action": "An APPEARANCE judgment, not a functional walk. With a real spawn running and its line rendered live in a transcript alongside real streaming `delta` prose, the owner looks at the transcript and judges whether the spawn line READS as \"a subagent is working right now\" — at a glance, without being told what to look for, and without inspecting logs. The judgment covers three things: legibility of the running line, its visual distinguishability from the surrounding delta text, and whether the started -> finished flip reads as one line RESOLVING rather than as two unrelated lines. BLOCKED AS WRITTEN (2026-07-26): the leg presumed the in-app interactive orchestrator chat, retired by ADR-0174 for an embedded terminal running real Claude Code, so there is no in-app conversation left in which to spawn. Recorded here so the judgment is preserved, not so it can be run.",
  "successConditions": "The owner judges the line legible and unambiguous as a live-spawn signal, visually separable from delta prose, and its resolution readable as a resolution. Nothing else. FALSE-PASS FENCES: (a) THE PRIMARY ONE — attesting that the STRINGS are correct is not this leg. The exact text, its ordering and its arrival are already compiled and machine-witnessed by leg 5's sibling, leg 3, over the real `ChatEvent` union and `ChatPanel` render; re-attesting them here would launder a compiled fact into an unrepeatable signature, which is exactly what the ADR-0209 D8 pass exists to undo. Attest ONLY the remainder that has no compiler. (b) Attesting against a jsdom component-test screenshot, a Storybook frame, or a hand-built HTML fixture is a false pass: the judgment is about how the line reads AMID real streaming prose at real timing, which a static fixture cannot present. (c) Attesting the started state alone is insufficient — the resolution reading is half the claim. (d) `ChatPanel.tsx:415-419` has THREE branches (started, `ok === false` -> \"failed\", otherwise -> \"finished\"); an attestation that never saw the failure branch must say so rather than implying the whole surface was judged. (e) SPEND IS NOT THE BASIS. A live spawn being subscription-billed is a cost and blast-radius reason not to run this on a gate pass; it is recorded as a separate fact and must never be offered as the reason the leg is human. If the only thing standing between this leg and a harness is money or setup, it is machine and belongs on the machine rung.",
  "evidenceExpectations": "An owner attestation naming the date, the surface actually looked at, and which of the three sub-judgments (legibility / distinguishability / resolution reading) were exercised — plus a capture of the live transcript in the state judged. Since the leg is currently BLOCKED, the honest evidence state is: none, and unstamped. NOTE: no owner attestation has ever been recorded against any leg of this story; the earlier \"already operator-attested\" wording traced to a single docs-only commit (903b6da5, 2026-07-04) that authored it as a bare assertion. The standing open call on whether an attestation carries forward onto a re-adjudicated leg (`wisp-as-story-claim`, open modeling call 1) therefore does not bite here — there is no signature to carry forward.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

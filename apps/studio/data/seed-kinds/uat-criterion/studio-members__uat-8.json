{
  "kind": "uat-criterion",
  "id": "studio-members#uat-8",
  "action": "As a resolved admin, grant friend@example.com the `builder` role through the in-app `/api/users` route the Members panel calls (POST an invite carrying role `builder`, or PATCH an existing member's role to `builder`) — no gcloud, no Cloud SQL IAM grant. Then, as that granted identity, read a corpus API, write a comment, and evaluate the brokered-write predicate the write-broker gate reads.",
  "successConditions": "The grant is ACCEPTED (not a 400 rejecting the role) and the user projection holds a row for that email with role `builder`. The same identity then resolves with the member read+comment scope, and satisfies the brokered-write predicate (`admin` and `builder` satisfy it; a plain `member` does not). Every clause is machine-decidable — an HTTP status, a persisted row's role field, and a pure predicate over the resolved role — which is why this criterion is witnessed `machine`, not `human`. NOT YET WALKABLE as of 2026-07-26: the `/api/users` role validator admits only `admin` or `member`, and the Members panel's invite select and row toggle offer no `builder` at all, so the grant this criterion names is a 400 at the API and unreachable in the UI. The criterion is therefore left with NO proof binding on purpose.",
  "evidenceExpectations": "The grant request/response pair showing an accepted status and the persisted `builder` role; the projection row read back; the read + comment outcomes for that identity; and the brokered-write predicate's value for the resolved role. No operator attestation is required — nothing here is an aesthetic or value judgment, so a human signature would launder a missing build into a signed pass. INSUFFICIENCY — what would make a PASSING run a FALSE PASS: (1) evidence drawn only from the pure role compute in `@storytree/studio-members` (that `builder` parses, that `resolveAccess` returns it, that the brokered-write predicate orders `admin` over `builder` over `member`) proves the role model, NOT the in-app grant — those assertions already pass today while the grant path is still a 400, so binding this criterion to the existing membership proof command would go green without ever exercising it; (2) a `builder` row seeded by a direct store write, a fixture, or a stubbed backend satisfies 'a builder row exists' while leaving intact the exact out-of-band path ADR-0117 existed to remove — the grant MUST travel the in-app route; (3) exercising the route while stubbing the role validator proves the handler, not the decision, since the validator is precisely what refuses `builder` today.",
  "refs": []
}

{
  "kind": "uat-criterion",
  "id": "wisp-as-story-claim#uat-2",
  "action": "Build one territory's scene (`buildScene`, `packages/forest-world/src/scene.ts`) carrying four claims — one `exploring`, two `waiting` supplied in a known input order, one `work`, plus a fifth claim with NO `grade` field — and assert the PLACEMENT of each resulting body: which drawable family it belongs to, whether it carries an orbit `phase`, its transform nesting, and its radius/offset relative to the territory centroid and the story tree spot. Then assert the same three stages survive into the rendered DOM through the mapper (`packages/app-surface/src/SceneView.tsx`, jsdom). ADR-0212 fixes the vocabulary: POSITION is the stage channel — window shopping beside the island, waiting in a stationary line, work orbiting the whole island.",
  "successConditions": "An `exploring` claim sits on a SMALL LOCAL orbit beside the story tree at `HOVER_ORBIT_R`, carries its own orbit `phase`, and has its rest spot on a PARENT `g` distinct from the rotated node. A `waiting` claim is STATIONARY (no `phase`) and index-placed along the queue line by its position in INPUT order. A `work` claim orbits the WHOLE island on the deliberately wider radius (`radius * 0.72 + 22`), wider than the retired build wisp's, so the two never coincided. A claim with an ABSENT `grade` renders exactly as `work` — the pre-grade back-compat lock (ADR-0200 D2). FALSE-PASS FENCES, and the first is the dangerous one: (a) A SPEC ASSERTING THE `exploring` BODY IS STATIONARY ENCODES A REVERTED RULE. ADR-0200 D7 originally made window shopping 'stationary by construction'; ADR-0212 REVERSED exactly that detail so position alone separates the stages — window shopping now carries its own `phase` and spins on its tight local orbit. A spec written from the story's pre-2026-07-26 prose asserts the reverted rule, goes red against correct code, and invites a regression back into the render. Stationarity is the WAITING stage's property, not the exploring stage's. (b) The THREE-level `g` nesting is load-bearing and must be asserted as nesting, not flattened: an SVG `animateTransform` rotate REPLACES the `transform` attribute of the node it animates, so if the rest spot and the rotation share one `g` the dot sweeps the territory centroid instead of its own rest spot. A spec that only checks 'a hover body exists with a phase' would pass that defect. (c) Queue placement must be asserted as INPUT ORDER, index-placed — a weaker 'the two waiters are at distinct positions' also passes hash-random jitter, which would destroy the queue's whole meaning (waiters are ordered by `claimedAt`). (d) The grade-absent case must be asserted explicitly as a back-compat lock; letting it pass incidentally because the fixture happens to include a `work` claim proves nothing.",
  "evidenceExpectations": "The spec's output showing, per claim, the emitted family name, the presence or absence of an orbit `phase`, the transform chain across all `g` levels (so the rest-spot/rotation split is visible), and the computed offsets — with the exploring, waiting and work radii printed side by side so the position separation is legible as numbers rather than asserted in the abstract. The waiting bodies' input order and their resulting queue indices must both appear. The DOM half should show the composed classes and the orbit durations per stage.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

{
  "kind": "uat-criterion",
  "id": "map-terminal-build#uat-4",
  "action": "In the Electron `_electron` Playwright harness (`apps/desktop/e2e/`), in a NEW spec file — never by extending `session-survival.e2e.mjs`, which asserts exactly ONE live session in both directions and would fail the moment the seed opens a second tab (ADR-0186: a seed opens a FRESH tab). Launch via the shared `harness.mjs` contract: `STORYTREE_DESKTOP_E2E=1` (backend sidecar skipped), Playwright-route `/api/*` to the harness fixtures (`stubApi`), and satisfy the repo gate BEFORE entering the forest by writing `userData/repo-selection.json` pointing at the launch checkout — since `terminal-repo-picker`'s `TerminalRepoGate` renders the gate message instead of the dock until a valid repo exists, so a bare launch never shows a terminal at all. Then walk the REAL seam end-to-end: reach the forest, click a story node with `win.mouse.click` at a coordinate from `findStoryTarget` (never `locator.click()` — the pointer-capture retarget is Electron-specific, see `node-click.e2e.mjs`), wait for `.tree-detail`, click the panel's Build button, and read the resulting session set and screen text through `window.desktopTerminal.list()` / `.snapshot()`.",
  "successConditions": "After the Build click, `desktopTerminal.list()` reports one MORE session than before it (the seed opened a fresh tab, never wrote into the pre-existing one), and the NEW session's `snapshot()` contains the exact string `composeBuildCommand({ unitId, scope })` returns for the clicked unit — today `pnpm storytree story build <id> --real --store pg` — with the unit id being the id of the node actually clicked. The pre-existing session's `snapshot()` is UNCHANGED across the click. The command sits UN-EXECUTED: no shell output, no new prompt line, and no exit status follows it in the snapshot. FALSE-PASS FENCES: (a) The load-bearing hazard is proving un-run BY ABSENCE OF OUTPUT — a seed that never arrived at all also produces no output, so absence alone is worthless. The command TEXT must be positively asserted present in the snapshot AND unaccompanied by execution; a spec that only asserts 'no output' passes trivially against a completely broken bridge. (b) Re-running the jsdom `mbt-*` (BuildSection) or `son-*` (TerminalDock) suites is NOT this leg — those sign the button and the dock over a MOCKED `desktopTerminal`, and a green there says nothing about whether the real IPC bridge and the real `PtySessionManager` carry the command across the process boundary, which is the only thing this leg adds. This leg exists precisely because the TreeView `seed` glue between them is un-asserted by design (ADR-0158). (c) Reading pane text via DOM `textContent` must NOT be used: the dock paints on xterm's WebGL renderer where available, so a DOM read returns empty and the assertion would pass or fail by GPU availability rather than by the behaviour under test — `snapshot()` is the main-held, renderer-independent observable. (d) Asserting a trailing-newline absence on the string passed to the mocked bridge is `son-prefills-without-trailing-newline`'s job, not this leg's; here the observable is the real pty's state.",
  "evidenceExpectations": "The new e2e spec's `node:test` output showing, in order: the session-id set before the Build click, the set after (one new id, the prior id still present), the clicked node's story id, and the new session's `snapshot()` text with the composed command visible and no following output — so the 'landed, targeted the clicked unit, and did not run' claims are each auditable rather than asserted. Evidence that shows only a jsdom vitest run, only DOM row/text reads, or only a screenshot of the dock does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

{
  "kind": "uat-criterion",
  "id": "studio-build#uat-1",
  "action": "Bring the live Cloud SQL store up and start the studio against its live backend (the default), then assert two things from a REAL connection: the data-api startup line, and the `GET /api/health` payload. This is deliberately NOT the shape of the existing `apps/studio/server/healthApi.integration.test.ts`: that suite declares `let deps: HealthDeps` and injects `health: async () => ({ db: 'ok', schema: { code: 2, db: 2 } })` per test, so its `db: 'ok'` is a literal the test supplied — it asserts the payload SHAPE and passes offline against a fake, which structurally cannot witness reachability. The spec must therefore let the real probe run (a real `createPool`/connector path, no injected `HealthDeps`) and must be LIVE-GATED: SKIP when the store is unreachable rather than failing, the `*.live.test.ts` pattern from `packages/library/src/store/test-db.ts` — an environment-dependent red is not evidence about this behaviour. Note this needs a live-test surface `apps/studio` does not have today (`vitest.config.ts`: \"offline by design: no DB, no gcloud … no network\"); see the story's open modeling call 2.",
  "successConditions": "The data-api line logs `library/comments → Cloud SQL Postgres` — naming the live backend, not the json fallback — and `GET /api/health` answers 200 with `store: 'pg'` and `db: 'ok'` where that `db` value was DERIVED by a real probe against the live instance. Asserting the shape alone is insufficient: the existing injected-deps test already asserts exactly that body and is green offline, so a shape assertion is not a reachability assertion. Equally, a run that quietly fell back to the json store is a FALSE PASS for this leg — the `store` field must read `pg` and the log line must name Cloud SQL Postgres, so a store-fallback cannot read as a pass.",
  "evidenceExpectations": "The spec's output showing the captured startup log line verbatim, the health response status and body, and an explicit marker of which branch ran — a SKIP is an honest non-result, never a pass. The evidence must also establish that the probe was real: no `HealthDeps` injection in scope, and a real pool/connector observably used. This leg was reclassified human -> machine on 2026-07-25 (ADR-0209 §8) because a log line and a JSON payload are string comparisons with no judgment in them; needing a reachable Cloud SQL instance makes it expensive and live-gated, which is expense, not an irreducible judgment gap.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

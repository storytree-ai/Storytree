{
  "kind": "uat-criterion",
  "id": "desktop#uat-4",
  "action": "Launch the packaged desktop app through Playwright's `_electron` WITHOUT STORYTREE_DESKTOP_E2E and WITHOUT the harness's /api/* route stubs, so the Electron main actually spawns the tsx backend sidecar (electron/backend-entry.ts) and the static server proxies /api/* to it. This is deliberately NOT the existing offline harness mode: that mode sets `if (!E2E_MODE) backendPort = await startBackend()` to skip the sidecar entirely and stubs every /api call (harness.mjs), so it structurally cannot observe this leg. Wait for the studio origin, then issue a GET from the renderer against a read route the sidecar composes — `/api/tree`, `/api/docs` or `/api/activity` — and capture the HTTP status and the response body. Gate the spec on its live preconditions (a git checkout plus a reachable store) and SKIP when they are absent rather than failing, the live-store-test pattern — an environment-dependent red is not evidence about this behaviour.",
  "successConditions": "The response is the composed organism drivers' real envelope: HTTP 200 with a body of the route's documented shape (e.g. /api/tree returning a TreePayload with a `stories` array). It is specifically NOT `static-server.ts`'s fallback — a 503 whose body is `{\"error\":\"no backend in the desktop shell (Step 1; worker wiring is Step 2)\"}`, the stub the thick-client layer exists to replace (ADR-0113). Asserting 200-not-503 on the status code alone is insufficient: the body must carry real driver-composed content, because a proxy that answered from a fixture would also be 200. Equally, the sidecar must be the thing that answered — a spec that silently fell back to stubs, or that ran in E2E_MODE, is a FALSE PASS for this leg and must be structurally impossible (assert E2E mode is off).",
  "evidenceExpectations": "The spec's node:test output showing the request URL, the status code, and enough of the body to establish it is driver-composed rather than a stub or fixture — plus an explicit assertion that the sidecar was spawned (a non-null backend port, or the main's `[main]` sidecar log line) so a skipped-sidecar run cannot read as a pass. Because the spec is live-gated, the evidence must also record which branch ran: a SKIP is an honest non-result, never a pass. This leg was reclassified human -> machine on 2026-07-25 (ADR-0209 §8) precisely because a 503 stub versus a real envelope is a byte comparison with no judgment in it; the cost of standing up a live sidecar is expense, not an irreducible judgment gap.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

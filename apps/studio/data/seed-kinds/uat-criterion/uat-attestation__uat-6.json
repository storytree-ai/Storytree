{
  "kind": "uat-criterion",
  "id": "uat-attestation#uat-6",
  "action": "Build a story fixture with THREE legs in three different states — one carrying a signed pass verdict and no vouch, one carrying only a vouch and NO verdict, one carrying neither — and render every surface that displays per-test state: `storytree tree <story> --pg`, `storytree witness list <story> --pg`, and the studio's `UatTestCriteriaSection` over the `/api/attestations` payload. Then move ONE log at a time and re-read: add a vouch to the proven leg, and sign a verdict on the vouch-only leg.",
  "successConditions": "CLI tree: each row carries `proven=` (✓ / ✗ / –) derived ONLY from `events.verdict` AND, in a separate trailing column, the vouch mark (`◉ human:<outcome>` / `▣ machine:<outcome>` / `–`) derived ONLY from `events.attestation`; the vouch-only leg reads `proven=–` while carrying a ◉ mark, and the bare leg reads `–` in both. `witness list` carries the `proven=` column and NO vouch column. Studio panel: each row renders exactly ONE right-edge button whose SHAPE is the witness (robot for `machine`, person for `human`) and whose class carries `proven-pass|proven-fail|proven-none` derived from `events.verdict` alone; the vouch-only row is INDISTINGUISHABLE from the bare row (the payload's `human`/`machine` vouch marks reach the client but are never rendered), and no vouch-derived value appears in any class, icon, `title` or `aria-label`. Offline, both the proven and the vouch columns drop silently and the leg list still renders. Moving one log never moves the other column. FALSE-PASS FENCES: (a) The most dangerous false pass is a fixture where the voucht leg ALSO has a verdict — the two columns then agree and a conflation bug is invisible. The discriminating case is a leg voucht and NOT proven: the vouch tier must not lift it. (b) Asserting that the two glyph CHARACTERS differ is insufficient; the claim is that the two columns are derived from DIFFERENT logs, which only the move-one-log re-read proves. (c) Do NOT assert ⚑/⚐ anywhere in the studio panel: an owner UX call REMOVED the vouch from the row (`apps/studio/src/components/TreeView.tsx`), so expecting it is a false RED against correct code — this is the exact drift the leg was restated to remove on 2026-07-26. (d) Do NOT assert `✓/✗/–` characters in the panel either; the panel's proven state is a CSS class and colour on an icon, not a glyph. (e) A rendered-colour assertion is the wrong observable for the panel — assert the class list and `aria-label` the component sets, not a computed hue.",
  "evidenceExpectations": "The CLI `tree` and `witness list` output verbatim for all three legs (both columns visible, plus the offline run where they drop); the panel's rendered row markup per leg — class list, icon element, `title`, `aria-label` — with the vouch-only and bare rows shown side by side to prove they are identical; and the before/after pair for each move-one-log re-read. This leg is the MACHINE half of the old fused leg 6, split on 2026-07-26 (ADR-0209 §8): the structural distinctness has compilers, the legibility does not, and that stays on human leg 7.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:splitting-rule"]
}

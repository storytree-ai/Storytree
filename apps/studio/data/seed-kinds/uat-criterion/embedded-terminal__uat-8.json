{
  "kind": "uat-criterion",
  "id": "embedded-terminal#uat-8",
  "action": "Launch the desktop app through Playwright's `_electron` WITHOUT `STORYTREE_DESKTOP_E2E` and WITHOUT the harness's `/api/*` route stubs, so the Electron main actually spawns the backend sidecar and its advisory overlay reads answer from the live store (the `desktop#uat-4` precedent — the offline harness mode structurally cannot observe this leg, since it skips the sidecar and stubs every `/api` call). With the repo gate satisfied, expand the terminal and run, IN THE REAL PTY, the existing observability seam: `storytree noticeboard declare --node embedded-terminal --pg`. Then read three things: the `events.node_claim` row for that session; the app's own `GET /api/activity` payload; and the rendered scene's claim wisps. Gate the whole spec on its live preconditions (a git checkout plus a reachable store) and SKIP when they are absent rather than failing — an environment-dependent red is not evidence about this behaviour.",
  "successConditions": "The declare, typed into the embedded terminal like any other command, writes a `work`-grade `events.node_claim` row keyed to that session, `/api/activity` reports it as a claim (`claimsToActivity`), and the scene carries EXACTLY ONE claim-wisp for that session id (ADR-0212: wisp count encodes sessions, the build wisp folded into the claim body). The load-bearing claim is ADR-0174's premise — NO new observer code: nothing in the path may be terminal-specific, and the spec must not introduce a shim to make the claim appear. Two false passes to exclude: a run that declared from the HOST shell rather than through the embedded pty proves nothing about the terminal; and a run in E2E/stub mode would read a fixture, so assert the sidecar was really spawned and the store really answered. Asserting the claim row alone is also insufficient — the leg's end-to-end claim is that the MAP paints a wisp from it. The wisp's LOOK (colour, orbit, distinctness from a proof bloom) is `wisp-as-story-claim`'s own leg and is not re-judged here.",
  "evidenceExpectations": "The spec's node:test output showing which branch ran (a SKIP is an honest non-result, never a pass), the declare's exit status as observed in the pty, the claim row (unit id, session id, grade), the `/api/activity` payload, and the single claim-wisp keyed to that session. This leg was reclassified human -> machine on 2026-07-26 (ADR-0209 §8): its prior justification was purely cost — \"needs a live store + the running map + a real session\" — and live, expensive and unharnessed are never judgment gaps. Its prose was also CORRECTED in the same pass: it used to credit the `SessionStart` presence hook with declaring, but ADR-0200 D3 retired presence rows and made the hook's `start` mode pure and offline (it prints the anchor nudge and touches no store), so a machine spec written from the old text would have asserted a write that correctly no longer happens and gone red against correct code.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

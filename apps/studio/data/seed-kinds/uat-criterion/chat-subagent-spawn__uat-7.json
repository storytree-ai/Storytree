{
  "kind": "uat-criterion",
  "id": "chat-subagent-spawn#uat-7",
  "action": "Assemble a `runHeadlessOrchestrator` session twice — once WITH spawn deps and once without — and inspect what was constructed, not what it does at runtime. Capture the SDK `Options` the session is built with (`tools`, `allowedTools`, `mcpServers`), enumerate every tool definition on every mounted in-process MCP server, and inspect `buildSpawnTools`' handlers. Separately, drive `orchestrate()` a second time while a composition is already in flight. No SDK call, no spend, no live surface: every condition below is a shape or an absence over constructed values.",
  "successConditions": "The session's own `tools` is `[]` and `allowedTools` names ONLY `mcp__<server>__<tool>` entries, so no `Write`, `Edit`, or `Bash` is reachable at any point. BOTH spawn tools are claim-gated by CONSTRUCTION — `buildSpawnTools` wraps every handler in `claimGatedSpawn`, so no constructor path can mount an ungated spawn. The spawn server advertises exactly `spawn_story_author` and `spawn_builder`, and neither carries a PR, merge, or land verb. NO tool on ANY mounted server writes an ADR — ADR-authoring, the sole claim-free act, is a deliberately-unshipped follow-on (Open modeling call 1). A second concurrent orchestration is refused with the typed `{ ok: false, refused: true, reason: \"single-session\" }`. With spawn deps absent, no spawn tool is advertised at all. WHY MACHINE (re-adjudicated 2026-07-26, ADR-0209 D8): this leg was tagged `human` because it read as the retrospective companion to the live walk of legs 5-6 — but being ABOUT a live walk is not the same as needing one. Every condition here is a property of a constructed object; none requires spend, taste, or an owner's acceptance. FALSE-PASS FENCES: (a) THE LANDING SCOPE IS LOAD-BEARING AND THE ORIGINAL PROSE WAS STALE. As authored this leg claimed 'accept-to-land stayed the human's ... nothing merged without the ceremony' and 'spawn power only'. ADR-0152 (accepted 2026-07-04, amends ADR-0137) lifted that wall: the SAME session mounts `run_gate` / `open_landing_pr` / `poll_pr_checks` when landing deps are present, and ADR-0173 adds a read-only inspect surface. A spec asserting the session cannot land, or that the session carries spawn tools ONLY, goes RED against correct code. The claim that compiles is narrow: THIS STORY'S SPAWN SURFACE adds no landing verb. (b) Asserting the positive mounts is not enough — the load-bearing claims are ABSENCES (no raw write verb, no ADR verb, no land verb on the spawn server). An absence must be checked by enumerating the whole advertised surface, not by asserting the two expected tools are present. (c) Checking the runtime REFUSAL path of a gated spawn is leg 3's job and cannot substitute: this leg's claim is stronger and structural — that no ungated handler can be constructed in the first place, which a behavioural test over one code path cannot establish. (d) `propose_unit` NO LONGER EXISTS (retired by ADR-0155): do not assert it, and do not describe the dep-less session as 'propose-only' — it is orientation-only. (e) Reading the source comments that state these walls is not evidence; the surface must be assembled and enumerated.",
  "evidenceExpectations": "The assembled `Options` printed verbatim for both dep configurations (`tools`, the full `allowedTools` array, the mounted server names), the complete enumerated tool list per server with each tool's name, the constructor-level demonstration that every spawn handler is gate-wrapped, and the typed `single-session` refusal object. For each absence claim, the full enumerated surface it was checked against — an absence asserted without the enumeration it was derived from does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:pair-the-fence-with-the-affordance"]
}

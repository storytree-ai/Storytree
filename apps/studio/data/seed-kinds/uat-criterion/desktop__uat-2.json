{
  "kind": "uat-criterion",
  "id": "desktop#uat-2",
  "action": "In the Electron `_electron` Playwright harness (apps/desktop/e2e/, following the session-survival.e2e.mjs precedent), launch the desktop app offline with STORYTREE_DESKTOP_E2E=1 and the /api/* stubs in force, then interrogate the REAL context-isolated bridge the preload exposed. Read the shape of `window.desktopAuth` from the renderer (its own enumerable keys, and the typeof each). Call `window.desktopAuth.status(kind)` for each of the two live credential kinds (`oauth`, `api-key`) and record what resolves. Open the desktop-only Credentials panel and read each password input's `value`. Finally dump every renderer-reachable storage surface: `localStorage`, `sessionStorage`, and the resolved value of every desktopAuth method call made during the walk. DO NOT store a real secret: `NapiKeychain.set` (apps/desktop/src/keychain/napi-adapter.ts) writes to the real OS keychain and has NO offline fallback, so a store-cycle assertion belongs to human leg desktop#uat-3, not here. Reading `status` IS safe — the adapter's `get` normalises a missing or unavailable entry to `null` rather than throwing.",
  "successConditions": "`window.desktopAuth` exposes EXACTLY `status`, `store`, `signOut` — three functions and NO getter, no `read`, no `reveal`, no property carrying a value. `status(kind)` resolves a BOOLEAN (`typeof === 'boolean'`), never a string and never an object wrapping one — the boolean-only status contract of ADR-0109 Decision 4, asserted here against the real contextBridge rather than a jsdom fake. Every Credentials panel password input has `value === ''` — the panel never pre-fills from a stored credential. And no raw credential byte is reachable from the renderer: `localStorage` and `sessionStorage` contain no entry holding a token-shaped value, and no desktopAuth call resolved one. The load-bearing distinction from credential-broker's contracts 5-9: those prove the PANEL's geometry and the broker's logic over an injected fake at the jsdom/node tier; this leg proves that the surface the REAL preload actually handed the real renderer has no read-back affordance at all. A pass recorded without reading the real bridge (e.g. asserting against an injected double) does not discharge this leg.",
  "evidenceExpectations": "The e2e spec's node:test output showing the enumerated desktopAuth key set, the `typeof` of each `status()` result, the empty input values, and the storage dump. The run should be the standing CI one where possible: the e2e-desktop workflow already triggers on apps/desktop/** for PRs to main, so this leg's proof is a path-triggered standing check rather than a local-only run. Evidence that only re-states credential-broker's jsdom panel assertions is NOT evidence for this leg — the observable must come from the real Electron renderer. Note that this leg deliberately proves a NEGATIVE (no read-back path exists); the spec must therefore enumerate the bridge surface rather than merely confirm that one known-bad getter is absent.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

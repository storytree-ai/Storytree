{
  "kind": "uat-criterion",
  "id": "wisp-as-story-claim#uat-1",
  "action": "Drive the REAL studio surface fold end to end and walk the scene tree it returns: `buildWorld` → `worldToScene(world, cells, now, builds, claimsByStory, departuresByStory, …)` → `buildScene` (`apps/studio/src/components/TreeView.tsx` + `packages/forest-world/src/scene.ts`), seeded with a claim set that deliberately mixes the cases — several sessions' claims across distinct STORY units, at least one claim whose `unitId` is a CAPABILITY of a story in the world, one claim on an id present in no world at all, one claim whose `heartbeatAt` is past `CLAIM_STALE_RECLAIM_MS`, and one story carrying an `exploring` + a `waiting` + a `work` claim simultaneously. The harness is `apps/studio`'s vitest suite in the default `node` environment; the working analogue is `apps/studio/src/components/sceneAdapter.test.ts`, which already drives this exact path — but EVERY call site there passes `NO_BUILDS` and omits `claims`/`departures` entirely, so the studio-side claim fold (`claimsByStory`, `foldBuildOntoClaims`, `claimWispTitle`, and `territoryToScene`'s `claims:` pass-through) has no coverage at all today. This leg's spec is that missing case.",
  "successConditions": "Every LIVE claim yields exactly ONE body in the scene tree. A claim whose `unitId` is a CAPABILITY is RE-ANCHORED onto its owning story's territory and drawn there (`claimsByStory`, `TreeView.tsx`) — it is not dropped. A claim on an id in neither the story set nor the capability→story map is dropped. A claim past `CLAIM_STALE_RECLAIM_MS` yields no body. And at most ONE whole-island orbit — one `claim-wisp` family body — exists per story territory regardless of how many claims that story carries, because the `work` claim is an exclusive mutex (ADR-0200 D2) and wisp COUNT encodes SESSIONS (ADR-0212); `hover-wisp` and `queue-wisp` bodies may legitimately coexist alongside it. FALSE-PASS FENCES: (a) re-running `apps/studio/server/inFlightActivity.test.ts` is NOT this leg — that is the SERVER row fold, its one-per-unit property rests on `unit_id` being the row primary key rather than on any dedupe in the code, and its existing spec asserts only the stale-drop and the field mapping, never 'never two for one unit'; a green there says nothing about what the surface draws. (b) Asserting 'no wisp appears on the capability node' passes TRIVIALLY and proves nothing: `ScenePlantInput` (the capability drawable) has no `claims` field at all, so a capability wisp is a TYPE-level impossibility, not a tested behaviour — the assertion that carries weight is the claim ARRIVING on the owning story's territory with the right owner id. (c) A spec asserting a capability claim is DROPPED encodes the wrong behaviour and would go red against correct code. (d) 'Exactly one wisp per claimed story' must not be asserted as a raw body count: three grades on one story is three legitimate bodies. The invariant is at most one body of the ISLAND-ORBIT family.",
  "evidenceExpectations": "The spec's output listing, per story territory, the scene-node families present and their counts — so the coexistence of hover/queue bodies with a single island orbit is visible rather than argued — plus the resolved owner story id for the capability-anchored claim, the absence of any body for the unknown-unit claim and for the stale claim, and the total body count against the input claim count. The seeded claim set must be visible in the evidence so the mixed cases are auditable.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

{
  "kind": "uat-criterion",
  "id": "website-experience#uat-1",
  "action": "In a headless browser against the published site or its preview build, load the experience entry with JS on and motion allowed (so the storm arms), assert the AT-REST surface, then dispatch exactly ONE send gesture and drive NOTHING further — no clicks, no keys, no scrolling — while sampling the DOM and the audio state until the plan reaches its peak. The spec belongs in the `storytree-web` repo's own Playwright suite, alongside the existing storm behaviour checks; the storm's plan is SEEDED (`STORM_SEED`, a mulberry32 PRNG, no `Math.random`), so the choreography is deterministic and its thresholds are exact rather than flaky.",
  "successConditions": "At rest: exactly ONE terminal, offering suggested chips and a prompt line whose leading suggestion is \"build me a shopping website\" (the prompt reused across both acts, ADR-0148), with no audio running. On the send gesture: an audio context transitions to running INSIDE that gesture (silent before it — the unlock is gesture-scoped, not load-scoped). Then, with zero further input: the terminal count rises to the seeded plan's ~10-12 hard cap, each new terminal created BY the page (never by a visitor-opened window); every terminal ends parked on an unanswerable demand, the boot terminal parking LAST; and the HUD's `AGENTS: n` value rises monotonically to that peak. FALSE-PASS FENCES, each load-bearing: (a) asserting the peak COUNT alone passes a storm that required the visitor to keep clicking — the spec must prove NO input event was dispatched between the send and the peak, which is the actual claim; (b) asserting an audio element EXISTS is not the unlock — the running transition must be observed inside the gesture, since audio starting at page load would violate the design and still satisfy a mere existence check; (c) asserting the HUD's final value is not \"rising\" — a HUD that jumps straight to the peak passes a final-value check and fails the claim; (d) asserting terminals exist is not diegetic spawn — the spec must show the page created them. This leg is the MECHANICS only: whether the result actually overwhelms, reads retro, or reads plausible-but-opaque is story leg 3's human verdict and must never be asserted here.",
  "evidenceExpectations": "The web-repo Playwright run's output naming each asserted threshold with its observed value (at-rest terminal count, the leading chip's text, the audio-context state before/inside the gesture, the peak terminal count against the seeded cap, the per-terminal parked demands, the HUD's value series), plus the explicit record that the input-event count between send and peak was zero. Because the parent prove-it-gate cannot run inside the web repo, the run is NOT parent-signed today — the route by which this verdict reaches the proof spine is the story's open modeling call 6, and an unrouted green run is evidence, not a discharge.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

{
  "kind": "uat-criterion",
  "id": "embedded-terminal#uat-6",
  "action": "In the Electron `_electron` harness (repo gate pre-satisfied, REAL node-pty, REAL xterm in the real renderer), exercise four terminal mechanics in one live session and read each back through `window.desktopTerminal.snapshot(sessionId)`. SCROLLBACK: emit more lines than the visible viewport, then confirm the earlier lines are still retained in the session's screen model (the dock constructs xterm at scrollback 5000, aligned with the main-held headless model, ADR-0190). REFLOW: drag the dock's resize edge to a materially different geometry and confirm the session comes back at the NEW cols/rows with wrapped content rewrapped rather than truncated or duplicated. KEYS: start a long-running command and send Ctrl+C, confirming the real shell interrupts it and returns a prompt. DOCK LIFECYCLE: collapse and re-expand the dock and confirm `list()` reports the SAME session id with its scrollback intact.",
  "successConditions": "All four hold against a REAL renderer and a REAL shell. The observable is the main-held serialized screen state, never renderer DOM text (WebGL vs DOM renderer makes DOM text GPU-dependent). Restating capability `terminal-dock-panel`'s mocked-xterm contracts is insufficient and is the specific false-pass to avoid: `tdp-resizes-with-the-dock`, `tdp-ctrl-c-copies-selection-ctrl-v-pastes`, `tdp-toggles-visibility-keeping-terminal-mounted` and `tdp-constructs-with-aligned-scrollback` already prove the WIRING (fit invoked, dims forwarded, handler return values, constructor options) over a fake Terminal — a mock can never exhibit reflow or scrollback retention, because it has no screen model. A spec that asserts `bridge.resize` was called with new dims has proven nothing this leg claims. Ctrl+C must be observed as a real SHELL interrupt (the running command stops), not as the copy-on-selection branch of the key handler. If the main-held headless model turns out not to rewrap historical rows on resize, NARROW this leg's reflow clause to the observable the seam genuinely offers and say so — re-tagging the leg `human` because the observable is awkward is forbidden.",
  "evidenceExpectations": "The e2e spec's node:test output showing, per mechanic, the pre-state and post-state read from `snapshot()` — the retained earlier lines, the new cols/rows with the rewrapped content, the interrupted command's prompt return, and the identical session id across the collapse/expand cycle. This leg is the MACHINE half of the old leg 5, split on 2026-07-26 (ADR-0209 §8): the old leg fused these mechanics with the terminal's FEEL, and the fused leg was tagged human because the feel is irreducible. The mechanics all have compilers; only the feel does not, and it stays on human leg 7.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:splitting-rule"]
}

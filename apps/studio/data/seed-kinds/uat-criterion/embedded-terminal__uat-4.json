{
  "kind": "uat-criterion",
  "id": "embedded-terminal#uat-4",
  "action": "In the Electron `_electron` harness with the repo gate pre-satisfied, expand the terminal and wait for `window.desktopTerminal.list()` to report a live session — a REAL node-pty spawning a real shell in the selected checkout, no fake and no mock anywhere in the path. Then drive TWO observations. LINE MODE: focus `.terminal-dock-body:not([hidden])`, type a probe command, press Enter, and poll `window.desktopTerminal.snapshot(sessionId)` until the probe output appears (`session-survival.e2e.mjs` already performs exactly this walk with `echo survival-probe`). INTERACTIVE MODE (net-new): run a full-screen interactive program in the same session — one that switches to the alternate screen buffer and redraws on keypress — send a keystroke, and observe the screen state CHANGE in response, then quit it and observe the shell prompt restored.",
  "successConditions": "The probe's output is present in the main-held serialized screen state, and the interactive program's screen changes in response to a keystroke and is torn down cleanly on quit. Read every observable through `desktopTerminal.snapshot()` — the main-held `@xterm/headless` model (ADR-0190) — NEVER through renderer DOM text: the dock renders on xterm's WebGL renderer where available and falls back to the DOM renderer where not (CI runs `--disable-gpu`), so a DOM-text read would pass or fail by GPU availability rather than by the behaviour under test. A line-echo assertion ALONE is insufficient: `echo` round-tripping proves a pipe, not a pty. The claim that predicts real Claude Code works is that a full-screen interactive program driving the alternate screen buffer behaves correctly, which a non-tty pipe cannot do. Equally insufficient: any run against the fake `PtyPort` (that is capability `pty-session-manager`'s signed verdict) or against the mocked xterm/mocked bridge (capability `terminal-dock-panel`'s) — this leg exists precisely to observe the REAL native module the capabilities deliberately exclude.",
  "evidenceExpectations": "The e2e spec's node:test output showing a non-empty `list()`, the probe text recovered from `snapshot()`, and the interactive program's before/after screen states around the keystroke. The evidence must establish the pty was REAL — a session id from the real bridge, and a shell cwd inside the selected checkout — so a stubbed-bridge run cannot read as a pass. This leg is the machine HALF of the old leg 4, split on 2026-07-26 (ADR-0209 §8): the paid Claude Code session it used to fuse with is now the separate human leg 5, kept human under the owner's honesty wall (real metered spend the proof spine must never burn unattended), while the pty faithfulness it always depended on is proven here.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:splitting-rule"]
}

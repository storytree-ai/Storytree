{
  "kind": "uat-criterion",
  "id": "chat-drive-bridge#uat-5",
  "action": "In the desktop app, hold a REAL chat conversation against a real subscription-billed SDK query(), obtain a proposal carrying a machine-actionable unit id, and CLICK the Build affordance to accept it. PRECONDITION, recorded 2026-07-26: this walk is NOT currently performable — ADR-0155 / PR #587 removed the propose_unit tool, the proposedUnitId field, the chat accept route and the ChatPanel Build button. The leg is retained as the authored record of what an operator would have to vouch for; being unwalkable is a fact about the surface, independent of which witness kind is right.",
  "successConditions": "Only the two conditions no compiler can reach, both of which the operator vouches for personally. (1) SPEND: the conversation ran on a genuinely metered subscription query() — a real session was billed, not a scripted queryFn. (2) OUTWARD WRITE: the accepted unit id really drove the worker to a spine-signed verdict persisted to events.verdict, and a NON-DRAFT PR is really open for CI to auto-merge onto the trunk (ADR-0022). The mechanical conditions are deliberately NOT restated here: the dispatch's validate/route/refuse and the coarse-progress fold are chat-drive-bridge#uat-3, and the conditional affordance, the single accepted-id POST, the progress render and the no-free-text-path wall are chat-drive-bridge#uat-4. This leg is human on SPEND and OUTWARD ACTION, never on irreducibility — a basis that would dissolve entirely if the run stopped costing money and stopped writing to the trunk.",
  "evidenceExpectations": "The operator's own attestation, naming: the billed session (cost and turns as the SDK reported them), the signed verdict's id and its events.verdict row, and the opened PR number with its non-draft status. A machine transcript alone is not the evidence — the point of the leg is the operator vouching that the spend and the trunk write were real. INSUFFICIENCY — four ways a PASSING run is a FALSE pass. (a) PROVENANCE OF THE DISPATCH: a signed verdict plus an open PR proves a build ran, NOT that a human's click started it; dispatchAcceptedBuild (packages/drive/src/build-worker.ts:405) is today reached only from spawn-builder.ts:40, the orchestrator's own spawn_builder tool, so an agent-initiated landing produces evidence indistinguishable from this leg's unless the attestation ties the run id back to the operator's click. (b) SCRIPTED SPEND: a run driven by an injected queryFn or a --dry-run walk yields a transcript and an envelope that read the same while nothing was billed and no red-green was observed; the verdict must be spine-signed off a real run, not a scripted one. (c) DRAFT PR: a PR that opens as a draft satisfies 'a PR opened' while defeating the whole outward-action basis, since CI will not auto-merge it. (d) NON-PERSISTED VERDICT: an envelope shown in the panel is not a row in events.verdict — the persisted row is the claim, and since ADR-0144 even a chat-accepted NODE id routes to a persisting --real build, so 'it was only a node build' never excuses an absent row.",
  "refs": []
}

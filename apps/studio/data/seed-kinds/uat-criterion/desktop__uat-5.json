{
  "kind": "uat-criterion",
  "id": "desktop#uat-5",
  "action": "In the same live-gated non-E2E `_electron` launch that discharges desktop#uat-4 (the sidecar really spawned), hold a FAKE credential for the run — a recognisable sentinel value, never a real subscription token — and drive one build/orchestrate driver invocation through the local backend. Observe TWO things. (a) The IN-PROCESS hand-off: the driver invocation receives that sentinel from the broker without any TLS hop — the credential travels as a function argument / process env inside the main-owned sidecar, not over a network request to a remote credential service. (b) The RENDERER ISOLATION: sweep every renderer-reachable byte stream produced during the invocation — each /api/* response body the renderer received, the SSE frames of any streamed route, localStorage, sessionStorage, and every desktopAuth resolution — for the sentinel. The sentinel is what makes this leg machine-observable at all: a real token could not be searched for safely, and no real spend is incurred.",
  "successConditions": "The sentinel IS present at the driver invocation (the in-process hand-off happened) and is ABSENT from every renderer-reachable byte stream swept in (b). Both halves are load-bearing: presence alone would not prove isolation, and absence alone would not distinguish a working hand-off from a credential that never arrived — so a spec asserting only one half does not discharge this leg. 'No TLS hop' is established by the topology the assertion runs in (broker in the main process, sidecar spawned by it, credential passed in-process) plus the absence of any outbound credential request during the invocation — NOT by inspecting a comment. local-credential-wiring's contract test asserts this same hand-off and isolation over the component seam; this leg adds only the claim that the REAL Electron main actually wired it, so evidence that merely re-runs the component test is not evidence here.",
  "evidenceExpectations": "The spec's node:test output showing the sentinel observed at the driver invocation, and the negative sweep enumerating each renderer-reachable surface it was searched for in (naming the surfaces, so the negative is auditable rather than a bare `assert(!found)`). As with desktop#uat-4 the spec is live-gated and must SKIP honestly when the git checkout or the store is unavailable; a SKIP is never a pass. The credential used MUST be the fake sentinel — evidence produced with a real subscription token is inadmissible for this leg regardless of outcome, since it would put a live credential into captured proof output.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

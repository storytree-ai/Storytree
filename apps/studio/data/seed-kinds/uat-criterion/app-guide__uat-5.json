{
  "kind": "uat-criterion",
  "id": "app-guide#uat-5",
  "action": "Drive the chat sidecar's reset dispatcher in-process, against the REAL drive composition guard. Mount the reset dispatcher behind a wrapper that 404s when it returns `false`, over a real `node:http` server reached by loopback `fetch`. Put the drive composition single-session guard (`compositionInFlight`, a module-level `let` at `packages/drive/src/orchestrate.ts:142`) into the in-flight state through the real `orchestrate()` path, confirm a second composition is refused while it is stuck, then `POST /api/chat/reset` and re-attempt. Separately, exercise the dispatcher with other paths and methods. No Electron, no live SDK, no DB, no spend, no UI — every condition below is decidable in one offline process.",
  "successConditions": "With the guard in-flight, a second composition is refused (the typed `single-session` refusal). `POST /api/chat/reset` returns `200`. The guard is thereafter observably cleared and a subsequent composition is ADMITTED where it was previously refused — the wedge recovery itself, proven against the real drive export rather than asserted. The dispatcher returns `false` for every other path and method (a `GET /api/chat/reset`, a `POST /api/chat`, a `POST /api/x`), so the sibling dispatchers and the existing 404 still fire. WHY MACHINE (re-adjudicated human -> machine 2026-07-26, ADR-0209 D8): the leg was `human` on the words `backend-wedge recovery is witnessed on the live surface`. Live-versus-in-process is FIDELITY, not a judgment gap — `human-witness-is-a-judgment-gap-not-cost`. There is nothing here for a person to judge: a boolean guard is either cleared or it is not, a route either returns 200 or it does not, a session is either admitted or refused. `backend-chat-reset-route` already authors these very conditions as `bcr-clears-the-composition-guard` and `bcr-falls-through-not-404s`; a success condition that is already someone's written assertion is not an irreducible human verdict. FALSE-PASS FENCES: (a) THE MOST LIKELY FALSE PASS IS A ROUTE THAT ALWAYS RETURNS 200. Asserting the status code alone proves a handler exists, not that recovery happened. The load-bearing observation is the STATE CHANGE across the reset — refused before, admitted after — and it must be observed on both sides, not inferred from one. (b) STUBBING THE DRIVE GUARD VOIDS THIS LEG. The capability's whole reason to be an integration test rather than a unit is that it drives the REAL `resetCompositionGuard` export across the package boundary; a mocked guard proves only that a mock can be called. (c) The fall-through claim is an ABSENCE and must be checked by exercising non-matching paths and methods, not by asserting the matching one works — a catch-all dispatcher passes the positive assertion and silently swallows every sibling route. (d) NOTHING IN THIS LEG EXISTS AT HEAD: `apps/desktop/src/backend/chat-reset-route.{ts,test.ts}` are absent and `orchestrate.ts` exports no guard reset. That absence IS the authored red — it is not a dead binding to a deleted file, and a spec must not read it as one. (e) The leg carries NO `(proof-gate: ...)` binding, on purpose: `app-guide#gate-1` observes the studio vitest suite, which never touches the sidecar or the drive guard, and minting an observe gate over source absent at HEAD is the rubber-stamp ADR-0097 section 2 bans. `resolveWitness` therefore reports this leg `refused` — an open binding gap to close when the capability is built, not a defect to paper over. (f) Mounting the dispatcher in the Electron sidecar (`backend-entry.ts`) is un-CI-asserted composition glue, outside this proof's scope; it is NOT an operator attestation, and this leg must not be stretched to cover it.",
  "evidenceExpectations": "The observed refusal BEFORE the reset and the observed admission AFTER it, printed as the actual returned values rather than summarised — the state change is the proof, and one side of it is not evidence. The `200` response for `POST /api/chat/reset`. The full set of non-matching path/method probes with the dispatcher's `false` return for each, so the fall-through is derived from an enumeration rather than an assertion. Confirmation that the real `@storytree/drive` guard export was driven, naming it, with no stub or spy standing in its place. A run in which a mocked guard, or a status-code assertion unaccompanied by the before/after state, is the only evidence does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:pair-the-fence-with-the-affordance"]
}

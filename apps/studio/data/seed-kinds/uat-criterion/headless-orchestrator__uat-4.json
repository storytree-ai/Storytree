{
  "kind": "uat-criterion",
  "id": "headless-orchestrator#uat-4",
  "action": "Run this story's Phase-1 entry LIVE and judge what it proposed. `pnpm storytree orchestrate \"<a real intent>\"` (packages/cli/src/commands.ts:2231-2276) with NO injected queryFn, so `runHeadlessOrchestrator` falls through to the real subscription SDK `query()`. The command already injects the real orientation runner — `run([...toolArgv], { ...deps, writable: false })` — so the session reads the real tree and the real library; bring the DB up first if the live notice board is to be part of the orientation. Then READ the proposal it returned, alongside the turn/tool trail the session produced, and decide whether the proposed unit is coherent.",
  "successConditions": "The owner's verdict that the returned proposal is a COHERENT unit proposal — one that follows from what the agent actually read on the three surfaces, not plausible-looking prose it could have produced from the system prompt alone. WHY HUMAN, on TWO bases stated apart because they dissolve differently: (a) NO-COMPILER, the load-bearing one — 'is this a coherent unit proposal' has no oracle; nothing decides it but the owner, and no model-judged witness kind is reachable in this corpus today. This basis dissolves under nothing: no harness anyone could write would settle it. (b) REAL METERED SUBSCRIPTION SPEND — a live query() is subscription-billed and an agent may not burn it unattended (the `agent`#uat-5 / `studio-build`#uat-9 pattern; `agent`#uat-5 is a genuinely signed operator attestation, events.attestation seq 7, 2026-06-26). Basis (b) WOULD dissolve if the spend went away; (a) would not, which is why (a) is the one the leg rests on. FALSE-PASS FENCES: (a) A RETURNED PROPOSAL PLUS A NONZERO COST PROVES A SESSION RAN AND WAS BILLED, NOT THAT THE AGENT ORIENTED. A capable model can emit a fluent, entirely plausible unit proposal from the session-orchestrator system prompt alone, having called no read tool at all — and the CLI prints the proposal and the SDK-reported cost either way. The load-bearing observation is the TOOL TRAIL: real `mcp__orientation__*` calls that returned real envelope bodies, and a proposal whose content is checkable against what those envelopes actually said. (b) COHERENCE IS NOT 'IT PRODUCED TEXT'. A proposal naming a story that does not exist, or restating the intent back, is a fluent failure; the verdict is about whether the unit is a real next unit for this tree. (c) DO NOT DISCHARGE THIS LEG BY CONFIRMING THE MECHANICS. That the tools were wired, that the rendered prompt was the real session-orchestrator, that nothing was written or built — all of that is compiled by legs 2, 3 and 5, and confirming those and calling this leg done grades the PARTS while skipping the WHOLE. (d) THE ORIENTATION RUNNER MUST BE THE REAL ONE. With no runner injected, `packages/agent/src/headless-orchestrator.ts:252-253` advertises NO orientation tools; a run in that shape can only ever be a prompt-only answer, and it must not be offered as evidence of orientation. (e) THIS LEG CARRIES NO PROOF-GATE BINDING and could not honestly carry one: this story declares no reliability gates at all, and a human leg binds to none in any case.",
  "evidenceExpectations": "The intent as issued, verbatim. The session's tool trail — which orientation tools were called, with which drill-down args, and enough of each returned envelope body to show it was the real surface and not an empty or refused read. The full proposal text as returned, not a summary of it, so the coherence verdict is made against what the agent actually said. The turn count and SDK-reported cost line the command prints, as the record that this was a real billed run. The owner's own words on the verdict. A run whose only evidence is a proposal string and a cost figure, with no tool trail, does NOT discharge this leg — it is exactly the false pass fence (a) names.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

{
  "kind": "uat-criterion",
  "id": "spawn-visibility#uat-7",
  "action": "Drive the composed overlay-to-map path with a cold-start-shaped claims read. Stand up the advisory reader (`apps/desktop/src/backend/advisory.ts:46`, shared default 4s at :47) with an injected `inFlightClaims` fn that resolves a just-taken claim SLOWER than 4s but inside the softened per-read budget the fix grants it (`timeoutMs` override at :28/:58 and/or `retryOnce` at :31/:83; the call site opts into both at `apps/desktop/electron/backend-entry.ts:489-491`). Let the first overlay poll after the spawn land, and read the rendered map's wisp set for that claim's unit. NO HARNESS FOR THIS EXISTS TODAY — this is the spec for one.",
  "successConditions": "The first poll after the spawn carries the just-taken claim (not null), and the rendered map contains a wisp bound to that claim's unit — the claim is not dropped, which is the regression the cold-start fix exists to prevent. FALSE-PASS FENCES: (a) THE PRIMARY ONE — asserting that the advisory read RETURNED the claim is leg 2, not this leg, and a run that stops there is a false pass. Leg 2 already signs the read surviving the softened budget over an injected slow fn, bound to `spawn-visibility#gate-2`. The only thing leg 7 adds is that the returned claim reaches a RENDERED wisp. Assert on the rendered wisp set. (b) An injected fn that resolves UNDER 4s never exercises the softened budget at all and passes against the pre-fix code — the delay must genuinely exceed the shared 4s, and the test must state the tick it used. (c) The complement must hold too: a run with the softened budget REMOVED must drop the claim and render no wisp. Without that arm the test cannot distinguish the fix from the render simply working. (d) Do not raise the shared 4s to make the test pass — that is the exact over-build the story's open modeling call 3 fences, and it risks hanging `/api/tree`; only the claims read gets the softer budget, and the other four overlay reads (latest-verdicts, verdict-events, in-flight-builds, session-claims) must still null at 4s. (e) A genuinely failing or absent claims read must still return null and never throw (the ADR-0033 advisory contract); a harness that makes this leg pass by removing null-on-failure has broken a wall to prove a leg. (f) The wisp's COLOUR, role-tint, orbit and look are `wisp-as-story-claim`'s appearance UAT, not this leg — assert PRESENCE only. (g) 'It used a real cold-started DB rather than an injected slow fn' is a fidelity argument, not evidence of a different KIND of witness; it does not upgrade or discharge anything here.",
  "evidenceExpectations": "The test run showing the injected delay actually used, the poll's claim payload, the rendered wisp set with the claim's unit present, AND the negative arm (softened budget removed -> claim dropped, no wisp), so the fix is what the green is attributable to. Because no harness exists, the honest current evidence state is: none. This leg carries no `proof-gate:` binding — `spawn-visibility#gate-2` covers the advisory read, not the rendered wisp — so per ADR-0209 section 6 it stands UNSTAMPED, and the `machine` tag must not be read as a claim that the proof exists.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

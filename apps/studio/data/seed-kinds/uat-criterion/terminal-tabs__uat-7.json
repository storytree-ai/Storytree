{
  "kind": "uat-criterion",
  "id": "terminal-tabs#uat-7",
  "action": "In the same Electron `_electron` harness and the same repo-gate-satisfied launch as `terminal-tabs#uat-6`, drive the load-bearing ADR-0186 wall over REAL ptys. Expand the dock, let session 1 spawn, and type into it WITHOUT submitting — half-typed, un-executed input at the prompt is the exact state ADR-0186 says a Build must never corrupt. Capture `desktopTerminal.snapshot(session1)` as the BEFORE screen. Then open the fixture's `proposed` story panel (`harness.mjs`'s `TREE_FIXTURE` already carries `gamma-flow`, whose panel lights a real Build button) and click Build. No `/api/build` stub is needed: with `window.desktopTerminal` present and `onSeedTerminal` wired, `BuildSection` SEEDS the terminal with the composed command INSTEAD of POSTing — the seed replaces the in-app dispatch, never both. Capture the new session's snapshot and re-capture session 1's.",
  "successConditions": "A SECOND session exists after the click (`desktopTerminal.list()` goes 1 → 2) whose sessionId differs from session 1's; the new session's `snapshot` contains the composed `storytree … build … --real --store pg` text sitting at a prompt with NO trailing newline and NO evidence of execution (no command output, no new prompt line beneath it); the panel's active row is the new one; and session 1's AFTER snapshot is IDENTICAL to its BEFORE snapshot — the typed, un-submitted input still there, no injected command text, nothing appended. No `/api/build` request was issued. FALSE-PASS FENCES: (a) The most dangerous false pass is asserting only that the new tab got the command. The load-bearing claim is the NEGATIVE one — session 1 received nothing — and it must be proven by snapshot equality against a session that had REAL un-submitted input, not against an idle blank pane where 'unchanged' is vacuously true. (b) Re-running the jsdom `son-seed-never-touches-active-session` is NOT this leg: it proves the same wall over a MOCKED bridge by asserting `bridge.write` was not called with the active id. That cannot catch a corruption that reaches the pty by any other route (a stray IPC relay, the single-consumer `onData` callback being re-pointed, a main-side broadcast), which is precisely what a real-pty walk can. (c) Session 1 must NOT be running Claude Code here — a paid subscription session is leg 4's irreducible scope, and this leg must stay free to run unattended in CI. Consequently this leg does NOT discharge leg 4: a plain shell's screen being unchanged does not prove Claude Code's TUI is undisturbed (it may repaint or scroll on focus and activation changes), which is exactly why leg 4 stays human. (d) Pressing Enter is OUT of scope and must never appear in this spec: the seeded command opens a billed, outward-facing auto-merging PR (ADR-0136).",
  "evidenceExpectations": "The spec's `node:test` output with both of session 1's snapshots printed (or their equality asserted on the full captured strings, not a substring), the new session's snapshot showing the composed command and its terminating character, the two sessionIds, and an assertion or route log confirming no `/api/build` request was made. Evidence showing only the new tab's pre-fill does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

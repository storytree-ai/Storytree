{
  "kind": "uat-criterion",
  "id": "wisp-as-story-claim#uat-5",
  "action": "Two halves, and only the second is open. (i) The MAPPING and the STAMP: exercise the real `subagentColourState` (`packages/drive/src/subagent-colour.ts`) over every subagent role and every claim intent, its browser-side mirror `claimColourState` (`apps/studio/src/lib/claimColour.ts` — a deliberate duplicate because `apps/studio/src` may not import `@storytree/drive`, enforced by `modelPathBoundary.test.ts`), and `phaseActivityWriter` (`packages/drive/src/phase-activity.ts`) stamping the resolved token onto the `building` doc it appends. (ii) The SHIFT: drive ONE claimed story through the three phases of real work — story-author authoring, the red→green leaf proving, glue supplementing — and capture the colour state the SURFACE actually reads at each phase, from the intent/role a real producer wrote, not from a value the spec supplied.",
  "successConditions": "Half (i): each role and each intent resolves to exactly one of three mutually distinct tokens, none of which is a green/bloom token (the ADR-0138 §5 wall in the colour axis). Half (ii): the token the surface reads CHANGES across the three phases — three phases, three different colours, on one claim body. INSUFFICIENCY — WHY A PASSING RUN CAN BE A FALSE PASS: re-running `packages/drive/src/subagent-colour.test.ts`, `apps/studio/src/lib/claimColour.test.ts`, or `packages/drive/src/phase-activity.test.ts` discharges half (i) only; all three are green TODAY and none of them observes a shift, because each supplies the role itself. A spec that constructs its own three intents and asserts three colours has proved the mapping a second time, not the leg. PRODUCER GAP AT HEAD — this leg CANNOT pass today and the leg text says so: `subagentRole` is assigned by no production call site in `packages/**`, only by its own tests, so `colourState` is absent from real build activity; and the claim wisp colours from the CLAIM's `intent` (`claimColourState(c.intent)` in `TreeView.tsx`), whose live producers write only `\"orchestrate\"` (`packages/drive/src/noticeboard.ts`, `packages/agent/src/spawn-tool-surface.ts`) → supplementing, free prose on exploring claims (`packages/drive/src/noticeboard-claims.ts`, `packages/cli/src/worktree-create.ts`) → default → supplementing, `intent: mode` (`packages/drive/src/node-build.ts`) → `\"real\"` → proving and `\"live-smoke\"` → supplementing, and `intent: story:<mode>` (`packages/drive/src/story-build.ts`) → matching no case → supplementing. Only the literal intent `\"edit\"` yields `authoring`, and nothing in `packages/**` writes it. So TWO of three states are reachable from real data and the third is not produced at all (story open modeling call 3). Additionally the studio mirror's `default: return 'supplementing'` means an UNRECOGNISED intent renders as a legitimate state rather than failing loudly — a spec must therefore assert the mapping from the ACTUAL producer strings above, not from the three role words, or a producer that drifts to an unknown intent silently keeps passing as supplementing.",
  "evidenceExpectations": "For half (i), the resolved token for every role and every producer-written intent string, printed as a table, plus the explicit non-green assertion. For half (ii), the three captured surface-side colour states in sequence together with the intent/role string each was derived from and the producer that wrote it — the producer's identity is the evidence, because a spec that supplied the intent itself has proved nothing about the shift. If the third state is unreachable at the time of the run, the evidence must record that as a RED against the producer gap rather than being narrowed to two states to make the run pass.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

{
  "kind": "uat-criterion",
  "id": "headless-orchestrator#uat-5",
  "action": "Inspect what this story's Phase-1 entry actually constructs, offline and in-process — no live SDK, no spend, no DB, no UI. Drive the terminal `orchestrate` arm (packages/cli/src/commands.ts:2231-2276) with an injected scripted queryFn that CAPTURES the SDK `Options` object it is handed, and read three things off it: the `tools` array, the `allowedTools` list, and which MCP servers were mounted. Separately, hold one composition in flight (the deterministic blocking-queryFn pattern `packages/drive/src/orchestrate-single-session.test.ts` already uses) and await a second `orchestrate()` while it is stuck. Separately again, route a write/act verb through an orientation tool's args against a recording runner double. Every condition below is decidable in one offline process.",
  "successConditions": "SINGLE ORCHESTRATION: with one composition in flight, a second intent returns the TYPED refusal `{ ok: false, refused: true, reason: 'single-session' }` (packages/drive/src/orchestrate.ts:142, :178-186), and the first session, once unblocked, still completes normally with its proposal. READ-ONLY TOOL SURFACE: the captured Options carry `tools: []` (packages/agent/src/headless-orchestrator.ts:305), `allowedTools` contains only `mcp__orientation__*` entries, and a write/act verb routed as a tool arg is refused AT the surface with the runner never invoked (packages/agent/src/orientation-tools.ts:91-97, :176-183). NO ACT SURFACES: the terminal entry passes no spawn, landing or inspect deps, so `spawnTools`/`landingTools`/`inspectTools` are all empty, no `spawn_builder`, `run_gate`, `open_landing_pr` or `git_inspect` name appears in `allowedTools`, and the corresponding MCP servers are absent from `mcpServers` entirely. NO HOSTED ENTRY exists (Phase 5). WHY MACHINE (re-adjudicated human -> machine 2026-07-26, ADR-0209 D8): the leg named NO judgment at all — not spend, not liveness, not even a harness excuse. Every condition is a refusal, an absence, a shape or a count, which `human-witness-is-a-judgment-gap-not-cost` puts on the machine rung, and `studio-build`#uat-8 already settled the identical 'confirm the no-land walls hold' shape as machine. Three of the conditions are already someone else's written assertion — `oc-single-session-guard`, `hsr-refuses-concurrent-session`, `ots-write-verb-refused-at-surface`. FALSE-PASS FENCES: (a) THE MOST LIKELY FALSE PASS IS PROVING A WALL BY ABSENCE OF OPPORTUNITY. A run that never had spawn/landing/inspect deps wired obviously lands nothing — observing 'it landed nothing' proves the deps were absent, not that a wall held. The load-bearing observation is the CONSTRUCTED SURFACE: assert on the `allowedTools` list and the `mcpServers` keys the entry actually built, an enumeration, not on the run having had no visible effect. (b) THE WALL IS SCOPED TO THIS ENTRY AND THE SCOPE IS LOAD-BEARING. Run these same assertions against the DESKTOP path and they go RED, correctly: `packages/drive/src/chat-stream.ts:280-282` forwards spawn (ADR-0137), landing (ADR-0152) and inspect (ADR-0173) deps into `orchestrate`, so a desktop chat session genuinely can spawn a builder, run the gate and open a landing PR. Those doors were opened by later accepted decisions, not by drift. A walk that reads this leg as a corpus-wide 'the agent can never land' claim will either mis-pass or mis-fail. (c) ASSERT THE TYPED DISCRIMINATOR, NEVER A BARE `ok: false`. The runner's own module-level inFlight flag already makes a bare `ok: false` true without the composition guard existing at all; only `refused === true` and `reason === 'single-session'` distinguish 'busy, retry' from a hard failure. (d) THE WRITE-VERB REFUSAL MUST BE OBSERVED AS THE RUNNER NOT BEING CALLED. Asserting only that the returned text says 'refused' passes equally if the verb reached the runner and was rejected downstream by `notWritable` — a different wall, one layer late. (e) THIS LEG DELIBERATELY CARRIES NO PROOF-GATE BINDING. This story declares no `## Reliability Gates` section, so there is no gate id to name; legs 1-3 are unbound for the same pre-existing reason. `resolveWitness` therefore reports this leg `refused` — an honest open binding gap to close if the story is ever re-armed, never a defect to paper over by minting an observe gate over capabilities whose `real:` arms were dropped at retirement (the rubber-stamp ADR-0097 section 2 bans).",
  "evidenceExpectations": "The captured SDK `Options` printed as actual values — the `tools` array, the full `allowedTools` list, and the full set of `mcpServers` keys — so the no-act-surface claim is derived from an enumeration rather than asserted. The second call's returned object printed in full, showing both `refused` and `reason`, plus the first session's later normal completion with its proposal (one side of a state change is not evidence of the state change). The recording runner's call log showing ZERO invocations for the write-verb attempt, alongside the refusal text. Confirmation that the real `orchestrate` composition and the real orientation surface were driven, named, with no stub standing in for either. A run whose evidence is a passing assertion count, or a bare `ok: false`, or a wall claimed from the run having produced no side effect, does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:pair-the-fence-with-the-affordance"]
}

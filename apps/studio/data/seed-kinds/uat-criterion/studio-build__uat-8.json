{
  "kind": "uat-criterion",
  "id": "studio-build#uat-8",
  "action": "OBSERVE GIT — no test in the repo does today, which is why this leg has been human-tagged since it was authored. Around a NODE-scope build, snapshot `git worktree list`, the full local and remote branch list, and HEAD; run the build; snapshot the same three again. Then, separately and offline, assert the routed options a NODE id resolves to through `routedBuildRunner` (`apps/studio/server/buildWorker.ts:144`): `live: true`, `verdictStore` undefined, `real` falsy, and no `openPr` key. The two halves are complementary — the git snapshots observe the EFFECT (nothing was created or pushed), the options assertion observes the INTENT (the `--live` non-persisting shape was requested).",
  "successConditions": "All three git snapshots are byte-identical before and after — no new worktree, no new local or remote branch, no moved HEAD, nothing landed — and the routed options match the accepted `--live` non-persisting shape, with the remaining walls holding (no hosted run, no `--real` toggle on a single node, no manual `gh pr merge`). THIS LEG MUST CURRENTLY FAIL, and that is the point: `routedBuildRunner` sends a node through `nodeBuild(..., { real: true, verdictStore: 'pg' })`, and `buildWorker.test.ts:135-159` pins exactly that `--real`, persisting, parked-branch shape — the OPPOSITE of the accepted wall the story locks under \"Known implementation gap\" (\"do not redefine the target\"). A spec written to PASS against today's behaviour would be a false pass that pins the divergence as correct; author it against the ACCEPTED wall so it goes green only when the code is restored. Note also that today's `park` claim (`buildWorker.test.ts:158`) asserts only that a transcript LINE mentions parking — a line of prose, not a git observation, and no substitute for one.",
  "evidenceExpectations": "The before/after `git worktree list`, branch list and HEAD captured verbatim so a reader can diff them, plus the routed options object as constructed. Until the node route is restored to `--live`, the honest evidence is a RED run naming which wall was breached — recorded as a red, never suppressed, skipped, or relaxed to green. Reclassified human -> machine on 2026-07-25 (ADR-0209 §8): worktree, branch and ref state is `git` output and the routed options are directly inspectable, so nothing here is a judgment — the leg was human only because nothing observed git, which is exactly the mis-tag `human-witness-is-a-judgment-gap-not-cost` forbids. Making it pass is a CODE obligation, not a re-tag. The STORY route's outward-facing approve-to-land, split out of this criterion, is the operator's leg 10.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:never-bypass-the-gate"]
}

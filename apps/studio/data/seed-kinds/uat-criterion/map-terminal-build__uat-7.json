{
  "kind": "uat-criterion",
  "id": "map-terminal-build#uat-7",
  "action": "The OWNER — never an agent, and never unattended — presses Enter on a command that arrived in the terminal by clicking Build on the forest map (not one typed or pasted by hand), and watches the run to a terminal state in the native shell. A story-scope seed spends the Claude subscription and opens an auto-merging PR (ADR-0136); a node-scope seed spends the subscription and parks a `claude/real/<unit>-<run>` branch (ADR-0031/0136). The owner then judges whether the build that ran is the build the Build click promised for the unit they clicked.",
  "successConditions": "The owner attests that pressing Enter on the SEEDED command launched a real build of the CLICKED unit as their own Claude Code, and that the run's outward effect was the intended one for its scope (the PR opened / the branch parked). FALSE-PASS FENCES: (a) The disqualifying substitution — a signed verdict plus an open PR proves a build RAN, not that this seeded pre-fill is what started it; a hand-typed or pasted command produces byte-identical evidence, so the provenance of the keystroke is the thing being attested and must be part of the record. (b) The unit identity must be checked, not assumed: a seed that composed the wrong id still runs a perfectly healthy build of the wrong unit, and every downstream signal looks green. (c) A `--dry-run` or scripted stand-in does NOT discharge this leg — and conversely, once the spend and the outward-facing PR are removed from the walk, nothing human remains in it and the observation belongs on leg 4. (d) An agent must not fire this to 'unblock' the leg: the metered spend and the auto-merging PR are exactly the irreversible, outward-facing actions an agent may never take unattended.",
  "evidenceExpectations": "The owner's recorded verdict, naming the unit clicked, the scope, and the resulting PR number or parked branch — plus the terminal transcript showing the seeded command at the prompt and the run that followed it, so the seed-to-run provenance is auditable rather than inferred. Evidence that shows only a signed verdict, only a merged PR, or only a green build log does not discharge this leg: none of them distinguishes a seeded launch from a typed one.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:agent-never-self-exempts", "asset:meter-fail-closed-caps-in-real-cost"]
}

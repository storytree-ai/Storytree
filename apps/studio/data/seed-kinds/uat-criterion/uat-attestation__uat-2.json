{
  "kind": "uat-criterion",
  "id": "uat-attestation#uat-2",
  "action": "Sign a REAL operator-attested verdict for a `human`-witness leg on BOTH write surfaces, then read the derived state back. CLI: `storytree witness attest <story>#uat-<n> --outcome pass --pg` (the ADR-0118 canonical verb; `uat attest` is the back-compat alias) against a CLEAN tree with a resolvable non-agent signer. Studio: `POST /api/uat/attest` as a verified identity permitted to attest (admin or `canAttestUat`) with the studio's serving commit resolvable — the row's muted PERSON icon is that call's in-UI trigger. Read each leg back through `storytree witness list <story> --pg` and the `/api/attestations` payload. Then drive the four REFUSALS on the same surfaces: a `machine`-witness leg, a `sandbox:` signer, the building session's own identity as signer, and (CLI) a dirty tree / no `--pg`.",
  "successConditions": "Each signature appends exactly ONE `events.verdict` row with `proofMode: \"operator-attested\"`, `unitId` = the leg id, the requested `outcome`, `signer` = the operator identity resolved by the SURFACE (the CLI's signer chain; the studio's VERIFIED caller — never a client-supplied field), `commitSha` = the commit being attested, and an `operator-attested` evidence ref; the leg then reads `proven=✓` and feeds `rollupStoryUat`. Each of the four refusals returns its reason AND leaves the verdict log byte-identical. FALSE-PASS FENCES: (a) The most dangerous false pass is proving only the POSITIVE path — every honesty wall in this leg is a REFUSAL, and a run that signs a human leg successfully and stops has demonstrated nothing about forging. (b) Asserting a refusal returned an error is insufficient: assert the store is UNCHANGED after each one, because fail-closed means no partial write, not a message. (c) Re-running `packages/orchestrator/src/proof/uat-proof.test.ts`'s `checkUatProof` table is NOT this leg — it proves the pure guard in isolation and cannot catch a write surface that forgets to CALL it, or that calls it with a witness read from the request body instead of the story's declared prose. The claim here is that the SIGNING SURFACES are held to the guard. (d) On the studio path the signer must not be injectable: a test that supplies `signer` in the POST body and then finds it in the verdict has proven the OPPOSITE of this leg. (e) This leg does not need a human. Its SUBJECT is a human's signature, but everything it asserts is a row's shape and an absence — both have compilers. Re-tagging it `human` because its topic is an operator is exactly the confusion the 2026-07-26 re-adjudication removed.",
  "evidenceExpectations": "Per surface: the persisted verdict row printed verbatim (all of `unitId`/`proofMode`/`outcome`/`signer`/`commitSha`/`evidence`/`at`), the leg's derived proven state before and after, and — for each of the four refusals — the refusal reason together with a re-read of the verdict log proving nothing was appended. Evidence showing only a successful signature does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:agent-never-self-exempts"]
}

{
  "kind": "uat-criterion",
  "id": "spawn-visibility#uat-6",
  "action": "Drive the COMPOSED dock+map surface, not either half alone. Mount `TreeView` with `ChatDock` wired to its `reloadTree` (`apps/studio/src/components/TreeView.tsx:1518`), backed by a tree fetch that resolves a corpus WITHOUT story `<id>` on the first call and WITH it on the second — modelling the spawned story-author having written `stories/<id>/` between the two. Snapshot the rendered map's node set. Deliver a `spawn`-finished frame for `role: \"story-author\"` through the scripted `api` stream. Let the reload settle, then snapshot the node set again. Issue no manual reload, no remount, and no refresh between the snapshots. NO HARNESS FOR THIS EXISTS TODAY — this is the spec for one, not a description of a passing test.",
  "successConditions": "Snapshot one does NOT contain a node for `<id>`; snapshot two DOES; and the only thing that happened in between was the spawn-finished frame. The delta must be observed as a real absence-then-presence transition, not as presence alone. FALSE-PASS FENCES: (a) THE PRIMARY ONE — asserting that `reloadTree` was CALLED is leg 4, not this leg, and a run that stops there is a false pass. Leg 4 already proves the callback fires on the right frame; the only thing leg 6 adds is that the call actually results in a node for `<id>` being present in the RENDERED map. Assert on the rendered node set, never on the callback. (b) Seeding the tree fetch to return `<id>` on BOTH calls makes snapshot two pass trivially while proving nothing about the reload — the first snapshot MUST be missing the node, and the test must assert that it is. (c) Remounting the component, or letting a mount-effect reload (`TreeView.tsx:1537`) run between the snapshots, fabricates the transition; the reload must be attributable to the frame alone. (d) Asserting on a fetch call count instead of the rendered output leaves the render path untested — the claim is about what the map CONTAINS. (e) Asserting the island's position, colour, or visual quality is out of scope and belongs to the map stories' appearance UAT; this leg is presence only. (f) `ChatDock` is dormant in the shipped app (ADR-0174/0175), so any harness necessarily composes the two components itself; the resulting green proves the wiring is sound, NOT that the shipped application does this today, and must be captioned accordingly.",
  "evidenceExpectations": "The test run showing both node-set snapshots verbatim, so the absence-then-presence transition is auditable rather than asserted, plus an explicit record that no manual reload or remount occurred between them. Because no harness exists, the honest current evidence state is: none. This leg carries no `proof-gate:` binding — none of the story's three declared `observe` gates covers this end-to-end assertion — so per ADR-0209 section 6 it stands UNSTAMPED. A `machine` witness tag names the right KIND of witness; it does not assert that the proof exists, and this leg must not be read as green.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

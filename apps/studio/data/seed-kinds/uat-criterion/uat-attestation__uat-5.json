{
  "kind": "uat-criterion",
  "id": "uat-attestation#uat-5",
  "action": "Against a story whose UAT has one leg ALREADY PROVEN by a signed verdict and a second leg with nothing signed, record a lower-rigor vouch on both — via `storytree witness vouch <story>#uat-<n> --pg` relayed (signer = the operator, `relayedBy` = the scribing session; `storytree attest` is the back-compat alias) and via `POST /api/attestations`. Also record one `machine`-witness vouch from inside a session. Snapshot the FULL `events.verdict` log, each leg's derived proven state, `rollupStoryUat`'s return, and the story's rendered island/crown status BEFORE and AFTER each vouch.",
  "successConditions": "Each vouch appends exactly one `events.attestation` row (`testId`, `outcome`, `witness`, `signer`, `at`, optional `note`, `relayedBy` on a relayed human vouch and ABSENT on the machine one even inside a session). Across every vouch: `events.verdict` is byte-identical before and after; the previously-unproven leg still reads `proven=–` and the proven one still `proven=✓`; `rollupStoryUat` RE-DERIVED after the write returns the same value; the story's island/crown status is unchanged; and a malformed signal grants nothing (refused at the write boundary, and skipped by `deriveAttestations` if already stored). FALSE-PASS FENCES: (a) The load-bearing claim is a NEGATIVE — nothing moved. Asserting only that the attestation row landed is a false pass. (b) The unchanged-verdict assertion must be made against a story that HAS a signed verdict and a real roll-up to move: 'unchanged' over an empty verdict log is vacuously true and would pass even if a vouch were writing verdicts. (c) The roll-up must be RE-DERIVED after the vouch, never compared against a memoized value — a cached snapshot proves nothing about isolation. (d) `packages/cli/src/attest.test.ts`'s `FakeStore` cannot discharge this leg: it has no verdict log, and the claim spans TWO logs. (e) This leg is not `human` merely because an owner is the one vouching — the vouch's CONTENT ('I eyeballed it') is a judgment nobody asserts here; what is asserted is where the bytes went, which has a compiler.",
  "evidenceExpectations": "The test output showing, per vouch: the attestation row verbatim (including the presence/absence of `relayedBy`), the FULL verdict log before and after, each leg's derived proven glyph on both sides, `rollupStoryUat`'s freshly-derived return on both sides, and the story's status on both sides. A run that prints only the recorded attestation does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:never-bypass-the-gate"]
}

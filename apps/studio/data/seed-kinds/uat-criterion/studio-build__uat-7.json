{
  "kind": "uat-criterion",
  "id": "studio-build#uat-7",
  "action": "Two halves. (a) LIVE-GATED, positive: after a build of the right shape has produced a verdict for the unit, read `events.verdict` DIRECTLY through a real connection (`createTestPool`, `packages/library/src/store/test-db.ts`) and assert the row exists for that unit and carries a SPINE signer — a verdict produced by the gate's observed red->green, per ADR-0091's \"the verdict is produced by the gate, never handed in\". SKIP when the store is unreachable rather than failing; see the story's open modeling call 2 for the live-test surface `apps/studio` still lacks. (b) OFFLINE, negative: assert the frontend holds no path to write a verdict at all — the `/api/*` route table (`apiRouter.ts`'s `handleApiRequest`) exposes no verdict-write endpoint reachable from the client, and no module under `apps/studio/src` imports the signer or the proof machinery. BOTH halves are required.",
  "successConditions": "(a) A row for the unit in `events.verdict` whose signer is the spine's. Reading the hue off `GET /api/tree` is NOT a substitute and is a FALSE PASS for this leg: it observes what the UI displays, which is precisely the thing the leg exists to distinguish from what the store holds. (b) The negative half is load-bearing, not decorative — a signer-field assertion alone does not establish spine provenance, because a UI-written row could carry any signer string; the leg's claim is provenance, so the absence of any frontend write path is what closes it. CONTINGENCY, and the reason this leg may be unreachable as authored: the story's open modeling call 3 records that the accepted NODE route is declared \"non-persisting\" under \"Known implementation gap\", and `buildWorker.test.ts:92-109` pins `buildRunnerFromNodeBuild` with `verdictStore` UNDEFINED (\"ADR-0099-B no forged persist\"). If that wall is the true one, no row exists on the node route and this leg must be RE-HOMED to the story route or to a `--live --store pg` variant by the owner — never forced to pass by making the node route persist, which would settle an acceptance question a witness re-adjudication has no authority over.",
  "evidenceExpectations": "The live spec's output showing the row's contents (unit, signer, anchor) and an explicit record of which branch ran — a SKIP is an honest non-result, never a pass — plus the route-table and module-graph assertion output for the negative half. The evidence must make clear that the row was read from the store directly and not through `/api/tree`. Reclassified human -> machine on 2026-07-25 (ADR-0209 §8): a row's presence and its signer field are DB reads and \"the frontend holds no write path\" is a static assertion, so nothing here is a judgment; needing a reachable store is expense, not irreducibility, and the claim that the verdict came from a genuinely BILLED run is the operator's leg 9.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:never-bypass-the-gate"]
}

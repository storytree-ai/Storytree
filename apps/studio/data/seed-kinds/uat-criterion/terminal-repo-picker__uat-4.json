{
  "kind": "uat-criterion",
  "id": "terminal-repo-picker#uat-4",
  "action": "In the Electron `_electron` harness, walk the fail-closed gate in three phases against a clean userData (no persisted repo-selection.json). PHASE 1 — no selection: enter the forest and read the gate. PHASE 2 — pick: stub `dialog.showOpenDialog` in the main to return a known git checkout, call `window.desktopRepo.pick()`, expand the terminal. PHASE 3 — change: re-stub the dialog to return a DIFFERENT git checkout, call `window.desktopRepo.pick()` again, and observe the cwd-keyed remount (TerminalRepoGate renders `<TerminalDock key={cwd} />`, so a cwd change forces a fresh mount and a fresh pty).",
  "successConditions": "PHASE 1: `.terminal-gate-message` is present reading \"Select a repository to start the terminal\", and `window.desktopTerminal.list()` is EMPTY — the terminal is genuinely refused, not merely hidden. Asserting the gate message alone is insufficient: the load-bearing claim is that NO pty is spawned. PHASE 2: the dock renders and list() reports exactly one session whose pty cwd is the picked checkout. PHASE 3: list() reports a session with a DIFFERENT sessionId from phase 2, whose cwd is the second checkout — a genuinely fresh pty, not a re-used one. This is the end-to-end fail-closed behaviour over the REAL bridge and a REAL pty; capability terminal-repo-gate's own signed verdict proves only the same logic over a mocked bridge and a mocked TerminalDock.",
  "evidenceExpectations": "The e2e spec's node:test output covering all three phases, with the phase-1 empty-session-list assertion explicitly present and the phase-3 assertion comparing session ids across the change. Evidence that shows only the gate message rendering does not discharge this leg.",
  "refs": ["asset:uat-proves-the-goal-not-the-surface"]
}

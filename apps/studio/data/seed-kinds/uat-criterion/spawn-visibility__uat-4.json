{
  "kind": "uat-criterion",
  "id": "spawn-visibility#uat-4",
  "action": "Render the studio dock/panel pair in jsdom (the `studio` vitest suite) with an injected `reloadTree` callback, and drive a scripted `api` chat stream that emits `spawn` frames. Walk four cases against the SAME mounted tree: (a) `phase: \"finished\"`, `role: \"story-author\"`; (b) `phase: \"finished\"`, `role: \"builder\"`; (c) `phase: \"started\"`, `role: \"story-author\"`; (d) two consecutive story-author finishes. Read the callback's invocation count after each. The wiring under test is `ChatDock.handleSpawnFinished` (`apps/studio/src/components/ChatDock.tsx:77-83`, fence at :79 `if (frame.role === 'story-author') onReloadTree?.()`), passed to the panel at :170; the existing suite is `apps/studio/src/components/ChatDock.reload.test.tsx`.",
  "successConditions": "The callback fires EXACTLY once for (a), ZERO times for (b) and (c), and twice for (d) — the fence is on role AND phase, not on either alone. `onReloadTree` reaches the dock as a plain prop carrying no drive/agent/orchestrator value, and `apps/studio/src/modelPathBoundary.test.ts` stays green (its FORBIDDEN list at :21 is `@storytree/agent`, `@storytree/cli`, `@storytree/drive`, `@storytree/orchestrator`; the walk at :34 must still yield `offenders === []`). The callback identity must be the same `reloadTree` the crown-refresh uses — `apps/studio/src/components/TreeView.tsx:1518`, passed as `onCrownRefresh` at :2594 — not a second reload path. FALSE-PASS FENCES: (a) THE BIG ONE — this leg proves the COMPONENT SEAM only. `ChatDock` is DORMANT in the shipped app: since ADR-0174 gave the dock slot to the embedded terminal and ADR-0175 held spawn/landing out of `app-guide`, nothing imports or mounts `<ChatDock>` (`TreeView.tsx:2561` records this in a comment). A green run here therefore proves the callback would fire IF the dock were mounted, and says NOTHING about whether any reload happens in the real application — do not read it as end-to-end. That end-to-end presence claim is leg 6, which is unharnessed. (b) Asserting only case (a) passes trivially against a dock that calls the callback on EVERY spawn frame; the negative cases (b) and (c) are what pin the fence and must be present. (c) Asserting that `reloadTree` was *defined* or *passed* rather than *invoked* proves nothing — read the invocation count. (d) Stubbing `ChatPanel` and calling `handleSpawnFinished` directly bypasses the `onSpawnFinished` wiring at :170 that is half of what this leg claims; drive the frame through the panel.",
  "evidenceExpectations": "The vitest run output naming the four cases with the observed invocation count for each (1 / 0 / 0 / 2), plus a green `modelPathBoundary.test.ts`. Because the dormancy fence above is the leg's main hazard, the evidence must be captioned with the fact that `ChatDock` is unmounted in the app — a bare green with no such caption invites the reader to over-claim. Evidence that shows only a single positive case, or that asserts on prop presence rather than invocation, does not discharge this leg.",
  "refs": ["asset:uat-proves-the-goal-not-the-surface", "asset:human-witness-is-a-judgment-gap-not-cost"]
}

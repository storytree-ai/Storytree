{
  "kind": "uat-criterion",
  "id": "terminal-tabs#uat-8",
  "action": "In the same Electron `_electron` harness and repo-gate-satisfied launch as `terminal-tabs#uat-6`, hold TWO real ptys and exercise the per-tab state the leg-5 feel verdict rests on. Write a distinct marker into each session (e.g. `echo alpha-probe` in tab 1, `echo beta-probe` in tab 2) and let each shell echo it back. Switch rows and read BOTH sessions back via `desktopTerminal.snapshot`. Emit an ANSI colour sequence into ONE session only. Resize the dock (the drag edge, or a `ResizeObserver`-triggering container change) while a known tab is active. Type after a switch, without clicking the pane, and observe which pty receives it. Finally close one row and re-read the survivor.",
  "successConditions": "Each session's snapshot contains ONLY its own marker across every switch — never the sibling's — so switching neither loses nor cross-contaminates scrollback (a switch hides the pane via the `hidden` attribute, it never unmounts or re-spawns). The ANSI colour bytes appear only in the emitting session's snapshot. The resize forwards new `cols`/`rows` to the ACTIVE session's pty and to no other (the debounced forward means the assertion must await the settle, not the mousemove). Post-switch keystrokes arrive at the newly-active session's pty and not the previously-active one. After the close, the surviving session's snapshot still carries its full marker history — the close reaped exactly one pty and disturbed nothing else. FALSE-PASS FENCES: (a) DOM `textContent` is NOT a usable observable — the dock paints on xterm's WebGL renderer where available, so pane text reads empty and an assertion built on it can pass or fail for reasons unrelated to the behaviour. Every screen claim must go through the main-held `desktopTerminal.snapshot` relay. (b) Re-running the jsdom `mst-scopes-io-per-tab` / `tdp-resizes-with-the-dock` / `tdp-refits-on-container-resize` suite is NOT this leg: those route a SCRIPTED chunk through a mocked bridge into a `FakeTerminal` that records writes. They cannot show that a real pty's own output, at real dimensions, lands in the right pane and survives a switch — the only thing this leg adds. (c) Asserting a marker is PRESENT in the right session is half the test; the discriminating assertion is that it is ABSENT from the sibling. (d) This leg proves the mechanics only. Whether the result reads as ONE coherent tabbed terminal — including whether the colours look right — is leg 5's irreducible feel verdict and must never be inferred from a green here.",
  "evidenceExpectations": "The spec's `node:test` output printing both sessions' snapshots after each switch (so the present-here / absent-there pairing is visible rather than argued), the forwarded `cols`/`rows` per sessionId across the resize, the sessionId that received the post-switch keystrokes, and the survivor's snapshot after the close. Evidence from a jsdom vitest run, or that shows only one session's screen, does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

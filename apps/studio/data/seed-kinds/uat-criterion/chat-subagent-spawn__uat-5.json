{
  "kind": "uat-criterion",
  "id": "chat-subagent-spawn#uat-5",
  "action": "In the desktop app, with the sidecar (`apps/desktop/electron/backend-entry.ts`) composing REAL spawn deps into the chat mount — the pg claim store, the live `BuildContext`, the repo cwd, the session identity/branch — converse with the orchestrator until it decides a story should exist, and let it act. The orchestrator takes the ADR-0121 story-claim and calls `spawn_story_author`, which starts a REAL (not scripted) `query()` story-author session against the paid subscription. Let the spawned session run to completion, then inspect the working tree and the chat transcript. NOTE: this story is RETIRED in place (ADR-0174/ADR-0175 replaced the interactive chat with an embedded terminal running real Claude Code), so this leg records which witness would be RIGHT for the walk — it is not scheduled work.",
  "successConditions": "`stories/<id>/story.md` and its capability files exist on disk, authored during the spawned session's run, with the claim held for the spawn's duration. The ONLY condition that needs a human here is that a real, subscription-billed spawn actually occurred and produced them. WHY HUMAN — the SPEND basis, and that basis alone: a live `query()` story-author session is subscription-billed (ADR-0030), so ADR-0010 §5 forbids running it on a gate pass. There is NO judgment gap in this leg — nobody grades taste, look, feel, or fit; if the spawn ever became free to run unattended, this leg becomes `machine` unchanged. FALSE-PASS FENCES: (a) The most dangerous false pass is a walk that finds story files and concludes the spawn authored them. Capture the tree state BEFORE the conversation; a pre-existing (or hand-authored, or terminal-authored) `stories/<id>/` directory satisfies the observable while proving nothing. Bind the files to the spawned session's own run, not merely to the wall clock. (b) A scripted or stubbed `queryFn` anywhere in the composed path VOIDS this leg: with a double in place the walk degenerates into leg 1, which already proves the fence offline and does not need an operator. If no real spend was incurred, this leg did not run. (c) Do NOT re-assert the machine claims here — the write fence and the typed non-verdict result are leg 1's, the claim-before-spawn ordering and the holder-naming refusal of a concurrent claimant are leg 3's (over the real `ClaimResult` shape), and 'the chat itself wrote no file' is leg 7's absence audit over `tools: []`. Restating them as operator-attested conditions launders compiled facts into an unrepeatable signature. (d) The wisp's authoring COLOUR is NOT witnessed here: that is `wisp-as-story-claim`'s appearance UAT. Grading the colour on this leg would silently import another story's human verdict.",
  "evidenceExpectations": "The before/after working-tree state (`git status` plus the new files' paths), the spawned session's own trace or summary tying the authored files to that run, the claim row showing the story held by this session for the spawn's duration, and positive evidence that the run was live — a real session id and real subscription usage, not a scripted transcript. Evidence that shows story files existing, without tying them to a billed spawn, does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

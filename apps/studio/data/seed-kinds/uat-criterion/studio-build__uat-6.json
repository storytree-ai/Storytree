{
  "kind": "uat-criterion",
  "id": "studio-build#uat-6",
  "action": "Two halves, both offline. (a) SERVER: over the real `node:http` + `handleApiRequest` + `BuildRegistry` stack with the runner injected, let a run terminalise and assert the terminal response's ENVELOPE carries all four declared fields — verdict line, signer, cost, and phase trail. (b) CLIENT, jsdom: render the island panel with a mocked `api` client whose `buildStatus` reaches a terminal status, and assert that reaching terminal ITSELF causes a re-pull of `/api/tree` and that the node's rendered verdict hue/state changes to the newly signed value — with no navigation, no remount and no hand-fed props in between. Fake timers plus `BUILD_POLL_MS`, the pattern `BuildSection.test.tsx` already uses, drive the poll to terminal.",
  "successConditions": "(a) All four envelope fields are asserted. Two of them are currently unasserted anywhere: `cost` and the phase-trail line sit in fixtures (`buildApi.integration.test.ts:110`) while the assertion at :113 matches only `/verdict: PASS/`; `verdict` and `signer` are already pinned at `BuildSection.test.tsx:124-125`. (b) `api.tree`'s call count increases STRICTLY AFTER the terminal poll, and the rendered hue/state reflects the new `latestVerdicts` value. Asserting that `onTerminal` fired — all that exists today, `BuildSection.test.tsx:232-257` — is insufficient: it proves a callback was invoked, not that anything repainted, and the leg's claim is the repaint. Equally insufficient, and a FALSE PASS: re-rendering the component by hand with new props, or reloading the harness page, would demonstrate that fresh data renders while saying nothing about \"WITHOUT a manual reload\" — the re-pull must be CAUSED by the terminal poll for this leg to be discharged.",
  "evidenceExpectations": "The envelope assertion showing all four fields, the `api.tree` call counts immediately before and after the terminal poll, and the rendered hue/state before and after — so the causal ordering (terminal poll, then re-fetch, then changed hue) is visible in the output rather than inferred. The harness must show no reload or navigation occurred. This leg is the MACHINE half of a criterion that fused the repaint mechanics onto the subscription-billed run; per `human-witness-is-a-judgment-gap-not-cost` the mechanics hold for any run of the right shape and only the spend stayed human (the story's leg 9), while whether the finished panel LOOKS right is the operator's leg 11.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

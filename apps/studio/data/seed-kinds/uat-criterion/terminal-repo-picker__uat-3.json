{
  "kind": "uat-criterion",
  "id": "terminal-repo-picker#uat-3",
  "action": "In the Electron `_electron` Playwright harness (apps/desktop/e2e/, following the session-survival.e2e.mjs precedent), launch the desktop app offline with STORYTREE_DESKTOP_E2E=1 and the /api/* stubs in force. Stub the native directory dialog IN THE ELECTRON MAIN — `app.evaluate(({ dialog }) => { dialog.showOpenDialog = async () => ({ canceled: false, filePaths: [<a known git checkout>] }); })`, which takes effect because electron/main.ts calls showOpenDialog as a property on the shared `electron` module object. Then drive the renderer's real bridge: `window.desktopRepo.pick()`. Expand the terminal (the `[aria-label=\"expand terminal\"]` toggle), wait for `window.desktopTerminal.list()` to report one session, click into `.terminal-dock-body:not([hidden])`, type `pwd` and press Enter.",
  "successConditions": "`window.desktopTerminal.snapshot(sessionId)` — the MAIN-held scrollback, the renderer-independent observable — contains the picked checkout's path, and NOT the app's serve root. The main-held ring is used deliberately rather than DOM text: the dock renders on xterm's WebGL renderer where available, so a DOM textContent read would pass or fail by GPU availability rather than by the behaviour under test. Only the dialog call is stubbed; everything downstream stays REAL — the real node:fs DirProbe validating the path, the real userData repo-selection.json persisting it, the real ipcMain handlers, and the real node-pty spawned with cwd = repo-selection.resolveCwd(serveRoot).",
  "evidenceExpectations": "The e2e spec's node:test output showing the assertion on the resolved pty cwd, plus the captured session snapshot text containing the picked path. The run must be the standing CI one where possible: the e2e-desktop workflow triggers on apps/desktop/** for PRs to main, so this leg's proof is a path-triggered standing check rather than a local-only run. A PASS recorded without the pty actually spawning (an empty session list) is not evidence for this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

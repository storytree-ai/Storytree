{
  "kind": "uat-criterion",
  "id": "wisp-as-story-claim#uat-9",
  "action": "Take a real branch that holds claims across MORE THAN ONE grade on more than one unit — a `work` claim with at least one live `waiting` claim queued behind it on the same unit, plus one stale waiter — merge its PR for real so the CI merge job runs, and then read the live ledger: `events.node_claim` for the merged branch, and the `events.claim_event` audit rows written by the sweep. The observable is the merge job's EFFECT, not the workflow file's text.",
  "successConditions": "Every `events.node_claim` row for the merged branch is gone whatever its grade (exploring, waiting and work alike — the release keys on `branch` alone). One `released` `claim_event` row exists per cleared claim. On each cleared unit the OLDEST LIVE waiter is promoted in the same transaction, stale waiters skipped. Releasing a branch that holds no claims is a no-op returning 0. And a release FAILURE does not fail the merge — the sweep is advisory and logged, because the merge has already happened. INSUFFICIENCY — WHAT MAKES A PASSING RUN A FALSE PASS: (a) re-running `packages/notice-board/src/store/claim-store-release-by-branch.live.test.ts` proves capability A's FUNCTION and is a false pass here — that spec calls `releaseClaimsByBranch` directly and never touches the workflow; this leg's entire added claim is that the CI merge job ACTUALLY CALLS it, with the merged branch name threaded through. (b) Grepping `.github/workflows/ci.yml` for a step that names the release proves TEXT, not EXECUTION — a step can exist and be skipped by an `if:`, run against the wrong branch ref, or swallow its own failure while doing nothing. (c) A spec that checks only 'the holder's rows are gone' passes a sweep that stranded the queue: waiter promotion must be asserted, or the next session waits forever behind a claim that no longer exists. (d) A spec that checks only 'the rows are gone' also passes a workflow that HARD-FAILS the merge job when the release errors — the advisory, log-and-continue contract must be asserted as its own case, because a sweep that can fail a merge is worse than a sweep that misses one (the 2 h stale-reclaim and `worktree prune` are the designed backstops for a miss). (e) A single-grade fixture is insufficient: 'every grade' is the claim, and a work-only branch cannot distinguish a grade-filtered release from an unfiltered one. CARRY, UNRESOLVED: `ci-clear-on-merge` is `proof_mode: operator-attested` precisely because a workflow edit has no isolatable red→green the prove-it-gate can drive. That is consistent with this leg's `machine` witness — the witness kind states what KIND of observer is right (released rows and audit events are byte-level observables, and an un-harnessed workflow is a COST, not a judgment gap), while the capability's proof mode describes how the gate can reach it. How a CI-observed effect reaches the parent proof spine is story open modeling call 4 and is NOT settled by this leg being tagged machine.",
  "evidenceExpectations": "The pre-merge ledger snapshot (rows by unit, grade, session and branch, including the stale waiter), the merge job's own log lines showing the release step ran and the count it returned, and the post-merge snapshot showing zero rows for the branch, one `released` audit row per cleared claim, and the promoted waiter's `promoted` event naming which waiter won and by what `claimed_at`. The advisory case needs its own separate evidence: a run in which the release errors and the merge job still succeeds. The branch name must be visible in the release call's evidence so it is unambiguous the workflow passed the right ref rather than a default.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:never-bypass-the-gate"]
}

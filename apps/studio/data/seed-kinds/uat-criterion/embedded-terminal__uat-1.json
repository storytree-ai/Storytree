{
  "kind": "uat-criterion",
  "id": "embedded-terminal#uat-1",
  "action": "In the Electron `_electron` Playwright harness (`apps/desktop/e2e/`, the `session-survival.e2e.mjs` launch pattern: `STORYTREE_DESKTOP_E2E=1`, `/api/*` stubbed, wait for the studio origin), satisfy the terminal-repo-picker gate by pre-writing the userData `repo-selection.json` with a real git checkout, then enter `#/tree` from a clean mount and wait for the forest to settle. Read THREE structural facts out of the real Electron renderer: (a) the dock slot exposes the `[aria-label=\"expand terminal\"]` toggle inside the map's `.world-frame`; (b) clicking it renders a live `.terminal-dock` with its session panel (`.terminal-dock-panel .terminal-dock-panel-row`); (c) query the whole document for a chat dock (`.chat-dock`) and for any second interactive input surface.",
  "successConditions": "(a) and (b) present, and (c) EMPTY — the dormant chat is not mounted as a second interactive surface (ADR-0175), while `ChatDock.tsx`/`ChatPanel.tsx` and their vitest suites remain in the tree untouched. The three facts must be read from the REAL Electron renderer, not from a jsdom render of TerminalDock: capability `terminal-dock-panel`'s signed verdict already proves the dock's internals over a MOCKED xterm and a MOCKED bridge, so a jsdom assertion would restate that capability instead of proving the story-level mount. Asserting only that `.terminal-dock` exists is insufficient — the load-bearing claims are its PLACEMENT in the interactive dock slot the chat used and the ABSENCE of a competing chat surface.",
  "evidenceExpectations": "The e2e spec's node:test output showing the repo-gate precondition being written, the toggle located and clicked, the dock and its session panel attached, and an explicit assertion that the `.chat-dock` query returned nothing. A run that never got past the repo gate (dock absent because no repo was selected) is a FALSE PASS for clause (c) — absence of a chat dock is trivially true when nothing rendered — so the evidence must show the dock present and the chat dock absent in the SAME observation. This leg was reclassified human -> machine on 2026-07-26 (ADR-0209 §8): its prior justification, \"mounting the already-proven TerminalDock in TreeView is operator-attested glue, not a CI leg\", conflated a capability-TIERING call with a witness kind — presence, placement and single-surface-ness are DOM structure, and an unwritten harness is a cost, never a judgment gap.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

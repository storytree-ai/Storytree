{
  "kind": "uat-criterion",
  "id": "map-terminal-build#uat-6",
  "action": "Show the owner the command the composer actually emits for both scopes — today `pnpm storytree story build <id> --real --store pg` and `pnpm storytree node build <id> --real --store pg` (`apps/studio/src/lib/buildCommand.ts`) — as it appears pre-filled at the terminal prompt, and ask the single question this leg exists to settle: is this the invocation you want the Build button to hand you on your own shell? Surface the specific fork open modeling call 1 left open — ADR-0174's text writes the BARE `storytree … build <id> --real`, while the composer emits the `pnpm ` prefix because the embedded terminal spawns the platform shell at the runtime worktree root (ADR-0181), where a bare `storytree` is not on `PATH`. The owner answers: keep the `pnpm ` prefix, or drop it (a one-token edit to that one function plus its `cbc-*` contracts).",
  "successConditions": "The owner states, on the record, which invocation form the Build button should seed — and if that is anything other than what the composer emits today, the change lands in `buildCommand.ts` and its `cbc-*` contracts before this leg is attested. FALSE-PASS FENCES: (a) The disqualifying substitution is proving RUNNABILITY instead of ACCEPTANCE. That the command executes is machine-checkable by anyone — `pnpm storytree story build --help` settles it for free — and a demonstration that the command runs proves only that it runs, NOT that it is the form the owner wants. This leg is human solely because 'is this the invocation I want on my machine' is an owner value call with no compiler; a passing run is not evidence for it. (b) An agent reading CLAUDE.md and concluding `pnpm storytree …` is 'the documented invocation' is re-deriving the orchestrator's original reasoning, not obtaining the owner's acceptance — the whole point of leaving this open is that the owner may keep a global `storytree` bin the repo cannot see. (c) Silence is not acceptance: this leg is discharged by a stated answer, never by the absence of an objection. (d) An attestation gathered against a screenshot of the pre-fill without the owner being shown the bare-vs-`pnpm` fork does not discharge it — the fork IS the judgment.",
  "evidenceExpectations": "The owner's recorded answer naming the chosen invocation form, plus a pointer to the composer as it stands at attestation time (`apps/studio/src/lib/buildCommand.ts`) so the attested form and the emitted form can be compared later. If the answer changed the composer, the landing commit/PR that carried the change and its passing `cbc-*` tests. Evidence that shows only a successful command execution, or only the pre-filled string with no stated verdict, does not discharge this leg.",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:agent-never-self-exempts"]
}

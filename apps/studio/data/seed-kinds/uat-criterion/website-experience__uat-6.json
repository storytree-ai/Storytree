{
  "kind": "uat-criterion",
  "id": "website-experience#uat-6",
  "action": "In the same web-repo Playwright walk that reaches Act 1's peak, install instrumentation BEFORE the send — a network request log keyed by URL, a navigation/unload listener, and a record of the document's identity (a value stamped on `window` at first load) — then, at peak overload, locate the calm storytree affordance and click it exactly ONCE. Continue observing through the resolve into Act 2. This is the runtime companion to the parent-side static-import wall (`check:web-experience`, story leg 2): the gate proves no WebGL/island specifier is STATICALLY reachable from the Act 1 entry, and this leg proves the deferred bundle's bytes actually waited for the click.",
  "successConditions": "Exactly ONE calm affordance is present at peak (not a menu of exits) and one click discharges it. The document is NEVER navigated across the transform: no `unload`/`load`, no URL change (including no hash or history push that reloads), and the `window` stamp taken before the click is still present after the calm land has resolved — the same DOM session carries through, so the change is a TRANSFORM in place and not a page swap. And the deferred Act 2 bundle's first request timestamp is AT or after the click, with zero bytes of it requested during Act 1. FALSE-PASS FENCES: (a) asserting the calm land is visible does not distinguish a transform from a navigation to a calm page — the same-document evidence (the surviving stamp plus the absent navigation events) is the whole claim, and a spec that only screenshots the outcome is a false pass; (b) 'no WebGL byte was fetched' is NOT this leg on the post-ADR-0148 surface — that ADR retired the R3F landing island, so a spec asserting the absence of `three`/R3F chunks can pass trivially while the real deferred module was eagerly fetched during Act 1; the spec MUST name the actual deferred Act 2 module on the current surface (story open modeling call 8) or it proves nothing; (c) re-running `check:web-experience` is leg 2, not this leg — a static-import wall cannot see a runtime eager fetch. This leg is the MECHANICS only: that the collapse into soil is perceived as one continuous exhale, and that audio resolves rather than cuts, is story leg 7's human verdict.",
  "evidenceExpectations": "The web-repo Playwright run's output showing the affordance count at peak, the click, the navigation-listener silence and the surviving document stamp, and the network log excerpt with the deferred module's URL and its first-request timestamp relative to the click — the module named explicitly, so the evidence is auditable rather than a bare 'no R3F' negative. As with every site-behaviour leg here the run is not parent-signed today (story open modeling call 6).",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost"]
}

{
  "kind": "uat-criterion",
  "id": "website-experience#uat-8",
  "action": "Continuing the same web-repo Playwright session past the inflection, walk increment G's arc from the calm land to its end, advancing ONE step at a time and counting the inputs each advance costs. Before advancing, assert the opening surface; at each beat, snapshot which walk elements are present and which are still hidden; at the end, assert how the arc terminates. The beats are DATA — the `act2-beat-director` LEAF in `packages/forest-world-r3f` is the parent-proven, leaf-signed source of the script (ADR-0165 §10 kept it byte-untouched through the redesign) — so the spec asserts the SITE's rendering against the director's beat sequence rather than against a hand-copied list.",
  "successConditions": "The orchestrator's chat surface is the REAL app's and sits AT THE BOTTOM, and it opens by presenting the story as an OUTCOME BRIEF WITH AN EXAMPLE and proposing a MOCK local website with no backend. Each advance costs exactly ONE input and no beat demands a second. The rendered beat sequence matches the director's: routed to the drive machinery, the story branches, the machinery deepens (CI/CD, devops, gates, wiring), then the pull-back to one legible forest with the tri-state vocabulary (green = proven, sapling = in-progress, withered = broken). A limb renders green ONLY where a signed-passing-proof marker is present on that node's data — never on beat position alone. Elements not yet walked are absent or hidden at each snapshot (progressive disclosure). There is NO 'skip the intro' control and NO reachable link out to a static or deprecated page anywhere in the walk (a11y fallback only, ADR-0153). There is NO wrong-way-road 'antipattern flagged' teach at any beat (retired, ADR-0150 section 4). The arc ends on a CTA that CONTINUES into 'what's next' — a continuation seam into increment H, not a terminating hand-off. FALSE-PASS FENCES: (a) asserting the final state reached is not the ONE-TAP claim — a walk that needed two clicks at one beat passes an end-state check and fails the criterion, so the per-advance input count is load-bearing; (b) asserting green limbs EXIST is a false pass unless the spec also shows a node WITHOUT a signed-proof marker rendering NOT-green — otherwise a decorative always-green render passes; (c) the absence assertions (no skip, no static escape, no antipattern flag) must sweep every beat, not just the first screen — the retired teach lived at beat 4; (d) asserting the CTA's text is not the continuation claim — that it continues the same walk rather than terminating is what leg 10 then picks up. This leg is the MECHANICS only: whether Act 2 READS as Act 1's request answered, whether the proposal reads honest, whether the language is plain, and whether the Act 1 contrast lands are story leg 9's human verdicts and must never be asserted here.",
  "evidenceExpectations": "The web-repo Playwright run's output enumerating: the chat surface's position, the opening brief/proposal assertions, a per-beat table of (advance number, input count, beat id matched against the director's script, elements revealed, elements still hidden), the green-only-on-marker positive AND negative cases, each named absence sweep, and the terminating CTA's continuation target. Naming the director beat ids keeps the site-vs-engine correspondence auditable — a run that re-asserts a hand-copied beat list proves the spec agrees with itself. The run is not parent-signed today (story open modeling call 6).",
  "refs": ["asset:human-witness-is-a-judgment-gap-not-cost", "asset:uat-proves-the-goal-not-the-surface"]
}

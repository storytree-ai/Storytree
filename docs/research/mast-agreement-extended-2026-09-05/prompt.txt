You are one of two independent annotators. Classify each of the 40 failure records below against the frame given here, using ONLY the record's own text and this frame. Do not use any tool, do not read any file, do not run any command, do not search. Answer from the text alone.

THE FRAME — 19 labels. Assign exactly ONE primary label per record: the single mode that best describes the ROOT CAUSE of the failure the record describes.

FC1 — SPECIFICATION AND SYSTEM DESIGN (the agent's handling of its specification)
- disobey-task-specification — the agent fails to honour the specified constraints or requirements of the task.
- disobey-role-specification — the agent acts outside its defined responsibilities or role.
- step-repetition — the agent needlessly repeats steps already completed.
- loss-of-conversation-history — the agent unexpectedly truncates or forgets context it was holding.
- unaware-of-termination-conditions — the agent does not recognise the criteria that should end the interaction.

FC2 — INTER-AGENT MISALIGNMENT (between agents, or between an agent and the human)
- conversation-reset — a dialogue is unexpectedly restarted, losing progress.
- fail-to-ask-for-clarification — the agent proceeds on insufficient information instead of asking.
- task-derailment — the agent deviates from the intended objective.
- information-withholding — the agent fails to pass on information it holds that another party needs.
- ignored-other-agents-input — the agent disregards input or a suggestion from another agent.
- reasoning-action-mismatch — the action taken does not follow from the stated reasoning.

FC3 — TASK VERIFICATION AND TERMINATION
- premature-termination — the agent ends before the objective is achieved or the required exchanges are complete.
- no-or-incomplete-verification — the agent omits the check, or checks only part of the outcome.
- incorrect-verification — a check RAN but was itself wrong: it validated against the wrong thing, so it passed while the defect stood.

EXTENSION — CAUSES THAT ARE NOT THE AGENT'S REASONING (the tooling, platform, or data model the agent worked within)
- tool-defect — a command, verb, check, or script that EXISTS behaves wrongly: a destructive or incorrect write, an unresolved convention, a misleading refusal, a bad default or budget, a flag interaction that breaks a documented exemption.
- environment-defect — the machine, shell, platform, or CI is the cause, not the repository's code.
- missing-capability — the verb, mechanism, or guard needed simply does not exist yet.
- data-model-gap — a schema, record type, allowlist, or surface cannot express or see something it must.

THE ESCAPE HATCH
- no-mast-home — the failure is real and NONE of the 18 modes above describes it. Use this freely when it is true; it is a finding, not a cop-out. Never stretch a mode to fit.

OUTPUT FORMAT — respond with ONLY a JSON array, no prose before or after, no markdown fence, one object per record in the order given:
[{"id": "<record id>", "mode": "<one label exactly as spelled above>", "reason": "<one sentence>"}, ...]
All 40 records must appear. The "mode" value must be one of the 19 labels, spelled exactly.

THE 40 RECORDS (fields: id, title, statement, evidence, impact):
[
  {
    "id": "friction-studio-kind-blind",
    "title": "The studio was blind to the friction kind",
    "statement": "The studio UI and its write-allowlist had no `friction` category, so friction artifacts were invisible and a friction write was rejected as an invalid category.",
    "evidence": "apps/studio/server/apiRouter.ts:457 `if (!ASSET_CATEGORIES.includes(category)) throw new HttpError(400, 'invalid category')` — the allowlist array (lines 80-92) omitted 'friction'. `pnpm exec vitest run server/serveApi.integration.test.ts -t friction` returned `AssertionError: expected 400 to be 201` before the fix.",
    "impact": "Increments 1-3 (kind schema, capture CLI, drain gate) landed friction end-to-end, but no session could SEE its own filed friction in the members studio until inc 4 wired the six studio surfaces (types union, ASSET_CATEGORIES, gloss, TYPE_LABEL, server allowlist, cat colour)."
  },
  {
    "id": "coverage-tool-parses-tsx-as-scriptkind-ts",
    "title": "storytree coverage parses .tsx as ScriptKind.TS, silently understating coverage for inline-JSX-object tests",
    "statement": "Five correctly-named, passing terminal-dock-seed contract tests read 1/5 covered; only hoisting the inline `seed={{ command, token }}` JSX prop to a named const made the coverage AST detect the assertions, flipping it to 5/5.",
    "evidence": "packages/orchestrator/src/proof/contract-coverage.ts:238 calls `ts.createSourceFile('__coverage__.ts', src, ..., ts.ScriptKind.TS)` on a .tsx file. `storytree coverage terminal-dock-seed` reported 1/5 with inline `<TerminalDock seed={{...}}/>`; after `const seed = {...}; <TerminalDock seed={seed}/>` it reported 5/5 — both states with all 10 tests passing under vitest.",
    "impact": "Debugging plus a per-test restructure workaround; every .tsx cap author using inline JSX object props hits it, and it likely already understates the sibling terminal-dock-panel / chat-panel coverage. Fix: infer TSX for a .tsx source (name it '__coverage__.tsx' or pass ScriptKind.TSX)."
  },
  {
    "id": "friction-adr-nnnn-placeholder-is-an-overloaded-token",
    "title": "Repo-wide ADR-NNNN substitution collided with specs that use ADR-NNNN as a generic pattern token",
    "statement": "A repo-wide sed of the allocate-late placeholder ADR-NNNN -> ADR-0192 rewrote two specs never edited this session, because ADR-NNNN is also the corpus's generic any-ADR pattern token.",
    "evidence": "2026-07-13 session: the sed patch list included stories/studio/seed-library-corpus.md ('Each ADR-NNNN mention in a definition body adds its resolved doc ref') and stories/website/story.md (a `data-grounds=\"ADR-NNNN\"` example) — neither touched by the session's edits; both spotted in the patch output and reverted in-session before commit.",
    "impact": "Two silent wrong edits in unrelated specs nearly landed; cost one catch-and-revert loop. Any future session that authors spec text against a not-yet-allocated ADR number and patches after allocation hits the same overload unless the substitution is scoped to the session's own edited files or uses a session-unique token."
  },
  {
    "id": "friction-real-build-marks-interactive-session-done",
    "title": "A --real build launched from a session clobbers that session's presence declaration and marks the whole session done",
    "statement": "events.session_event for session clever-chatelet-76014c (2026-07-15): the orchestrator declared nodes=[embedded-terminal] at 13:45 and again at 14:02; each subsequent 'pnpm storytree node build terminal-dock-panel --real --store pg' re-declared the SAME session id with nodes=[terminal-dock-panel] workingOn='real run real-mrm51y12/real-mrm5mc8c' (13:51, 14:07) and, at build completion, emitted a 'done' event flipping the session declaration to status=done (13:58, 14:13). The interactive session was still mid-unit both times: the story wisp went out, the gate's check:declared warned 'session is not on the notice board', and the owner interrupted twice to ask why the wisp had disappeared. The build run deserves its own presence row (or none); it should not adopt and then terminate the launching session's declaration.",
    "evidence": "events.session_event rows (probe 2026-07-16): '13:45:43 declared nodes=[\"embedded-terminal\"]' → '13:51:41 declared nodes=[\"terminal-dock-panel\"] workingOn=real run real-mrm51y12' → '13:58:23 done' → '14:02:11 declared nodes=[\"embedded-terminal\"]' → '14:07:25 declared nodes=[\"terminal-dock-panel\"] workingOn=real run real-mrm5mc8c' → '14:13:41 done'; meanwhile events.node_claim still held the live embedded-terminal orchestrate claim (never released). Gate output: '[check:declared] WARN — session \"clever-chatelet-76014c\" is not on the notice board' while the session was active. Two owner interrupts on 2026-07-15/16 asking why the wisp disappeared.",
    "impact": "The map lies about a working session twice per inner-loop drive (wisp out at each build completion) until the orchestrator manually re-declares; the owner watching the node reads it as a lost session; check:declared warns falsely. Hits every session that routes work through node build/story build --real — i.e. the standard operating discipline."
  },
  {
    "id": "friction-builder-agents-stall-awaiting-background-gate",
    "title": "Spawned builder agents end their turn 'awaiting' a background gate that already needs polling",
    "statement": "A spawned builder that backgrounds its gate run and ends its turn waiting for a completion notification stalls indefinitely; the orchestrator must nudge it with 'read the output file now / act this turn', costing a round-trip per lane.",
    "evidence": "noticeboard-claim-ledger-arc inc 6, 2026-07-17: lanes B, C, and E all stopped in 'awaiting gate notification' state and each needed a SendMessage nudge to read the gate output and proceed (lanes landed as #763/#761/#760 after the nudge). Lanes F/G/H, whose briefs explicitly said 'run the gate foreground / poll in-turn, do not end your turn waiting', did not stall.",
    "impact": "~10–20 min dead time per stalled lane plus an orchestrator attention round-trip; on a 4-lane parallel wave this serialized what should have been parallel landings."
  },
  {
    "id": "studio-dev-server-needs-tsx-loader-bare-vite-500s",
    "title": "Starting the studio with bare vite 500s the whole data API, and the error blames the workspace",
    "statement": "Launching the studio as `npx vite` boots successfully and then 500s every /api route, and the error names a workspace package rather than the launch command — so the obvious diagnosis is a broken install, not a missing loader.",
    "evidence": "Ran `npx vite --port 5199 --strictPort` from apps/studio in a worktree. It served HTML fine, but /api/tree returned 500 with: \"Cannot find module ...\\packages\\orchestrator\\src\\sequence.js imported from ...\\packages\\orchestrator\\src\\index.ts\" — a .js specifier that only resolves under tsx. Because the message names packages/orchestrator, the diagnosis went to the environment: re-ran pnpm install, then `git stash -u` on my own changes and re-probed (the clean tree also returned 500, correctly exonerating the diff but confirming nothing about the cause). The cause was one line of apps/studio/package.json: \"dev\": \"node --import tsx node_modules/vite/bin/vite.js\". Relaunched that way, /api/tree returned 200 on the first try.",
    "impact": "Roughly eight tool calls plus a stash/pop round-trip on a dirty working tree, spent attributing a launch-command error to the worktree install. It bites any session needing the studio on a non-default port or store — both documented invocations (pnpm --filter studio dev, pnpm studio:up) carry the loader, so a port or env override is exactly what tempts you into calling vite directly. The failure is quiet in the worst way: the page loads, so it reads as a data problem rather than a startup problem."
  },
  {
    "id": "new-package-proof-command-passes-vacuously-so-confirm-red-halts",
    "title": "A new-package story's proof command passes vacuously, so the --real chain halts at CONFIRM_RED",
    "statement": "A new-package story's declared proof command cannot go red until the package scaffold exists, so `story build --real` halts at CONFIRM_RED on the first node -- and no leaf can fix it, because per-phase write scope fences every leaf out of package.json.",
    "evidence": "Story `context-traversal-capture` (arc linked-session-context-arc, inc 2), 2026-07-26. `pnpm storytree story build context-traversal-capture --real --max-turns 45` returned: `HALT at CONFIRM_RED -- terminal-boundary-observations failed closed at CONFIRM_RED: CONFIRM_RED requires an observed red (got 'green' for test terminal-boundary-observations)`, with nodes 2-5 `never ran (the run halted earlier; halt is never a pass)`. Reproduced directly: `pnpm --filter @storytree/context-traversal-capture test` printed `No projects matched the filters` and exited **0**. After scaffolding package.json/tsconfig.json/src/index.ts and running `pnpm install`, a throwaway test importing a missing module exited 1, and the same node then passed a genuine AUTHOR_TEST -> CONFIRM_RED -> IMPLEMENT -> CONFIRM_GREEN -> GATE trail.",
    "impact": "Burned a full leaf authoring slice ($0.77 advisory) and one build cycle for zero signed nodes, and cost a diagnosis detour mid-increment. Hits the next session that starts ANY story in a new package -- the failure names CONFIRM_RED, which points at the leaf's authoring rather than at the missing scaffold, so the obvious next move (re-run the build) fails identically. It also survives a good plan: the plan under consumption listed the scaffold inside a capability's file fence, which is unbuildable, because --real fences each leaf to its own real.testFile/real.sourceFile."
  },
  {
    "id": "strict-wire-shape-plus-fail-silent-capture-loses-data-invisibly",
    "title": "A .strict() wire shape behind a fail-silent writer loses data with nothing going red",
    "statement": "Adding one optional field to the leaf's per-model usage split made every usage event fail to persist for two whole --real builds, and nothing anywhere went red — because the wire shape is .strict() and the writer that validates against it is deliberately fail-silent.",
    "evidence": "`packages/proof-protocol/src/usage-event.ts` declares `ModelTokenUsage = TokenUsage.extend({costUsd}).strict()`. Commit 6d20343f added `contextWindow` to `SdkRunInfo.byModel` in `packages/agent/src/sdk-author.ts`. The `build-spawn-capture --real --store pg` build log then carried, twice: `[usage] usage event for build-spawn-capture (AUTHOR_TEST) did not persist: [{\"code\":\"unrecognized_keys\",\"keys\":[\"contextWindow\"],\"path\":[\"byModel\",\"claude-sonnet-5\"],\"message\":\"Unrecognized key(s) in object: 'contextWindow'\"}]`. Both that build and the preceding `leaf-slice-spawn-observations` build returned exit 0 with signed PASS verdicts, `pnpm -r typecheck` clean and the package suites green. `packages/drive/src/usage.test.ts:33` already parsed every mapped doc through `UsageEventDoc` — the guard existed and passed, because its fixture predated the field. Confirmed by reverting the schema fix: 2 failures, same `unrecognized_keys` issue. Fixed in 497e283a.",
    "impact": "Two signed --real verdicts landed with their `events.usage_event` accounting silently absent; the rows are unrecoverable without re-running the builds. Cost roughly 40 minutes to notice — and it was only noticed by reading the build log by eye while chasing an unrelated question, not by any check. The exposure is general, not specific to this field: every `.strict()` shape in `proof-protocol` sits behind a consumer that is fail-silent by design (accounting and capture must never change a build's outcome, ADR-0203 / ADR-0241 D3), so any future additive field on a producer silently switches off the corresponding stream. The next person to widen a producer type hits it, and the signal they need — a parse failure — is deliberately swallowed."
  },
  {
    "id": "task-brief-negative-existence-claim-went-stale",
    "title": "A task brief's \"X does not exist\" premise expired before the session opened it",
    "statement": "This session was briefed to author the Library `process` artifact ADR-0252 D4 mandates, on the stated premise that it \"does not exist\" — a premise sourced from a librarian-curator enumeration that was correct when made. A sibling session landed the artifact in between. The brief's whole mandate had already been discharged, and nothing in the brief could have said so.",
    "evidence": "The brief asserted: \"a librarian-curator pass on 2026-07-27 enumerated all 13 existing `process` artifacts and confirmed none covers verification decay\", and \"the `process` Library artifact ADR-0252 D4 MANDATES does not exist\". At session start, `pnpm storytree library artifact list process --pg` returned **15**, including `verification-decay-detection`; `pnpm storytree library artifact verification-decay-detection --pg` rendered a complete, D4-faithful artifact. The offline seed still lists 13 (`pnpm storytree library artifact list process` — the artifact is live-only, so a seed-based check would have agreed with the stale brief). The arc's own increment log already carried the landing entry, and separately carries a correction entry documenting the same parallel-session collision producing two entries both headed \"Increment 2\".",
    "impact": "~20 minutes of re-orientation, and a near-miss on the expensive failure: authoring a duplicate `process` artifact into a tier whose whole point is being a non-duplicated projection of the decision log — caught only because the live store was queried before writing rather than after. The offline read would NOT have caught it. Next hit: any session handed a scoped brief on an arc with concurrent siblings — which on this repo is the normal case, not the exception."
  },
  {
    "id": "check-coverage-counts-a-test-name-not-a-contracts-clauses",
    "title": "check:coverage counts a test NAME, so a multi-clause contract ships at N/N with most clauses unasserted",
    "statement": "`check:coverage` maps a contract id to a test TITLE, so a leaf can satisfy a multi-clause contract by asserting any one of its clauses and the capability still reads N/N — the unasserted clauses are indistinguishable from proven ones, in a signed verdict.",
    "evidence": "Capability `agent-ref-descent` (increment 11 of linked-session-context-arc) signed a --real PASS at 4658f34 reading `coverage 7/7 contracts`. Its contract 4 (`each-resolved-ref-becomes-a-front-matter-child-naming-the-agent-visit-as-parent`, stories/context-traversal-capture/agent-ref-descent.md) declares five things: the parent comes back FIRST, still `full_payload_read`, carrying NO `parentVisitId` key on a JSON round-trip; and the children carry the agent visit's `surfaceId`. The authored test (packages/context-traversal-capture/src/descend-agent-refs.test.ts, as committed at 4658f34) asserted only `visitId`, `kind`, `nodeId`, `sessionId` and `parentVisitId` on the children. `grep -n 'surfaceId' descend-agent-refs.test.ts` returned only two fixture lines and zero assertions; `grep -n 'JSON.parse'` returned zero. The `surfaceId` half was also UNIMPLEMENTED in descend-agent-refs.ts, so every child rendered as `surface=unknown-surface` in the live CLI. Falsifying the added assertion against the pre-consolidation implementation failed with `actual: undefined, expected: 'agents'` — i.e. the clause had never been exercised.",
    "impact": "A signed verdict over-claims: 7/7 read as 'every contract traceable to an assertion' when contract 4 was two-fifths asserted and one-fifth wrong in the shipped code. Caught only by reading each clause against its test by hand. This is ONE RUNG BEYOND the arc's existing `leaf-casts-away-the-type-it-cannot-see` (#960) — there a cast hollowed a test the count still credited; here nothing is cast, the test simply asserts a subset, and the count is equally blind. It hits every future capability whose contracts are written as multi-clause sentences, which in this arc is most of them; the same shape already shipped contract 8 unbuilt twice (#935) and left contract 5's non-accumulation half unasserted."
  },
  {
    "id": "claim-absence-surfaces-only-after-the-gates-expensive-legs",
    "title": "A claim that goes absent mid-session surfaces only after the gate's expensive legs",
    "statement": "A live claim that the gate verified OK can be absent two hours later in the same session with no merge in between, and because check:declared sits after `pnpm -r typecheck` and `pnpm -r test`, re-lighting it costs a whole gate re-run rather than ten seconds.",
    "evidence": "In one session on branch claude/silly-morse-db9dd9: a `pnpm gate` run printed `[check:declared] OK — session \"amazing-visvesvaraya-1ec29e\" holds 1 live claim(s): storage-protocol (work).` A later full run in the same session printed `[check:declared] FAIL — session \"amazing-visvesvaraya-1ec29e\" holds NO live claim: an unclaimed session cannot reach the merge ceremony (ADR-0200 D3).` Nothing had merged in between — the branch was still unpushed and `git log --oneline origin/main..HEAD` showed a single local commit. The DB was RUNNABLE at the time of the FAIL (`pnpm db:status` -> `RUNNABLE ALWAYS`), so this was not the offline SKIP path. Re-running `pnpm storytree noticeboard declare --working-on ... --node storage-protocol --pg` re-lit the claim in about ten seconds and the next gate passed. The cause of the disappearance was not established; only the disappearance itself was observed.",
    "impact": "One wasted full gate run (about twelve minutes) at the very end of a session, after check:manifest, check:boundaries, `pnpm -r typecheck` and every package's tests had already passed. The corrective action itself takes about ten seconds. It lands hardest on long sessions, which are exactly the ones where a re-run is most expensive, and it is invisible until the second-to-last step of the gate."
  },
  {
    "id": "friction-unfenced-cross-surface-mirrors",
    "title": "Only one cross-surface mirror is fenced; the rest drift until a human sees wrong pixels",
    "statement": "The desktop re-composes the studio's read routes by design (ADR-0100/ADR-0176), and check:mirror-conformance exists to fence exactly the drift that invites — but it registers only the GET /api/docs mirror, so the /api/activity claims mirror drifted unnoticed until a human squinted at the map and asked why two wisps orbited one story.",
    "evidence": "apps/desktop/electron/backend-entry.ts selected `unit_id, session_id, branch, intent, claimed_at, heartbeat_at` while apps/studio/server/libraryBackend.ts:961 selected `grade` as well (added at ADR-0200 D7). The running sidecar on port 60593 returned claims with no `grade` key at all, so the frontend's `c.grade ?? 'work'` default made every exploring/waiting claim orbit the whole island. Fixed in PR #993. packages/cli/src/check-mirror-conformance.ts registers ONE mirror (GET /api/docs); the same desktop handler also omits `departures` from /api/activity, which the studio serves and which is still unfixed.",
    "impact": "The desktop map contradicted the ADR-0200 D2 mutex it exists to visualise — two sessions on one story read as two work holders — and the defect was found by an owner reading pixels, not by the gate. Roughly an hour of session time to diagnose from a screenshot plus a live ledger read. The next re-composed route to gain a field fails the same way, silently; the `departures` gap is already sitting in the same file."
  },
  {
    "id": "friction-queue-has-no-claim-so-adjudicator-seats-race",
    "title": "The friction queue has no claim, so two adjudicator seats drained it concurrently",
    "statement": "Friction routing has no claim or mutex of any kind, and the noticeboard claims a STORY rather than the queue, so two adjudicator seats drained the same friction items in parallel without either being refused or even warned.",
    "evidence": "An owner brief on 2026-07-28 named 5 open unrouted items and directed this session to adjudicate them. At session start `storytree friction list --pg` showed 7 open, not 5. A sibling session on branch `claude/clever-pike-8efb63` (holding a `cli [work]` claim on the ledger) had already routed `an-unmeasured-risk-in-a-checks-own-prose-deferred-the-highest-value-work-for-three-increments` and `differential-control-harness-fails-uniformly-and-reads-as-a-valid-measurement` before this session finished reading them, and took `friction-capture-surface-is-itself-high-friction`, `a-false-positive-clause-that-names-instances-reads-as-a-verdict-on-them` and `convenience-wrapper-hides-the-substrate-health-its-caller-must-guard-on` while the graduation-synthesist was mid-adjudication -- the last of those during the write of a different item. Net: one full synthesist run (171,395 subagent tokens, 30 tool uses, ~86 minutes wall clock) produced routes for 2 of the 5 briefed items. Nothing refused, queued or flagged any of it: `storytree friction route` performs a bare upsert of `route` and `routeReason` with no claim check, and this session HAD declared on the ledger (`noticeboard declare --node feedback-graduation --pg`, claim taken and confirmed), which fenced nothing because ADR-0200 grades claims per story node and the friction queue is not a story.",
    "impact": "The measured cost here was one duplicated deep-verification pass, most of a synthesist budget spent on items already adjudicated. The larger exposure is silent: two adjudicators can route the SAME item with DIFFERENT routes and the second write simply overwrites the first, taking its justification with it, with no conflict surfaced at either seat -- so a peer's reasoning can be destroyed without either session learning it happened. This session avoided that only by re-checking the open set immediately before every write, which is a hand discipline the tool does not require and nothing would have caught had it been skipped. Who hits it next: any two sessions whose librarian passes overlap, which the merge ceremony makes routine rather than exceptional, since every session runs a librarian pass with a bounded friction drain before merging."
  },
  {
    "id": "corpus-content-gate-red-on-sibling-mid-stream-live-edit",
    "title": "The V=0 corpus-content ceiling turns MY gate red on a SIBLING's mid-stream live edit",
    "statement": "check:corpus-content's V=0 ceiling scores ALL live drift against the session running the gate, so a sibling session editing artifacts live mid-stream (its normal ceremony) turns my gate red on artifacts I never touched.",
    "evidence": "2026-07-30, branch claude/jolly-villani-e1d387: 06:36Z export-corpus --write captured 4 artifacts (2 mine to carry, 2 the cleanup session's: db-control, probe-dont-assume-db-reachability — it had ALSO edited those since my 06:20Z dry-run showed only 2 pending). ~06:40Z `pnpm gate` RED: `[check:corpus-content] RED — 1 value-drift: db-control` — the sibling had edited db-control AGAIN post-export. ~06:44Z dry-run showed 0/0 (the sibling's edit had converged/reverted); the re-gate went green with no action of mine beyond re-running.",
    "impact": "One wasted full gate run (~4 min) plus a re-export cycle, and a class risk: any two sessions where one edits seed-scope artifacts live make the OTHER's landing a race — the ceiling is per-repo, not per-author, so the more the live-edit ceremony is followed, the more sibling gates flake. Next hit: any session landing while a corpus-editing session runs."
  },
  {
    "id": "graduate-park-reason-ignores-at-path-and-overwrites-the-prior-verdict",
    "title": "graduate park --reason ignores the @path convention and overwrites the prior verdict with the literal path",
    "statement": "`storytree library graduate park <name> --reason @path` does not resolve the house `@path` convention — it stores the literal path string as the verdict — and because the ledger write replaces the record wholesale, the prior verdict prose is destroyed in the same call.",
    "evidence": "Hit 2026-07-31 while re-parking `worktree-reaper-silent-noop` after chairing a graduation call: the ledger record's `reason` came back as the literal 163-character path instead of the verdict, and the previous ~4.5k-character verdict was gone; recovered only because the agent read the record back, and restored via the batch `--file` form. Root cause read in code, not inferred: `packages/cli/src/commands.ts:2783` builds the park item with `reason: values.reason` — raw — while the same file resolves the convention at `:2658-2659` for `friction route`/`reinforce` (`reason = await resolveAtPathValue(values.reason)`) and at `:2380-2383` for `arc` (`intent`/`end-state`/`outcome`/`description`). `resolveAtPathValue` is defined at `packages/cli/src/commands.ts:465`. The destructive half is `packages/cli/src/graduate.ts` in `parkCommand`: `parks[item.name] = record` replaces the record outright, labelling it only `(refreshed)`. The same function already refuses to write when the ledger FILE does not parse, on the stated grounds that `overwriting it would silently drop recorded verdicts` — so the code protects the file-level case and leaves the record-level case unguarded. `storytree friction --help` documents the convention explicitly (`--reason and --evidence take @path ...`); `graduate park`'s help does not mention it either way.",
    "impact": "Cost this session a destroyed park verdict plus the read-back, re-author and restore to recover it; the ledger is machine-local and untracked by git, so nothing else could have recovered it. Whoever hits it next is exactly the agent the ADR-0202 ledger exists for — a librarian-curator or graduation-synthesist recording a long, reasoned park verdict, which is precisely the case that needs `@path` because a multi-line verdict cannot survive a shell argument. The failure is silent on the way in: the command reports success and prints `parked (refreshed)`, so an agent that does not read the record back keeps a ledger entry whose verdict is a path."
  },
  {
    "id": "a-security-boundary-sourced-from-a-mutable-checkout-is-only-as-current-as-its-branch",
    "title": "A hook registered by absolute path runs whatever branch its host checkout is sitting on",
    "statement": "Registering an enforcement hook by absolute path binds the machine to whatever code that checkout's current branch happens to hold, so the wall's implementation is decided by an unrelated branch choice made by someone else.",
    "evidence": "ADR-0257 increment 3 (2026-08-02). `storytree write-authority install` computed `node C:/code/storytree/packages/cli/write-authority-hook.mjs --root C:/code/storytree`; `ls` returned 'No such file or directory' because the primary checkout sat on `claude/act2-intro-decisions`, whose merge-base predated PR #1072. A failing PreToolUse hook does not block, so the registration would have reported an installed wall while enforcing nothing. The alternative host on the machine, `C:/code/storytree-runtime`, carried #1072 but a PRE-FLIP hook — inert unless an env var is set. Neither is a durable answer: a copy outside a checkout cannot resolve `tsx` and therefore denies every write.",
    "impact": "The flip could not complete on the day it landed: the static half is in force, the semantic half needs one command re-run once a durable checkout carries the code. Mitigated in this increment by verifying the host script exists AND is post-flip before registering, removing a stale registration rather than leaving it, and adding `--hook-from`. Next hit: anyone installing machine-scoped enforcement that must be sourced from a workspace with node_modules."
  },
  {
    "id": "scratchpad-one-shot-scripts-cannot-resolve-workspace-packages",
    "title": "A one-shot script in the scratchpad cannot import workspace packages, and fails twice before saying so",
    "statement": "The house incantation for a one-shot live-store script — `pnpm --filter @storytree/cli exec node --import tsx <script>` — silently assumes the script lives INSIDE the workspace: Node resolves both the ESM/CJS decision and the `@storytree/*` specifiers from the script's own directory, so the same command run against a scratchpad script fails twice for two unrelated reasons before either cause is legible.",
    "evidence": "2026-08-03, branch claude/sad-borg-39ce40, reconciling one artifact's seed entry from live. Attempt 1, `scratchpad/reconcile-seed.ts`: `Error: Transform failed with 4 errors: ... ERROR: Top-level await is currently not supported with the \"cjs\" output format` — a `.ts` file outside any `\"type\": \"module\"` package is transformed as CJS regardless of the `--filter` target. Attempt 2, after renaming to `.mts`: `ERR_MODULE_NOT_FOUND ... at resolveTsPathsSync (tsx/dist/register-CqMfTiWi.mjs)` on `@storytree/library/store`, because resolution walks up from the SCRATCHPAD, which has no node_modules. Both attempts exited 1 through `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`, which names the command but neither cause. Worked around by dropping the live fetch entirely and doing pure JSON surgery (no workspace imports), then verifying with `storytree library export-corpus --pg` — the artifact dropped out of the update list, which is the tool's own word that seed matched live.",
    "impact": "Roughly 10 minutes and two dead ends. The pull is toward the two obvious-but-wrong fixes: writing the script into the repo (which then has to be remembered and deleted, and can ride a commit), or abandoning the live read. The cheap correct move — restructure so the one-shot needs no workspace import, and verify the result with a CLI command that already has resolution — is invisible from either error message. Hits any session following the documented incantation for a scratchpad script, which is the scratchpad's whole advertised purpose."
  },
  {
    "id": "comparison-sheet-carries-no-code-state",
    "title": "A fork picture can silently compare two variables — a comparison sheet records no code state per cell",
    "statement": "This arc answers art-direction forks with rendered pictures, and ADR-0293 D3 makes that the house move: where a boundary sits is 'an art-direction choice with a rendered answer'. The variants live in per-flag output dirs and sheet.py composes them into one committed artifact. Nothing ties a cell to the code state that produced it — render-meta.json IS written per raw dir and does record the flags, but sheet.py never reads it, the delivered PNG dir does not carry it, and no check compares cells. A sheet built from dirs rendered either side of an edit is therefore indistinguishable from one built in a single pass, and the failure is silent in both the picture and any table derived from the same dirs. It is cheap to hit, because exploring a fork across an edit is the normal way a fork gets explored, and expensive to detect later, because the variant dirs are gitignored and reproducible-but-not-reproduced.",
    "evidence": "This session, caught by recall rather than by any instrument. work/m00, m032, m045 and m10 were rendered before CANOPY_CORE_LIFT (the funnel canopy floor) existed; work/m022 was rendered after it. sheet.py composed all five into crown-normals-fork.png with no error and no visible cue, so the committed fork picture varied two levers while its entire purpose was to isolate one. The same stale dirs had already been measured into a table committed in blender_tree.py's source under the caption 'with the funnel floor below in place', which was false for four of its five rows when written. Re-rendering the four variants moved their bark counts 185->206 and changed the 0.00 row's cap sizes from [174,170,147,106,73] to [177,168,148,103,72].",
    "impact": "Two committed artifacts — an owner-facing fork picture and a source measurement table, both of them the evidence an owner is asked to decide a fork from — stated a one-variable comparison that was a two-variable one. Detection depended entirely on the author recalling the order of about twelve renders spread over an hour; had it held, the delivered default would have been picked against a lever it was not actually isolating."
  },
  {
    "id": "arc-parked-work-renders-behind-the-whole-history",
    "title": "An arc's forward-looking Parked work renders behind its entire increment log",
    "statement": "The one section of an arc a session needs in order to START work — the parked entries — is rendered last, behind every landing the arc has ever recorded, so on a mature arc it is unreachable without knowing in advance how far to scroll, and any output truncation silently removes it while leaving the render looking complete.",
    "evidence": "`storytree arc show verification-integrity-arc --pg` on 2026-08-04 emits 1069 lines; `## Parked work  (6 parked, 1 realized)` is at line 998, behind 31 multi-paragraph increment entries. This session truncated the read at 200 lines, then grepped the captured output case-insensitively for 'parked' and 'authorship', got ZERO hits on both, and concluded the two entries it had been directed to read did not exist on this arc — going on to `arc list --pg` and a raw store dump to look for them elsewhere. They were present the whole time. `renderArcRollup` (packages/cli/src/arc.ts:183-206) fixes the order: increments, then parked work.",
    "impact": "A false 'this does not exist' on the arc's only forward-looking content, and the pattern gets worse monotonically — an arc's history only grows, so every landing pushes the actionable section further out of reach. `verification-integrity-arc` is the most-worked arc in the repo at 31 increments, which makes it the one most likely to be read this way. The class is the same one the increment/parked split was designed to prevent (ADR-0298 D4 separates them so unbuilt intentions are not read as things that happened); ordering re-introduces it as an accessibility problem rather than a legibility one."
  },
  {
    "id": "friction-real-build-author-test-turn-ceiling-tiny-unit",
    "title": "Real Build exhausts AUTHOR_TEST turns on a tiny two-file unit",
    "statement": "`storytree node build --real` exhausted the AUTHOR_TEST phase twice on the two-file library DAG detector unit instead of reaching a promotable red-to-green patch.",
    "evidence": "Runs `real-msg3634o` and `real-msg3fcz2` each halted at AUTHOR_TEST after the maximum 16 turns and promoted no patch. The same bounded scope (`packages/library/src/knowledge-dag.ts` plus `knowledge-dag.test.ts`) was then proved through a scoped worker: RED was `ERR_MODULE_NOT_FOUND` for `./knowledge-dag.js`, GREEN was 3/3 focused tests, and the package suite/typecheck passed.",
    "impact": "Cost about 12 minutes and two real leaf attempts, then forced the session to leave the normal leaf/spine route and supplement the unit with a scoped worker despite the unit having an isolatable red-to-green proof."
  },
  {
    "id": "camera-probe-non-accretion-proxy-conflated-stable-picture",
    "title": "The camera probe's non-accretion proxy conflated stable and changing pictures",
    "statement": "The production camera benchmark treated growthNodeCount === 0 as an unchanged picture, but that condition excludes only accretion and does not exclude path or vegetation updates.",
    "evidence": "`pnpm --filter studio measure:camera-rasterisation` exited 1 with `probe failed closed` under protocol 4 after growthNodeCount === 0 admitted frames whose other visual inputs changed. The corrected `docs/research/act2-camera-frame-delivery-2026-08-06/camera-rasterisation.json` records protocol 5, 8 accepted / 0 rejected runs, and 654 control / 461 optimized stable-picture samples in the 20k+ bucket.",
    "impact": "The misleading predicate consumed two production-shaped 40-island reruns and could have signed a performance comparison over visually changing frames; the next renderer probe using a partial activity counter could repeat the same error."
  },
  {
    "id": "a-parked-entrys-comparative-steers-target-choice",
    "title": "A parked entry's unverified comparative steers the next session's target choice",
    "statement": "A parked increment's body can carry a COMPARATIVE that steers the consuming session's target choice — and unlike the remedy prose, nothing prompts anyone to check it, at authoring time or at consumption.",
    "evidence": "Measured 2026-08-08 on branch claude/optimistic-shannon-21f737, driving `capability-layer-coverage-arc-inc-06`.\n\nThe parked body of `capability-layer-coverage-arc-inc-06` records an ALTERNATIVE target and instructs: \"If the arc organ turns out to need a new story too, prefer doing the session-lifecycle one first, since its evidence is older and doubled.\" The arc organ DID turn out to need a new story, so the clause fired and the session had to weigh it.\n\nBOTH HALVES ARE FALSE, and both were checkable against two artifacts already in the store when increment 6 was authored:\n\n1. NOT OLDER. `storytree library artifact capability-layer-coverage-arc-inc-01 --pg` lists FOUR deferrals, and BOTH candidates are among them: `packages/drive/src/arc-rollup.ts` (\"its home story is a genuine open question\") and `packages/drive/src/write-authority-rules.ts` (\"NO story for the session-isolation wall exists anywhere in the tree\"). Same increment, same landing — 2026-08-07, PR #1199. They are exactly the same age.\n\n2. NOT UNIQUELY DOUBLED. `… -inc-05 --pg` re-defers BOTH: the worktree block (\"the SECOND increment to defer here\") and the arc organ (\"BEST-VALUE NEXT TARGET\"). Both are doubled, so \"doubled\" discriminates nothing.\n\nThe underlying characterisation was also wrong, which is what made the comparative costly rather than merely idle. Increment 5 calls the alternative \"one coherent SESSION-LIFECYCLE organ (~99 tests, two real-git integration suites)\" and \"Two independent deferrals onto ONE missing story\". Measured independently twice this session: the import graph is three-to-four DISJOINT clusters — `packages/cli/src/branch.ts` has zero edges to any of the other five files in either direction, and `worktree-create.ts` has none to the reaper pair — so they meet only at `commands.ts`, the shared dispatcher. The decision log agrees: no ADR ties the write-authority chain (ADR-0255 -> 0257 -> 0284) to the worktree-lifecycle chain (ADR-0031/0033/0142/0200/0220), and ADR-0284 D4 de-scoped worktree-to-worktree isolation by decision. The test figure is reconstructible only under one narrow reading (98, off by one from \"~99\"); the deferred six-file surface is 188, and the `worktree*.ts` glob alone is 140 — 113 net of `worktree-health.test.ts`, whose 27 tests import `packages/cli/worktree-health.mjs`, a file outside `src/` and outside the `.ts` filter.\n\nCost to resolve: two subagent measurement passes (~370k subagent tokens) spent establishing that the stated ground for the recommendation did not exist.",
    "impact": "The cost lands on the consuming session and is paid BEFORE any work starts, because the clause governs WHICH WORK TO DO. A session that trusts it silently takes the wrong target; a session that checks it pays the measurement. Here it also pointed at a target that would have been actively wrong to take as described — a single new story over four disjoint clusters is the bucket the arc's own charter forbids.\n\nThis is a different failure from the ROUTINE prose in a parked body. `increment check` exists for exactly this tier and cannot reach it: the mechanical freshness check compares an `anchor.sha` against the paths the increment names, so it detects code moving under a remedy. A comparative about two OTHER artifacts' provenance names no path, moves no code, and was wrong on the day it was written rather than overtaken afterwards.\n\nWho hits it next: any session consuming a parked increment that recommends between targets. The arc-tier increments are authored precisely to be consumed later by a session holding none of the authoring context, which is what makes an unverified comparative in them read as a finding.\n\nFiled 2026-08-08 from the session that drove capability-layer-coverage-arc increment 6.\n\nNOT A FOLD into `a-parked-entrys-premise-can-be-overtaken-with-no-freshness-check`, and the distinction is the adjudicator's to overturn if it disagrees. That item is about a parked entry's premise being OVERTAKEN BY A LATER DECISION — its four instances all name a symbol or a consumer that a subsequent ADR deleted, and its remedy space is a freshness check. This item's comparative was never true at any point: both deferrals were already in the same increment on the day increment 6 was authored, so no freshness check of any kind would fire on it. Same tier, opposite mechanism — staleness versus unverified-at-authoring — and therefore a different remedy space. Recorded rather than reinforced so the adjudicator can fold them deliberately if it judges them one class; reinforcing would have filed this evidence under a mechanism it does not exhibit.\n\nRelated by CLASS rather than by tier: this arc has a measured five-for-five record of shipping a defective first draft whose defect is in the SUMMARY CLAIM rather than the mechanism (named in increment 4's outcome, made a standing pre-merge review commitment in increment 5). Every prior instance was caught in a MANIFEST FENCE draft before landing. This is the first instance to land in a PARKED INCREMENT BODY, where it is consumed as direction rather than read as a rule."
  },
  {
    "id": "chip-carried-increment-is-invisible-to-arc-lifecycle",
    "title": "Work carried only by a chip is invisible to the mechanical arc lifecycle, so an arc auto-closes with its end state unmet",
    "statement": "ADR-0335 recomputes an arc's lifecycle from its increment log alone, so remaining work that was chipped rather than parked as an increment is invisible to the rule — the arc auto-closes and reads as finished, while its own body still names the work as outstanding.",
    "evidence": "`storytree arc show uat-journey-surgery-arc --pg` on 2026-08-11 rendered:\n\n- header: `lifecycle: closed — its end state was met; it is out of the default arc list`\n- Work section: `(nothing open — every increment on this arc is closed)`\n- and, in the SAME render, its own end-state prose: \"Two increments remain, each still carried by its chip: task_99f7e0a9 (ADR-0294 D3 — appearance verdicts to capabilities) and task_47c74cb0 (desktop/terminal + the remainder + the ~13 live human legs, including the check-minting judgement in point 4).\"\n\nThe closure claim is also falsifiable against the corpus. The arc's end state point 5 is \"the surviving human legs are roughly six genuine owner calls\". Measured leg-by-leg on origin/main @ 984fd554: 31 story-tier `_(witness: human)_` criteria across 10 stories, of which 17 are genuine taste — off by a factor of five.\n\nThis is not the ADR-0347 case (arc close refusing over OPEN increments): these increments were never parked, so there were no open increments for that rule to refuse over. It is also not the known `arc increment add` auto-close trap. The gap is specifically chip-vs-increment.",
    "impact": "The arc dropped out of the default `storytree arc list` worklist while carrying two unfinished increments, so the work was discoverable only by reading the closed arc's body or by finding the chips in the picker. It sat that way from 2026-08-08 to 2026-08-11.\n\nCost this session: the arc had to be diagnosed as wrongly-closed and reopened with `arc reopen --reason` before any work could be parked on it, and the three increments this session parked had to be re-derived from the arc's prose rather than read off its Work section.\n\nWho hits it next: any arc whose remaining work was chipped rather than parked — which the closing leg actively encourages, since ADR-0288's worth-a-session bar is applied to CHIPS while ADR-0183's residue rule parks INCREMENTS, and nothing reconciles the two."
  },
  {
    "id": "check-agents-stale-masks-an-unfixable-budget-red",
    "title": "check:agents reports \"stale\" when regenerating cannot fix it, hiding an essentials-budget violation behind a routine-looking red",
    "statement": "`check:agents --check` reports stale harness projections without saying whether regenerating them would actually produce a passing result. When a sibling's live agent edit pushes a rendered agent past the ADR-0156 essentials budget, the check still reports only `stale: <file>` — the routine sibling-race signature, whose documented remedy is `pnpm build:agents` + commit. Running that remedy replaces the staleness red with a DIFFERENT red (`essentials gate FAILED — over the 6000-token budget`), which no amount of regenerating clears. The check knows both facts at the moment it reports the first one: it renders the agent in order to compare it, so it could say `stale, and regenerating would exceed the essentials budget by N tokens — the fix belongs to whoever holds the live edit`.",
    "evidence": "2026-08-12, branch `worktree-uat-human-basis` (PR #1302). Gate run 1: `check:guidance` + `check:agents` FAIL, the latter naming 4 stale `librarian-curator` projections — the exact documented sibling-live-edit signature. Merged `origin/main` (still red), ran the recorded converse test (`git diff --quiet origin/main HEAD -- CLAUDE.md AGENTS.md .claude/agents/librarian-curator.md` -> exit 0), which says main is exactly as stale and carrying the regeneration is correct. Regenerated: `check:guidance` PASSED, and `check:agents` then failed with a wholly different cause — `librarian-curator.md is ~6009 tokens (est.), over the 6000-token essentials budget` across all five harnesses, caused by a sibling's in-flight ADR-0358 paragraph inlined into the agent's step 5. Reverted. Net cost: two regenerate/revert cycles plus a reversed decision that had already been reported to the owner, to learn a fact the first check had in hand.",
    "impact": "The documented remedy for the common case is actively wrong for this case, and the check gives no signal to tell them apart. A session following the recorded guidance regenerates, sees a new red, and must work out from scratch whether to carry it — or worse, commits an over-budget projection onto main because it looks like progress (one red became one different red). It also blocks every branch's gate: check:agents is red repo-wide until the live-edit holder trims the prose, and no other session can fix it, but nothing in the failure says so."
  },
  {
    "id": "adr-prescribed-build-address-outlived-by-later-infrastructure-adr",
    "title": "An ADR that prescribes a BUILD ADDRESS is falsified by later infrastructure ADRs, and the curator pass does not catch it",
    "statement": "An accepted ADR that prescribes an IMPLEMENTATION ADDRESS — a specific file, gate leg, script or test suite the work must land in — can be silently falsified by a later infrastructure ADR, and neither the trigger-gated librarian pass nor any gate rung detects the falsification, so the cost lands on whoever eventually builds it.",
    "evidence": "ADR-0223 D3 (2026-07-21, accepted, `amends: [185]`) instructed: \"A new corpus-guard check (`check:library-dag-acyclic`, sibling to `adr-number-unique`) fails `pnpm -r test` on any cycle in `standsOn`.\" That address depended on the corpus being a committed file a hermetic test could read. ADR-0302 D1 (2026-08-04) deleted `apps/studio/data/knowledge.json` and ADR-0302 D3 pinned `pnpm -r test` credential-free; ADR-0307 D4 then stated the replacement rule explicitly — \"assertions about the REAL corpus belong on a `check:*` rung, which may hold a connection.\" No edge, tag or check connected the two: `storytree adr list --load-bearing` renders ADR-0223 green, and ADR-0302 carries no `amends: [223]`. The falsification is visible only by reading both bodies and noticing that `adr-number-unique` survives because ADRs are still FILES while the Library corpus is not. This session (driving increment `standson-schema-and-acyclicity-gate`) had to re-derive that from ADR-0302/ADR-0307 before it could place a single line of the check, and then correct ADR-0223 D3 in place under ADR-0139.",
    "impact": "~15 minutes of this session spent establishing that the ADR's own build instruction could not be followed, plus the standing risk that a less careful session follows it literally — producing a corpus-reading test inside `pnpm -r test` that reds on every DB-less checkout and breaks ADR-0302 D3's credential-free guarantee for the whole leg. ADR-0324's Consequences anticipated the inverse miss (\"code that falsifies an ADR's prose without touching a curated path\"); this is the sharper case, because ADR-0302's branch DID touch `docs/decisions/**` so the librarian trigger fired — and the pass still missed it, since the falsification is semantic and cross-ADR rather than local to any one file. Whoever builds a deferred increment off an ADR older than the infrastructure it names hits this next."
  },
  {
    "id": "piping-gate-bg-defeats-its-detach-and-kills-the-run",
    "title": "Piping `pnpm gate:bg` into a shell filter defeats the detach and SIGTERMs the whole gate",
    "statement": "`pnpm gate:bg` returns the launcher's exit 0 immediately ONLY when nothing holds its stdout open. Piping it (`pnpm gate:bg 2>&1 | Select-Object -Last 12`, or any `| tail`) makes the caller wait for stream close, so the tool's timeout fires and SIGTERMs the pipeline — killing the detached gate mid-run. The failure is silent and easy to misread: the log simply stops growing, which is indistinguishable from the documented and expected 'a gate step at ~0 CPU is STARVED by sibling gates, not wedged'. Node processes from OTHER sessions' gates keep showing fresh CPU, which actively confirms the wrong diagnosis.",
    "evidence": "2026-08-14, worktree agent-aa7ad274ae857baa8. `pnpm gate:bg 2>&1 | Select-Object -Last 12` returned 'Exit code 143' (SIGTERM) after the 5m tool timeout. `.gate-logs/gate-20260814-221954-23305.log` froze at 657 lines with its last written step `[7/12] PASS: pnpm -r --no-bail typecheck` and mtime 22:24:45, while `.gate-logs/last-run.json` recorded the abort at 22:30:03. I read it as starvation and waited: `Get-Process node` showed 40 processes with fresh CPU (PIDs started 22:52), which looked like progress and was other sessions. Confirmed dead only at 23:01 by comparing log mtime (22:24) against wall clock. Re-launching via the agent tool's own `run_in_background` (no pipe) produced a run that completed all 12 steps.",
    "impact": "~35 minutes of wall clock waiting on a process that had already been dead for 30 of them, plus a wasted 40-minute gate run that had to be repeated from scratch. Recurs for any session that reaches for a pipe to trim gate:bg's launcher banner — which is the natural thing to do, since the launcher prints several lines nobody needs."
  },
  {
    "id": "branch-diff-fence-tests-the-branch-not-the-promise",
    "title": "A 'this file is untouched in the branch diff' fence tests the branch, not the promise",
    "statement": "Encoding 'this pass did not edit shared file X' as 'X is not in this branch's diff against origin/main' asks the intended question only while that pass's branch IS the branch — afterwards it reports a broken promise about someone else's work.",
    "evidence": "`docs/research/chapter2-greenery-techniques-2026-08-17/verify.py:128` ran `git diff --name-only <merge-base> -- <path>` over three files it promised not to edit. On this branch — which legitimately moved the plant positioner into `docs/research/chapter2-grass-reads-as-signal-2026-08-16/scatter.py` — it failed 35/36 with `scatter.py is untouched: docs/research/chapter2-grass-reads-as-signal-2026-08-16/scatter.py`, reporting a broken promise where there was none. The same form sat in `chapter2-high-frequency-options-2026-08-17/verify.py:79` as `scatter.py is not in this branch's diff`, and that pass's neighbouring check (`scatter.py still carries the affine-CRC32 draw, UNEDITED`) would have stayed GREEN while asserting something no longer true, because the legacy draw is deliberately retained behind a named branch and the literal substring still matched.",
    "impact": "Two red/misleading checks and two re-authorings this session. It recurs on schedule: the next increment on this arc (`shrubs-replace-long-grass-and-inherit-the-test-count`) edits `blender_species.py` by its own description, which is the second file on that same fence. The worse half is the silent one — a substring-matching 'still unedited' assertion that keeps passing after the thing it describes has changed shape."
  },
  {
    "id": "no-cross-kind-corpus-text-search-so-absence-is-unprovable",
    "title": "'Does any artifact still assert X?' is not mechanically answerable - query needs a kind AND a field path",
    "statement": "There is no cross-kind text search over the corpus, so a session correcting overtaken prose cannot mechanically prove the absence it is claiming and has to substitute a proxy for the sweep it wanted.",
    "evidence": "`storytree library query` requires `--kind <k>` and a dotted field path for its `~` contains operator (`storytree library query --help`: 'field~value  contains (substring, any case)'), and neither `storytree library --help` nor the query help offers a kind-agnostic or field-agnostic text search. Sweeping for a retracted claim therefore costs one process per (kind, field) pair. The attempt this session - 11 kinds x 10 plausible body fields, each a `pnpm storytree library query ... --count` - was killed at the 2-minute tool timeout having covered a fraction of the grid: 'Exit code 143 / Command timed out after 2m 0s'. Each invocation pays full node startup plus a live-store connection.",
    "impact": "I could not answer 'does any library artifact still assert the chip freeze?' directly, and substituted a proxy: check:guidance and check:agents compare the committed projections against the live store, so a clean projection proves a clean AGENT artifact. That proxy happened to be sound and cheap here, but it only covers the projected kind - definition, process, pattern, principle and guardrail artifacts reach sessions by pull, not by projection, and nothing mechanical covers them. I read session-orchestrator, session-cutting and merge-ceremony by hand instead and stopped, which is a spot check dressed as a sweep. Whoever runs the next ADR-0139 correct-in-place pass hits this identically: the operation is defined as 'every accepted ADR true in full', and the corpus half of that is currently unfalsifiable at reasonable cost."
  },
  {
    "id": "opaque-pixel-floor-cannot-see-a-prop-that-stopped-drawing",
    "title": "The live-render harness's non-vacuity floor counts OPAQUE pixels, so it cannot see a prop that stopped drawing",
    "statement": "`capture.mjs`'s non-vacuity guard counts OPAQUE PIXELS, and every prop on the island is drawn over ground that is already opaque, so no change to a prop — including its total disappearance — can move the number the guard reads.",
    "evidence": "Two consecutive capture runs against `http://localhost:5193/island.html`, either side of a real geometry change (`FAILING_HEAD_TILT_DEG` 118 -> 105 in `packages/forest-world-r3f/harness/flower-geometry.ts`, which visibly re-poses every wilted head on three panels), both reported `opaque px  : 11250412` — identical to the digit, and `distinct   : 44` unchanged. Verified the change WAS live rather than cached: `curl -s http://localhost:5193/flower-geometry.ts | grep FAILING_HEAD_TILT_DEG` returned `const FAILING_HEAD_TILT_DEG = 105;`, and the re-captured `panel-verdict-forms.png` shows the new pose. The guard's own comment (`capture.mjs`, the per-canvas block) states its purpose as catching a LOST WEBGL CONTEXT, which delivers precisely zero — and against that failure it works. It was never asked whether it also covers a prop that silently stops being generated, and it does not: the ground beneath the prop is opaque either way.",
    "impact": "The instrument reads as broader coverage than it has. A live-render pass that lost its flowers, its story tree, or any future prop to a descriptor regression would report `PALETTE CLOSED ON THE GPU` over a picture missing the thing the pass was about, exactly as the arc's earlier six-blank-panels run reported a clean closure it never tested. It cost this session no rework — the identical counts were noticed and chased down, ~10 minutes — but the next session to add a prop inherits a guard that cannot fail for the reason it will most want it to. The narrow fix is a per-canvas colour HISTOGRAM floor (a prop's own token must deliver a minimum count) rather than an opaque-pixel floor; that is adjudication's call, not capture's."
  },
  {
    "id": "promote-fixture-teardown-enotempty-reds-unrelated-prs",
    "title": "A temp-directory teardown race in build-worktree.test.ts reds CI for branches that touch nothing near it",
    "statement": "CI `verify` failed on `commitAuthored stages ONLY the declared scope` with an `ENOTEMPTY` rmdir error tearing down a temp git fixture — on a branch containing ZERO files under `packages/orchestrator`, blocking automerge until the job was re-run by hand.",
    "evidence": "PR #1491, CI run 32482362718, job 96771310730:\n\n    packages/orchestrator test: ✖ commitAuthored stages ONLY the declared scope — out-of-scope dirt never reaches the attested tree (154.840603ms)\n      [Error: ENOTEMPTY: directory not empty, rmdir '/tmp/storytree-promote-fixture-XmBXqk/.git/info'] { errno: -39, code: 'ENOTEMPTY', syscall: 'rmdir', path: '/tmp/storytree-promote-fixture-XmBXqk/.git/info' }\n    packages/orchestrator test: ℹ tests 538 / pass 535 / fail 1\n\nThe failure is in TEARDOWN, not in an assertion — the test's own subject (staging only the declared scope) never reported a problem. Confirmed unrelated to the branch, by measurement rather than assumption:\n\n    $ git diff --name-only $(git merge-base origin/main HEAD) HEAD | grep -c 'packages/orchestrator'\n    0\n\nThe package's suite ran at all only because the branch edits `.github/workflows/ci.yml`, a root path, so the affected-scope classifier correctly fails WIDE to the full `-r test`. `gh run rerun --failed` then passed with no code change (`verify pass 6m8s`), which is the definition of a flake rather than a real red.",
    "impact": "One hand-driven CI re-run and ~8 minutes of wall clock on a PR that could not have caused it, plus the diagnostic cost of proving it unrelated — which is the part that does not shrink with familiarity, because a red `verify` has to be read before it can be dismissed. It is a rmdir race on a `.git` subdirectory in a temp fixture, so it is Linux-CI-flavoured and will not reproduce on the Windows dev box where the local gate runs green; that asymmetry is what makes it expensive rather than merely annoying. Anyone whose branch touches a root path — the CI workflow, the lockfile, `docs/**`, `stories/**`, any `package.json` — forces full scope and is exposed to it. First observed instance; reinforce rather than re-file if it recurs."
  },
  {
    "id": "capture-goto-budget-assumes-a-warm-vite",
    "title": "capture.mjs applies a steady-state 30s page.goto budget to a COLD vite, so the first capture in any worktree dies blaming the page",
    "statement": "`capture.mjs` hardcodes a 30s `page.goto` budget with no override, and a cold vite transform of the R3F/three module graph exceeds it — so the FIRST capture run in any fresh worktree refuses with a timeout that names the page rather than the transform.",
    "evidence": "Measured 2026-08-22 in a fresh worktree with a cold vite. `capture.mjs:79` calls\n`page.goto(URL, { waitUntil: 'load', timeout: 30_000 })` with the budget hardcoded and no\noverride. The first capture against `http://localhost:5184/directions.html` died with\n`page.goto: Timeout 30000ms exceeded. Call log: - navigating to \".../directions.html\",\nwaiting until \"load\"` and wrote no output directory at all. `curl` had already returned 200\nfor the same URL seconds earlier, so the server was up — what exceeded the budget was vite's\nFIRST transform of the R3F/three module graph, which is per-worktree and is not shared with\nany other checkout.\n\nA throwaway Playwright script that loaded the same page with a 300s budget then reported\n`loaded in 477 ms` and `settled in 7791 ms`, and the very next `run capture` against the same\nwarm server exited 0 and wrote every file. So the 30s budget is far above the steady-state\ncost and far below the one-off cold cost, and nothing in the failure names the difference.\n\nThe warm-up itself cost a second detour: a scratchpad-resident `.mjs` cannot resolve\n`@playwright/test`, so it had to be copied into `packages/forest-world-r3f/` and deleted again\n— a temporary file inside a worktree whose diff is under review.",
    "impact": "Cost this session ~6 minutes and a throwaway warm-up script placed inside the package. The\nlarger cost is the misattribution the message invites: a `page.goto` timeout naming the page\nreads as \"the harness page is broken\" or \"the dev server did not come up\", and both of those\nare expensive things to start investigating — this arc has already had to correct five\ninstrument errors of the shape \"the measurement was right about the wrong object\", and a\nharness that refuses on its own cold start is that shape pointed at the operator.\n\nIt fires on the FIRST capture from any fresh worktree, which is every art or evidence session\non the chapter2 arc, and it fires hardest on a busy box (four concurrent gates were running).\nNothing tells the operator to warm the page, and the fix that suggests itself from the message\n— restarting vite — makes it worse by discarding the transform cache."
  },
  {
    "id": "bun-off-path-reds-the-gate-on-a-green-package",
    "title": "Bun installed but off PATH makes the gate read RED on a green package",
    "statement": "`pnpm gate` reports `packages/proof-protocol test: Failed` on this dev box and takes the whole gate RED, over a package that is green. The cause is PATH, not code: that package's own test script is `bun test src/` (the `bun-runtime-migration-arc` conversion), and Bun 1.4.0 IS installed here at `C:\\Users\\mickh\\.bun\\bin\\bun.exe` — the exact build CI pins via `oven-sh/setup-bun@v2` — but it is on NEITHER the Bash tool's PATH nor PowerShell's.",
    "evidence": "MEASURED 2026-08-23 on `claude/antislop-drive`, gate log `.gate-logs/gate-20260823-092506-46052.log`. The leg died with `'bun' is not recognized as an internal or external command`; `pnpm -r --no-bail test` exited 1 after 12m30s; the runner printed GATE RED with every other step PASS or SKIP. `where.exe bun` and `(Get-Command bun).Source` both return nothing, while `ls ~/.bun/bin` shows `bun.exe` and `bun --version` from that path reports 1.4.0. With `export PATH=\"$PATH:/c/Users/mickh/.bun/bin\"` the same package ran 34 pass / 0 fail in 195ms, and `pnpm gate --rerun-failed` returned the leg to PASS in 15m10s with the tree unchanged. CI is unaffected: `.github/workflows/ci.yml` installs Bun 1.4.0 explicitly and its own comment notes the pin matches the dev box.",
    "impact": "Each rediscovery costs a full `pnpm -r test` leg — 12 to 15 minutes here — plus the diagnosis, and it presents exactly as a real red: a named package, a `Failed` line, a non-zero gate. The failing package is one almost no branch touches, so the natural first suspicion is the merge just taken from `main`; nothing in the message names PATH. The signature is a CI-green / laptop-red split, which is the SAME signature CLAUDE.md tells a session to treat as a stale branch — so the documented first move actively points away from the cause. The remedy is environmental (put `~/.bun/bin` on the machine PATH, or have the gate runner prepend it when the plan contains a bun-backed script); reverting proof-protocol's test script would undo a decided conversion CI is already built for."
  },
  {
    "id": "uat-run-re-observes-the-same-command-once-per-leg",
    "title": "`uat run` re-runs a story's whole proof command once per criterion, where `adopt` runs it once",
    "statement": "The live route for signing machine UAT legs re-observes the identical, deterministic command once per leg, turning a single ~6-minute walkthrough into a ~13-run, ~80-minute serial job.",
    "evidence": "`packages/drive/src/adopt.ts` wraps its runner in `memoizeObserve` (line ~344) with a comment naming this failure verbatim: \"Wrap an `observe` runner so each DISTINCT command runs at most ONCE per adopt (the promise is cached). A story's machine UAT legs are observed against the SAME suite their covering observe gate runs, so without this the agent suite would re-run per leg; with it, one clean observation greens the gate AND every leg it covers. Sound because adopt observes a single clean HEAD — the command is deterministic.\" `packages/cli/src/uat.ts` builds the same `SignMachineCriteriaArgs` but passes `observe: deps.observe` raw (line ~347) — no memoization. Measured 2026-08-23 on branch `claude/lucid-chatelet-9c9cae` signing the `studio` story: all 13 legs bind the one gate `studio#gate-1` = `pnpm --filter studio uat`, which takes 5.3m standalone (6 passed). `storytree uat run studio --pg` reported 2 of 13 legs `proven=✓` after ~14 minutes of wall clock, i.e. ~7 minutes per leg, with Playwright (pid 38316) and a live Chromium observed re-driving the suite each time. Projected total ~80 minutes for what one observation could settle.",
    "impact": "It is paid per story, and it scales with the thing we want more of — the more acceptance criteria a story declares, the worse it gets, so the walkthroughs with the best coverage are the most expensive to sign. `studio` is the corpus's only full end-to-end journey and its 13 legs cost ~80 minutes of serial browser time instead of ~6. That is a session-length tail on every studio landing that re-signs, it holds the session non-inert for over an hour after the PR has already merged, and it burns a shared dev box the whole time (`storytree own --all` shows it LIVE, so siblings read the box as busy). ADR-0417 D2 made `uat run` the live route for machine legs and `adopt` now REFUSES them (ADR-0423), so every future machine-leg signing takes this path — the one that did not inherit the fix. The remedy is small and already written: apply `adopt`'s `memoizeObserve` to the `uat run` path."
  },
  {
    "id": "doctor-claude-login-fails-a-correctly-provisioned-vault-box",
    "title": "`doctor --dev`'s `claude-login` FAILs a correctly-provisioned box and prescribes a retired invariant",
    "statement": "`storytree doctor --dev` reports `[FAIL] claude-login — no logged-in Claude CLI detected` on a machine whose Claude credential is correctly hydrated from Google Secret Manager, because the probe observes only a browser-logged-in CLI (`~/.claude/.credentials.json`), and its fix hint instructs the reader to run `claude` and sign in \"— storytree never handles your credential (ADR-0207 D3)\", the exact invariant ADR-0430 retired.",
    "evidence": "MEASURED 2026-08-24 on the newly provisioned Mint box (`second-box-absorbs-the-expensive-work-arc-inc-02`). The box was provisioned per ADR-0430 / `docs/machine-onboarding.md` §2.2: the token was read from Secret Manager (`claude-code-oauth-token`, project `storytree-498613`) rather than minted, and placed in `~/.storytree/secrets.json`, which `packages/drive/src/secrets.ts:30` hydrates into `CLAUDE_CODE_OAUTH_TOKEN`. `~/.claude/.credentials.json` is correctly ABSENT — §2.2 explicitly says \"Do NOT run `claude setup-token` here\", because minting a second token could invalidate the one the other box depends on. `doctor --dev` nonetheless reported 15 passing / 1 failing, the single failure being `claude-login`. The probe and its hint are `packages/cli/src/doctor.ts:375-386`. ADR-0430's own Consequences predicted precisely this class of breakage but named the WRONG probe: \"`storytree doctor --dev`'s `secrets-file` probe changes meaning and must be re-pointed\" — `secrets-file` was re-pointed and passes; `claude-login` was not.",
    "impact": "It converts the one command the onboarding guide offers as the machine-level verdict into a source of false negatives on exactly the setup the current decision prescribes — and it does so at the moment of maximum uncertainty, when an agent or a new machine cannot yet tell a real defect from a stale probe. The prescribed remedy is worse than the false alarm: following the hint means running `claude setup-token`, which §2.2 forbids in bold because a second token may revoke the first and take the OTHER dev box down. So a reader who trusts doctor over the guide can turn a green machine into two broken ones. Whoever provisions machine three hits it first, and a probe that cannot be trusted on a good box teaches sessions to discount doctor generally, which is the durable cost."
  },
  {
    "id": "rerun-failed-flake-label-is-blind-to-live-store-writes",
    "title": "`gate --rerun-failed` calls a legitimately-fixed red 'infrastructure noise' when the fix was a live-store write",
    "statement": "`pnpm gate --rerun-failed` reports FLAKE SIGNATURE — 'Nothing was fixed in between, so that red is not attributable to this branch's code — it is infrastructure noise, not a defect' — whenever a fail→pass happens with git unchanged. But several gate rungs read the LIVE STORE, not the repo, so a session that repairs one with a store write gets a confident, wrong verdict telling it there was nothing to fix.",
    "evidence": "Hit 2026-08-25 on `claude/hungry-germain-862a06`. `pnpm gate` reported `FAIL pnpm check:verification-decay` — its `decision-source-drift` instrument (ceiling 0) found `adr-0121 — the span behind [buildNodeReal takes no claim of its own] HAS MOVED`, because merging origin/main brought ADR-0446's write-fence record into `buildNodeReal`. The correct remedy per ADR-0438 is `storytree adr rebind 121 --pg`, which hand-attests the claim and re-freezes the anchor — a write to the `adr-0121` STORE ROW, touching no repo file. `pnpm gate --rerun-failed` then printed: `FLAKE SIGNATURE pnpm check:verification-decay — FAILED ... and PASSED here, with HEAD and the working tree PROVABLY unchanged between the two runs. Nothing was fixed in between ... it is infrastructure noise, not a defect.` Something WAS fixed; it simply was not in git. The instrument's own ATTRIBUTION prose already names this blind spot for its findings ('an anchor ADDED by a live-store write in this session ... reads as inherited too, because git cannot see a store row'), so the gap is known at the instrument layer but not at the rerun-comparison layer.",
    "impact": "It tells a session the opposite of the truth at the exact moment it is deciding whether a red is real, and it does so in the confident register reserved for a provable claim. Two failure modes follow: a session that BELIEVES it dismisses a genuine store-side red as noise and lands on it; a session that DISBELIEVES loses the value of the whole flake-signature feature. It reaches any rung whose subject is the live store — `check:verification-decay`, `check:adr-health`, `check:guidance`, `check:agents` — which is four of the fourteen planned steps. Cheapest honest fix: make the FLAKE SIGNATURE verdict conditional on the failed step not being store-reading, and say `PASSED ON RERUN — the tree was unchanged, but this step reads the live store, which this comparison cannot see` for the ones that are. That is the vocabulary `--rerun-failed` already has for the case it cannot establish."
  },
  {
    "id": "bun-test-runs-a-mis-parameterised-instrument-to-completion",
    "title": "A measurement written as a test runs its arguments in the wrong order and publishes a plausible number",
    "statement": "A measurement instrument written as a test file called `buildRelaxedCells(draw, MESH_TUNING, mode, [new Set()])` against a signature of `(drawTiles, wheatSets, mode, override)` — the tuning object landed in the `wheatSets` slot and an array landed in `override` — and it RAN, produced a full sweep, and reported a headline percentage, because `packages/forest-world`'s `test` script is `bun test src/` and Bun transpiles without checking types. A measurement that cannot fail loudly on a wrong call is a measurement that publishes whatever it happens to compute.",
    "evidence": "The wrong call sat in `packages/forest-world/src/drift-containment.test.ts` and in a scratch probe beside it. `npx tsx --test src/drift-containment.test.ts` reported 9 pass / 0 fail on it, twice, across two sweeps whose numbers I then wrote into a commit message, a `scene.ts` doc comment and a test header. It surfaced only at gate step 10: `packages/forest-world typecheck: src/drift-containment.test.ts(193,30): error TS2740: Type 'SubstrateTuning' is missing the following properties from type 'readonly ReadonlySet<string>[]': length, concat, join, slice, and 26 more.` Corrected and re-measured on the same sweep, the published figures moved: escape rate 10.24% -> 10.21%, concentration 8.30x -> 8.20x, and the per-bed breakdown moved much further — 3.87% -> 5.55% at the low end, 30.00% -> 22.18% at the high end.",
    "impact": "About 30 minutes: two sweeps re-run, plus edits to a commit message, a source doc comment, a test header, and an already-pushed submodule commit that had to be amended and force-pushed. The cost that matters is not the time. This repo has a standing rule that a cited measurement must be REPRODUCED before it is built on (`increment-premise-can-be-refuted-at-its-own-source`, filed after two increments on this same arc cited figures that did not travel). I followed that rule, re-measured with my own instrument, and still published three wrong numbers, because the instrument itself was never type-checked. The direction and the class held; three of six figures did not. Anyone writing a probe as a test file in any bun-test package hits exactly this, and the failure mode is confident output rather than an error. The narrow remedy is one line in a session's loop — run `pnpm --filter <pkg> typecheck` on a measurement file BEFORE trusting its output, not at gate time — but whether that belongs in guidance, in the probe idiom, or nowhere is the adjudicator's call."
  },
  {
    "id": "mutation-remedy-omits-deleting-the-unobservable-branch",
    "title": "The mutation rung offers two remedies and the best one is neither: delete the branch no test can observe",
    "statement": "`check:mutation-diff` closes every failure with \"Strengthen this branch's own tests until each mutant above is caught, or record why the mutant is equivalent and cannot be killed\" — two options, and on three of this session's four survivors the correct answer was a THIRD one the text never names: the mutated branch was UNOBSERVABLE because the behaviour it guarded was already enforced somewhere else, so the fix was to delete the branch, not to test it or to excuse it.",
    "evidence": "PR for `render-depends-on-from-the-field`, 2026-08-28. `pnpm check:mutation-diff` reported four survivors in `packages/library/src/depends-on-edges.ts`:\n\n    SURVIVED depends-on-edges.ts:91  [ArrayDeclaration]\n    SURVIVED depends-on-edges.ts:103 [StringLiteral]\n    SURVIVED depends-on-edges.ts:104 [ConditionalExpression]\n    SURVIVED depends-on-edges.ts:104 [StringLiteral]\n\nThree of the four (103, 104 twice) were one defect wearing three mutators: the function tested `if (id === \"\" || seen.has(id)) continue;` to drop a pointer whose scheme it could not offer — but such a pointer ALSO resolves to no artifact two lines later and is dropped there. The behaviour was double-enforced, so no input could distinguish the guard's presence from its absence and no test could ever have killed those mutants. Deleting the guard removed all three at once and left the function shorter and its rule stated once. The fourth (91) was the same shape in the ordering pass: an intermediate array a placeholder element could enter without reaching the output; bucketing by group as edges arrive removed it.\n\nThe session's first instinct, taken straight from the printed text, was to write tests for the guard — which cannot work — and then to reach for `// Stryker disable next-line ... EQUIVALENT`, which would have preserved dead code behind a comment asserting it was fine. One genuine equivalence did survive the restructure (`/^asset:/` anchored vs unanchored) and was recorded that way, which is the option working as intended.",
    "impact": "One gate cycle (~2 min for the rung, inside an 18-minute full gate) spent reaching for the wrong two remedies first, and a near miss on the worse outcome: an `EQUIVALENT` disable comment that would have frozen a redundant branch into the file permanently, since the marker tells every later reader the line is settled. That is the opposite of what the rung is for.\n\nWho hits it next: every session the rung reds, which since `mutation-rung-in-ci` is every session changing mutable TypeScript. The remedy is one clause in `packages/cli/src/mutation-diff.ts`'s closing message — name the third option FIRST, because it is the only one that makes the code better: a mutant no test can kill is usually telling you the branch is unobservable, so ask whether it can be deleted before you excuse it. `mutation-diff.ts`'s own source already models this well (its `EQUIVALENT` markers are genuine narrowing arguments, not dead code), so the knowledge exists — it just is not in the message the failing session reads."
  },
  {
    "id": "kit-island-reproduce-command-names-an-uncommitted-asset",
    "title": "The kit-island measurement's reproduce command names an asset that is not committed",
    "statement": "The reproduce command printed at the top of docs/research/chapter2-kit-island-2026-08-28/README.md cannot run on a fresh checkout. harness/kit-island-measure.mjs:134 opens `/assets/dressing-webp90-${COMMITTED_RUNG}.glb` with COMMITTED_RUNG defaulting to 128, so it requests /assets/dressing-webp90-128.glb. The only committed kit asset is harness/assets/dressing-kit.glb (351,416 B). The file is not gitignored — it simply does not exist in the repo, so the failure is a 404 from vite rather than anything naming the real cause. The module's own comment explains that the OTHER texture rungs are absent because the kit is ~900 MB and lives on the owner's box, and it correctly defaults the RUNGS sweep to a single rung for that reason; the committed-rung URL was not given the same treatment.",
    "evidence": "Verified in worktree claude/land-crowd at commit ddd3628d, 2026-08-28. `grep -n 'dressing-webp90' packages/forest-world-r3f/harness/kit-island-measure.mjs` gives lines 134 and 256, both building `/assets/dressing-webp90-<rung>.glb`. `ls packages/forest-world-r3f/harness/assets/` gives exactly two files: dressing-kit.glb and pine-01.glb. `git check-ignore -v` on the requested path prints nothing, so it is absent rather than deliberately excluded. The sibling page written this session (harness/crowd-page.ts) uses KIT_ASSET_URL from kit-scene.ts — which is '/assets/dressing-kit.glb', the committed one — and runs from a fresh checkout, which is what makes the contrast a defect in one and not a missing asset in both.",
    "impact": "A committed reproduce command that cannot be reproduced. The next session that tries to re-derive or extend the whole-island kit measurement gets a 404 whose message names neither the missing asset nor the committed one, and has to read the driver to discover that ST_KIT_COMMITTED_RUNG must be overridden or the URL changed. The measurement's numbers are unaffected and were taken against a real asset; only the path back to them is broken. Cost is bounded — one session, some minutes of confusion — but it falls on exactly the reader the evidence directory exists for."
  },
  {
    "id": "check-land-art-cannot-pass-on-a-fresh-worktree",
    "title": "check:land-art reds on any fresh worktree and is not in CI, so a docs-only branch cannot gate green",
    "statement": "A merge-blocking-looking local gate rung cannot pass on a fresh worktree regardless of the branch's content, so `pnpm gate` cannot go green for a change that touches nothing it measures.",
    "evidence": "2026-08-29, PR #1714, a branch whose entire diff is four markdown files under docs/research/. `pnpm gate` reported `[12/24] FAIL: pnpm check:land-art` (2.8s, exit 1) with `COVERAGE — grain.html — could not read what was audited: ENOENT … .capture-scratch/land-art/grain/capture-report.json` for grain, island and directions, plus an ERR_UNSUPPORTED_ESM_URL_SCHEME stack. It reproduces with the branch's own commits removed (i.e. at origin/main), so it is not branch-caused. `grep -c 'check:land-art' .github/workflows/ci.yml` returns 0 — CI never runs it. check:land-art is in SKIP_CAPABLE_CHECKS but did not take its skip branch on this state.",
    "impact": "Every session on a fresh or pooled worktree meets a GATE RED it did not cause and must spend time attributing before it can honestly open a PR — this session spent a full extra gate cycle plus a with-my-change-removed control run to prove it was pre-existing. Because a red is indistinguishable from a real one until investigated, the standing risk is the opposite: a session learns to wave this rung through and stops reading the row."
  },
  {
    "id": "discharged-by-loses-its-fence-exemption-when-arc-is-also-passed",
    "title": "`--discharged-by` loses its parked-entry exemption when `--arc` is passed too, and the refusal tells you to mint a second increment",
    "statement": "`friction route --discharged-by` is documented as exempt from the parked-entry fence, but passing `--arc` alongside it re-arms the fence - and the closing leg closes the increment BEFORE the stamp, so the entry the fence wants is gone by then. The refusal's printed remedy is to mint a second increment for work that already landed.",
    "evidence": "2026-08-30, closing leg of PR #1734, stamping the delivery ref on two `tool` routings whose remedy\nhad just merged.\n\n  storytree friction route <id> --route tool --reason @f.txt --arc verification-integrity-arc \\\n      --discharged-by pr:1734 --pg\n  -> arc \"verification-integrity-arc\" carries no PARKED entry naming this friction item (ADR-0298 D2).\n     ... Park it first, then route:  storytree arc increment new ...\n  exit 1\n\nThe same command with `--arc` DROPPED succeeded on the next attempt:\n\n  storytree friction route <id> --route tool --reason @f.txt --discharged-by asset:adr-0483 --pg\n  -> discharged by asset:adr-0483 - the routed remedy has landed.\n\nBoth items were ALREADY routed `tool` against that arc earlier in the same session, while the\nincrement was still parked. The increment was then closed with `--pr 1734` (merge-ceremony step (a)),\nwhich is what removed the open entry the fence looks for.",
    "impact": "`--discharged-by` is documented as exempt from the ADR-0298 D2 parked-entry fence - \"A routing that\ncarries --discharged-by is exempt: the remedy already landed\" - but the exemption is defeated by also\npassing `--arc`, and `--arc` is what the earlier refusal's own remedy line told you to pass. So the\nordering the merge ceremony prescribes walks straight into it: the closing leg says close the\nincrement first (step a), and the discharge stamp then has no open entry to point at.\n\nThe refusal is actively misleading at that moment. It says \"park it first\" and prints an\n`arc increment new` command - which, followed literally, mints a SECOND increment for work that has\nalready landed, on an arc whose log is the permanent record. That is a durable wrong object, not a\nlost minute. What it should say is that the item is already routed and the stamp needs no arc.\n\nCosts about five minutes and one moment of believing the ceremony contradicts itself. Hits any\nsession that routes `tool` and later returns to stamp the landing - which is the normal shape,\nbecause `--discharged-by` exists precisely for the case where the remedy lands after the routing."
  }
]

Now output the JSON array.

# Exactly reproducible from the seed by the README command; ~2.7 MB of intermediate each.
raw/
raw2/
raw*/
# Variant outputs from the exploratory flags (--framing, --skeleton, --crown-normals).
# Reproducible the same way; only the comparison SHEET they feed is committed.
frames[A-Z]*/
work/
registry-block.ts.txt
__pycache__/

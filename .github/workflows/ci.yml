name: CI

# Every change reaches main through a pull request. On each pull request into main, `platforms`
# installs, typechecks and runs the tests on Linux and on macOS, against the merge of the branch
# with main, and `verify` passes once both have. Then `automerge` merges the pull request. Branch
# protection on main requires `verify`, so nothing merges unless the tests passed on both systems.
#
# The product ships for Windows first. The macOS run keeps the code working there from the day each
# line is written, so that Mac support later is packaging rather than porting (ADR-0625 D7, in
# storytree 0.2's decision log).
#
# A draft is never merged: open a pull request as a draft, or convert it to one, to hold it, and
# `gh pr ready` releases it (`verify` runs again, and the pull request merges when it passes). A
# pull request that carries the `hold` label when its run starts is not merged either. A pull
# request from a fork is checked, never merged automatically.
#
# `verify` also runs on each push to main. A merge that `automerge` makes starts no such run:
# GitHub starts no workflow from a push made with a workflow's own token.

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]

# A newer push to the same pull request, or to main, cancels the run it supersedes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # One run per system, each installing, typechecking and testing. A failure on one system leaves
  # the other running (fail-fast: false), so a failure on one alone shows as that system's.
  platforms:
    name: verify on ${{ matrix.system }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - system: Windows 1
            runner: windows-latest
          - system: Windows 2
            runner: windows-latest
          - system: Windows 3
            runner: windows-latest
          - system: Windows 4
            runner: windows-latest
          - system: Windows 5
            runner: windows-latest
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
      # pnpm 9.15.0, from package.json's packageManager field.
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      # Throwaway measurement, never merged: the whole suite 7 times, to count the Windows flake.
      - name: The suite, seven times
        shell: bash
        run: |
          node --version
          failed=0
          for i in 1 2 3 4 5 6 7; do
            start=$(date +%s)
            if pnpm test > "run-$i.log" 2>&1; then verdict=pass; else verdict=FAIL; failed=$((failed + 1)); fi
            echo "run $i: $verdict in $(( $(date +%s) - start )) s; $(grep -E '^ℹ (pass|fail|skipped) ' "run-$i.log" | tr '\n' ' ')"
            grep -E 'reset the connection before its refusal arrived' "run-$i.log" | sed "s/^/  retried: /" || true
            if [ "$verdict" = FAIL ]; then
              grep -n -E 'ECONNRESET|✖|not ok|Error' "run-$i.log" | head -40
              tail -60 "run-$i.log"
            fi
          done
          echo "failed runs: $failed of 7"

  # The check branch protection on main requires, and the one `automerge` waits for. It passes only
  # if every run in `platforms` passed. It runs whatever they did (always()), so a run that failed
  # or was cancelled fails it: were it skipped instead, GitHub would count the skipped check as
  # passed.
  verify:
    name: verify
    needs: platforms
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Every system passed
        env:
          RESULT: ${{ needs.platforms.result }}
        run: |
          echo "platforms: $RESULT"
          test "$RESULT" = success

  automerge:
    name: automerge
    needs: verify
    if: >-
      github.event_name == 'pull_request' &&
      !github.event.pull_request.draft &&
      !contains(github.event.pull_request.labels.*.name, 'hold') &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      pull-requests: write
    steps:
      # --match-head-commit: merge the commit `verify` passed, never one pushed since. --auto: if
      # main's protection still holds the merge back, GitHub's auto-merge makes it once it allows;
      # otherwise gh merges now. --delete-branch: the repo's own "automatically delete head
      # branches" setting left #1's branch in place after this job merged it, so gh deletes the
      # branch itself.
      - name: Merge
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
          SHA: ${{ github.event.pull_request.head.sha }}
        run: gh pr merge "$PR" --repo "$GITHUB_REPOSITORY" --auto --merge --match-head-commit "$SHA" --delete-branch
